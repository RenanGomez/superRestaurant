# PROJECT_NOTES

- Publicación Git financiera 2026-09-03: el corte aprobado de caja y pagos simples se publicó por fast-forward en `origin/main` como `b421c917e01f75b222cb3391876df145f9aff1ee`, partiendo de `83935bfff5bd0232d8b5b064b1d5b6c433ef3b77`; sin merge, rebase ni force-push.
- Aprobación financiera P1 2026-09-03: Emmanuel aprobó el corte de caja y pagos simples y pasa REVIEW → DONE. La migración `20260903000200` continúa sin aplicar; cualquier apply, E2E poblada o cambio de credenciales conserva su autorización separada. La siguiente tarea elegible es “Implementar cobro y cortes X/Z en web”, comenzando por inspeccionar el shell POS, contratos financieros y consumidores existentes sin inventar moneda, reglas fiscales, CFDI ni proveedor de pagos.
- Caja y pagos simples 2026-09-03: el corte server-side/API implementa apertura, cobro en efectivo, tarjeta manual confirmada externamente y cierre, con dinero entero exacto, replay previo a versiones, auditoría, aislamiento por Restaurant/Branch y ledger inmutable preparado para compensaciones. `card_manual` conserva solo proveedor, terminal y referencia opcional; no almacena PAN/CVV ni representa autorización del POS. La migración `20260903000200` permanece local y sin aplicar. La primera ejecución efectiva del rollback-only alcanzó el auditor y revirtió por SQLSTATE `42883`; la causa fue calificar `coalesce` como función de `pg_catalog` en el auditor local. Tras corregir sus dos apariciones, la segunda ejecución autorizada terminó `status=ok` con 5 políticas, 23 tablas RLS+FORCE RLS y 21 funciones `SECURITY DEFINER`, siempre dentro de rollback. El contrato estático volvió a pasar 25 pruebas. Caja/pagos pasa a REVIEW; no hubo cambios de credenciales, provisioning, fixtures, Data API, commit ni push.

Última actualización: 2026-09-03

## Estado del repositorio

- Aprobación humana KDS 2026-09-03: Emmanuel aprobó el corte verificado en `runId=f63ca754-f1e4-4bb3-b574-ecd88e4fc45a`; “Crear KDS mínimo por estación” pasa de REVIEW a DONE. Sus límites permanecen: Fase 1 es online-first, Socket.IO notifica y REST/cursor recupera, y no existe cola offline de acciones KDS.
- Publicación Git KDS 2026-09-03: después de un fetch sin cambios remotos, el corte funcional/documental KDS se publicó por fast-forward de `origin/main@297176c` a `f49d691` (`feat: complete kitchen display workflow`). No hubo merge, rebase, force-push ni incorporación de historia ajena. Un commit documental posterior registra este hash y las instrucciones de continuidad.
- E2E y smoke KDS final 2026-09-03: la ejecución autorizada `runId=f63ca754-f1e4-4bb3-b574-ecd88e4fc45a` terminó `status=ok`, 150 checks, `kdsTicketsVerified=true`, `ordersRealtimeVerified=true`, `menuCatalogVerified=true`, `protectedKdsSmoke=true`, `fixtureRowsRemoved=true` y `fixtureUsersRemoved=2`. Validó tickets vacíos/poblados, snapshots/versiones, `pending` excluido, `sent/preparing/ready`, retiro `delivered`, aislamiento de estación/sucursal/tenant/false-pair, Socket.IO, cursor durable y revocación. El navegador real verificó `sent`, `ready`, pantalla vacía tras entrega y retorno a login tras 403 en 1280×720 y 390×844, con consola limpia. La vista móvil reveló `scrollWidth=1110` por mínimos intrínsecos del selector; `controls`, labels, inputs y columnas se acotaron con `min-width:0`/`minmax(0,1fr)`, y el mismo run confirmó `scrollWidth=clientWidth=390`, targets de 46/54 px y desktop sin overflow. El postcheck final confirmó catálogo íntegro, runtime y cero sesiones. API pasó lint, typecheck, build y 194 pruebas; KDS pasó lint, typecheck, build y 9 pruebas. La habilidad Browser permitió detectar/corregir el defecto visual que las pruebas lógicas no cubrían. KDS pasa a REVIEW; no hubo recovery, repetición, migración adicional, Data API, rotación/provisioning, offline ni Git remoto antes del commit solicitado.
- Primer lanzamiento E2E KDS 2026-09-03: precheck `post_kds_v1` verde y clave `publishable` única cargada solo en memoria. El runner fue rechazado con `TENANCY_VERIFICATION_CONFIGURATION_REJECTED` antes de emitir `runId`: `runTenancyVerification` implicaba Orders/Realtime pero aún elegía las marcas del auditor post-órdenes, por lo que rechazó correctamente el SQL post-KDS antes de crear el plan, pools, usuarios o filas. No se repitió. Se añadió `verifyKdsTickets` como perfil explícito que implica Orders/Realtime y exige las dos marcas post-KDS; una compuerta local probó que el auditor llega a `onStart`, y API volvió a pasar lint, typecheck, build y 190 pruebas. CodeGraph mantiene el impacto en seis símbolos esperados del arnés. Una lectura administrativa y Auth confirmó `markedRows=0`, `markedUsers=0`; el postcheck final volvió a confirmar catálogo íntegro, `app_api` runtime y cero sesiones. Requiere autorización nueva para ejecutar el runner corregido; no hubo migración, Data API, cambio de credenciales, recovery, navegador funcional ni Git remoto.
- Preparación E2E KDS 2026-09-03: `verify:kds:remote` amplía el mismo harness recuperable de Orders/Realtime para comprobar `GET /api/v1/kds/tickets` sin autenticación, vacío, exclusión `pending`, snapshots y versiones exactos en `sent/preparing/ready`, retiro en `delivered`, aislamiento por estación/sucursal/tenant/false-pair y rechazo después de revocar la membresía. `verify:kds-protected-smoke:remote` pausa ese mismo run en `sent`, `ready` y `revoked`; publica credenciales temporales solo en la primera fase y elimina lease/ack al cerrar. El proxy Vite acepta únicamente HTTP local y permite dirigir la UI al puerto API aislado 4312. API pasó lint, typecheck, build y 190 pruebas en 30 archivos; KDS pasó lint, typecheck, 9 pruebas y build. CodeGraph quedó actualizado con 213 archivos, 3,822 nodos y 10,995 relaciones; los impactos están contenidos en los runners/coordinador y Vite esperados. No se ejecutó remoto, no se crearon usuarios/filas, no hubo nueva migración, Data API, credenciales persistentes, offline ni Git remoto. Requiere autorización separada para una sola ejecución con cleanup obligatorio.
- KDS apply 2026-09-03: Emmanuel autorizó aplicar únicamente `20260903000100_create_kds_ticket_read_model.sql`. El enlace CLI del checkout raíz apuntaba al proyecto prohibido `cxcnnhafchqslvgvkeye`, por lo que no se usó ni modificó. Un staging temporal reunió las 13 migraciones fuente; la conexión administrativa al proyecto correcto confirmó las 12 versiones previas y el dry-run anunció exclusivamente KDS, sin seeds ni roles. El push por URL explícita aplicó solo esa versión. El postcheck `post_kds_v1` devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`; el historial contiene exactamente 13 versiones y `db lint --level error` no encontró errores en `app`, `app_private` o `app_rls`. El staging fue eliminado. No hubo cambios de Data API, credenciales, provisioning, fixtures o Git remoto. Siguiente acción: preparar la E2E KDS poblada/revocación y solicitar autorización separada para ejecutarla.
- KDS rollback-only verde 2026-09-03: tras un precheck limpio, la única segunda ejecución autorizada de `verify:kds-ticket-schema:rollback` completó migración y ambos auditores dentro de una transacción, devolviendo `status=ok`, 5 políticas, 19 tablas RLS+FORCE RLS y 16 funciones `SECURITY DEFINER`. El rollback fue obligatorio y el postcheck confirmó nuevamente `app_api` runtime, catálogo post-órdenes íntegro y cero sesiones. No hubo apply ni otra mutación remota. Siguiente límite: autorización separada para aplicar únicamente `20260903000100`; después ejecutar el verificador read-only `post_kds_v1` y la E2E KDS.
- KDS rollback-only, primer intento 2026-09-03: el precheck read-only confirmó `app_api` runtime, catálogo post-órdenes íntegro y cero sesiones. La ejecución autorizada de `verify:kds-ticket-schema:rollback` aplicó la migración dentro de la transacción y falló al iniciar `catalog_audit` con SQLSTATE `42883`; el runner revirtió y el postcheck volvió a confirmar el baseline intacto. La causa fue calificar `COALESCE` como `pg_catalog.coalesce` en migración/auditores; todas las apariciones se corrigieron y `schema-contract` impide reintroducirlas. También se añadió el perfil pinneado `post_kds_v1`, su runner read-only y allowlist exacta con `list_kds_tickets`; al hacerlo se corrigió el pin histórico de los perfiles post-órdenes en provisioning/recovery. API pasó lint, typecheck, build y 187 pruebas ejecutadas en serie. No hubo apply, fixtures, credenciales, Data API ni Git remoto. La viñeta anterior registra la segunda prueba ya completada.
- E2E Orders/Realtime 2026-09-03: la única ejecución autorizada (`runId=e3e3cfca-2ed1-4743-b51a-33c526642509`) pasó 150 checks con órdenes/Realtime y catálogo verificados, cleanup de filas y dos usuarios temporales. El postcheck final confirmó `app_api` runtime, catálogo post-órdenes íntegro y cero sesiones; el slice pasa a REVIEW. No hubo repetición ni recovery.
- Decisión KDS 2026-09-03: los eventos durables siguen siendo notificaciones/cursor y no se convierten en una segunda autoridad de tickets. El KDS obtiene un read model server-only desde el agregado Order vigente, con snapshots históricos del ítem y `orderVersion` para transiciones optimistas; Socket.IO solo dispara refresh y REST recupera el cursor antes de releer tickets. El cliente no ofrece cola offline en Fase 1: una acción fallida se muestra como no guardada. La implementación local, auditorías y runner rollback-only existen; el primer rollback remoto revirtió por un defecto ya corregido en el auditor, por lo que la migración post-KDS aún no está validada ni aplicada persistentemente.

- Preparación E2E Orders/Realtime 2026-09-03: `verify:orders-realtime:remote` reutiliza el harness marcado de tenancy y el auditor global post-órdenes. Cubre rechazo sin autenticación, creación/replay/conflicto idempotente, denegaciones viewer/cross-tenant/false-pair, alta y apertura de Order, notificación Socket.IO, suscripción única, cinco eventos KDS durables, recuperación REST paginada por cursor, aislamiento por estación/tenant y rechazo tras revocación. Antes de crear una Order confirma que `orders`, `order_audit_events` y `kds_events` siguen inaccesibles por Data API para anon, authenticated y secret key. La limpieza `finally` verifica propiedad y prefijo exactos antes de borrar KDS → auditoría → Order → cursor; `recover:tenancy:remote` reconoce los prefijos parciales 1–7 y falla cerrado ante contaminación. Se añadió `socket.io-client@4.8.1`. Monorepo completo: lint, typecheck, pruebas y build verdes; `git diff --check` verde. CodeGraph quedó al día con 195 archivos, 3,627 nodos y 14,409 relaciones; los impactos del runner (2 símbolos) y recovery (4 símbolos) son los esperados. No se ejecutó la E2E ni se crearon fixtures remotos. Requiere autorización nueva para una sola ejecución; si emite `runId` y falla, ejecutar solo recovery para ese UUID y no repetir automáticamente.
- Publicación Git 2026-09-03: el corte acumulado de menú y fundamentos Orders/Realtime se publicó por fast-forward en `origin/main` como `1845c786749839f9b23e1f597efdb1deae6a5da0`, partiendo de `93d44ed`; sin rebase, merge ni force-push.
- Menú P1 fue aprobado expresamente por Emmanuel el 2026-09-02 y pasa de REVIEW a DONE; sus exclusiones funcionales documentadas permanecen sin cambio.

- Realtime P1, apply 2026-09-02: Emmanuel aprobó ADR-011 y `20260902000200` quedó aplicada persistentemente. El staging temporal reunió 12 migraciones hash-idénticas; el historial previo tenía exactamente 11 versiones y el dry-run anunció solo `20260902000200_create_orders_realtime.sql`, con seeds/roles vacíos y Vault omitido. La aplicación oficial reportó únicamente esa versión. El postcheck confirmó las 12 versiones exactas, catálogo íntegro post-órdenes, `app_api` runtime, cero sesiones y `db lint` sin errores en `app`, `app_private` y `app_rls`; el staging fue eliminado. Para fijar ese estado se añadió un runner read-only post-órdenes con auditor SHA-256 pinneado y allowlist exacta de capacidades; API pasó lint, typecheck, suite completa y build. CodeGraph quedó al día en 193 archivos, 3,562 nodos y 14,082 relaciones, con `verifyAppApiState` limitado a sus runners y pruebas esperados. No hubo Data API, credenciales, provisioning, fixtures, E2E, commit ni push. P1 continúa IN_PROGRESS; el siguiente límite es preparar y autorizar separadamente la E2E recuperable de órdenes/Realtime.
- Smoke protegido final de menú 2026-09-02: la única ejecución autorizada emitió `runId=0347895d-f216-492b-8cae-d41c728b78ab` y terminó `status=ok` con 141 checks, `protectedMenuWebSmoke=true`, `menuCatalogVerified=true`, `fixtureRowsRemoved=true` y `fixtureUsersRemoved=2`. El navegador real verificó login, selección manager, catálogo poblado y revocación en desktop y 390×844; consola sin errores/warnings y solicitudes de login, selección, menú y pantalla revocada en 200. La primera inspección móvil detectó overflow horizontal por mínimos intrínsecos del shell/formulario; se añadieron límites `min-width`, inputs fluidos, quiebre de identificadores largos y header compacto, y se revalidó dentro del mismo run con `scrollWidth === clientWidth`, sin repetir la E2E. El postcheck read-only inicial fue rechazado localmente antes de conexión por variables públicas/CA incompletas; ya corregido, observó primero la sesión transitoria del pool y finalmente devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`. Web pasó lint, typecheck, 40 pruebas y build; CodeGraph quedó actualizado en 182 archivos, 3,318 nodos y 13,086 relaciones. P1 menú pasó a REVIEW; no hubo nuevas migraciones, Data API, rotación, provisioning, recuperación, commit ni push.

- Sexta E2E de validación de menú 2026-09-02: precheck verde y clave `publishable` única mantenida en memoria. `runId=cdcbac4a-400c-42e4-837a-a703baba55f0` terminó `status=ok` con 141 checks, `menuCatalogVerified=true`, `fixtureRowsRemoved=true`, `fixtureUsersRemoved=2`; validó aislamiento de lecturas vacías, publicación manager, persistencia exacta, replay, conflicto idempotente, lectura viewer, rechazo de escritura viewer/cross-tenant/false-pair y revocación. No correspondió recovery. Dos postchecks inmediatos observaron la sesión transitoria del pool y omitieron el auditor; el postcheck posterior confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`. Se preparó `run-menu-web-protected-smoke.ts`, que reutiliza el coordinador seguro existente con `post_menu_v1` y un catálogo poblado; su contrato, lint, tipos, suite API y build están verdes. No se ejecutó smoke ni hubo migraciones, Data API, cambios de credenciales/`app_api`, commit o push. Falta autorización separada para crear sus fixtures temporales e introducir sus credenciales en el login local.
- Quinta E2E diagnóstica de menú 2026-09-02: el precheck quedó verde y la CLI entregó en memoria una única `publishable`. `runId=90b3b194-7dde-4156-8cea-dd8593e9b426` completó fixtures y confirmó que la primera lectura manager respondió HTTP 200, cache correcta, JSON y contrato válidos y scope exacto; el único resultado fue `catalog_mismatch`. La causa quedó demostrada localmente: `valuesEqual` trataba `null` como objeto y por ello `valuesEqual(null, null)` era falso aunque PostgreSQL y HTTP devolvieran correctamente catálogo vacío. Se añadió comparación de identidad previa y regresiones con `null` directo/anidado. No se repitió la E2E; recovery exclusivo devolvió `fixtureRowsRemoved=0`, `fixtureUsersRemoved=0`, y el postcheck final confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`. API pasó lint, typecheck, suite completa y build; CodeGraph resincronizó los dos archivos. No hubo migraciones, Data API, cambios de credenciales/`app_api`, smoke, commit ni push; la siguiente E2E de validación requiere autorización separada.
- Cuarta E2E diagnóstica de menú 2026-09-02: precheck verde y clave `publishable` única obtenida en memoria mediante CLI. El runner emitió `runId=fa0e96a2-17d8-4d6d-ba7c-357f560e87ac`, creó ambos usuarios y el grafo de tenancy, validó el rechazo anónimo y persistencia vacía, y falló con `TENANCY_VERIFICATION_ASSERTION_FAILED` durante `manager_empty_read`, antes de `other_tenant_empty_read`; no se repitió. El cleanup interno fue completo y el recovery exclusivo devolvió `fixtureRowsRemoved=0`, `fixtureUsersRemoved=0`. El primer postcheck detectó la sesión transitoria del pool y omitió el auditor; el siguiente confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`. Una consulta remota read-only confirmó `emptyStateValid=true` y `executeGranted=true`, acotando la brecha al trayecto HTTP autenticado. El runner ahora publica checkpoints y diagnóstico allowlisted para transporte, status, cache, JSON, contrato, scope y catálogo; API pasó lint, typecheck, suite completa y build. CodeGraph quedó actualizado en 181 archivos, 3,307 nodos y 13,055 relaciones. No hubo Data API, cambios de credenciales/`app_api`, migraciones, smoke, commit ni push; una nueva E2E requiere autorización separada.
- Tercera E2E diagnóstica de menú 2026-09-02: la CLI volvió a tener acceso y confirmó exactamente una clave moderna `publishable`, sin revelar claves. El runner emitió `runId=a6029fff-4ad4-4f4e-b180-ba3c3464e895` y falló en `fixtures` antes de cualquier checkpoint de catálogo. La lectura TLS exacta del driver aisló SQLSTATE `28P01`: la URL administrativa guardada no autenticaba. El primer paso del runner tras emitir el UUID es adquirir el advisory lock por esa conexión, antes de crear usuarios o filas; Auth Admin confirmó además `markedUsers=0`. Tras actualizar `ADR010_DATABASE_URL`, la conexión mínima TLS devolvió `status=ok`; el recovery autorizado y exclusivo del UUID confirmó `fixtureRowsRemoved=0` y `fixtureUsersRemoved=0`. Los postchecks oficiales anterior y posterior al recovery devolvieron `state=runtime`, `catalogAudit=true`, `activeSessions=false` y `status=ok`. Se añadieron checkpoints allowlisted para creación de ambos usuarios, transacción, restaurantes, sucursales, membresías y grants; API pasó lint, typecheck, suite completa y build. No se repitió la E2E, no hubo Data API, cambios de `app_api`, migraciones, smoke, commit ni push. Una nueva E2E requiere autorización separada.
- Segunda E2E corregida de menú 2026-09-02: el runner alcanzó la etapa funcional `menu_catalog` y emitió `runId=4d0c244f-5ecf-4a9b-bdf5-4be1ce1f9a72`, pero terminó `TENANCY_VERIFICATION_ASSERTION_FAILED` antes de exponer cuál aserción interna falló. No se repitió. La recuperación autorizada y acotada al UUID confirmó `fixtureRowsRemoved=0` y `fixtureUsersRemoved=0`, evidencia de que el cleanup interno ya había eliminado el grafo completo y los dos usuarios. El postcheck read-only devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false` y `status=ok`. Para evitar otra ejecución ciega, el runner ahora emite únicamente checkpoints allowlisted y no sensibles para las operaciones de lectura, publicación, persistencia, replay, conflicto, RBAC y revocación. API pasó lint, typecheck, suite completa y build; CodeGraph quedó actualizado en 181 archivos, 3,299 nodos y 12,988 relaciones. Una nueva E2E diagnóstica requiere autorización separada; no hubo Data API, cambios de credenciales/`app_api`, migraciones, smoke, commit ni push.
- Primera E2E de menú autorizada 2026-09-02: el precheck confirmó `state=runtime`, `catalogAudit=true` y cero sesiones; la CLI devolvió una única clave `publishable` moderna sin revelar la secret. El runner fue rechazado en `configuration` antes de emitir `runId`, por lo que no alcanzó red funcional, no creó usuarios/filas y no requirió recovery. La causa fue operativa: al derivar `DATABASE_URL` desde la URL administrativa se conservó también `sslrootcert`; el contrato privado admite solo `sslmode=verify-full` porque recibe la CA por `DATABASE_CA_CERT_PATH`. La derivación corregida elimina ese parámetro únicamente de la URL de `app_api` y pasó `readTenancyVerificationConfig` real local con las credenciales actuales. El postcheck remoto volvió a confirmar catálogo íntegro, runtime y cero sesiones. No hubo repetición, Data API, cambios de credenciales/`app_api`, migraciones, commit ni push.
- Preparación E2E de menú 2026-09-02: el harness de tenancy ahora cubre lectura vacía por tenant, publicación manager, lectura viewer en otra sucursal del mismo restaurante, denegación cross-tenant/false-pair, replay idempotente, conflicto por reutilización divergente, denegación de escritura viewer y revocación. El cleanup normal y el recovery independiente reconocen y eliminan únicamente un release completo marcado ligado al `runId`, incluidos head y auditoría, con comprobación exacta previa y postcheck cero. API pasó lint, typecheck, suite completa y build. CodeGraph quedó actualizado en 181 archivos, 3,297 nodos y 12,981 relaciones; `runTenancyVerification` alcanza seis runners conocidos, recovery cinco símbolos y `MenuCatalogService` diez símbolos esperados. No se ejecutó E2E remota, no se crearon usuarios/filas y no hubo Data API, credenciales, `app_api`, migraciones adicionales, commit ni push.
- Apply de menú 2026-09-02: con autorización expresa se creó un staging temporal de 11 migraciones hash-idénticas. El historial previo fue exactamente de diez versiones y el dry-run anunció solo `20260902000100_create_menu_catalog.sql`, sin seed, roles ni Vault. La CLI aplicó únicamente esa versión; el postcheck confirmó 11 versiones exactas, `state=runtime`, `catalogAudit=true`, `activeSessions=false`, y `db lint` sin errores en `app`, `app_private` y `app_rls`. La CLI 2.109.1 en Windows rechazó inicialmente la CA indicada por URL; se mantuvo `verify-full` y se confió el mismo certificado hash-idéntico solo para el proceso mediante `SSL_CERT_FILE`/`PGSSLROOTCERT`. El staging exacto fue eliminado. No hubo Data API, credenciales, provisioning, fixtures, E2E, commit ni push.
- Rotación administrativa posterior 2026-09-02: Emmanuel restableció la contraseña y actualizó el archivo ignorado. La forma local se validó sin mostrar valores. Un primer postcheck read-only durante la propagación fue rechazado en conexión; tras una única espera y un único reintento controlado, el verificador oficial confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false` y `status=ok`. No se modificó `app_api`, no hubo provisioning ni cambio persistente; la siguiente operación requiere autorización separada para aplicar solo `20260902000100`.
- Quinta validación global post-menú 2026-09-02: la única ejecución autorizada concatenó migración, auditor específico y auditor global `post_menu_v1` dentro de una transacción contra `zwbyiefqeujstyzysydn`; terminó `status=ok` con 16 tablas seguras, cinco políticas y 12 funciones `SECURITY DEFINER`, y revirtió sin cambio persistente. No hubo repetición ni apply. Durante el preflight local previo, una búsqueda de presencia imprimió por error el valor completo de `ADR010_DATABASE_URL` en la salida de herramienta; no se versionó ni se copió a los documentos, pero la contraseña administrativa debe considerarse expuesta y rotarse antes de cualquier operación remota posterior.
- Validación y perfil global post-menú 2026-09-02: la cuarta y única ejecución autorizada de `verify:menu-catalog-schema:rollback` pasó las 41 sentencias y el auditor específico contra PostgreSQL real, con `status=ok`, 16 tablas seguras, cinco políticas y 12 funciones `SECURITY DEFINER`; el rollback terminó y no hubo cambio persistente. Después se añadió el auditor global exacto post-menú y el perfil `post_menu_v1` para verificación/provisioning/recovery de `app_api`, permitiendo únicamente `get_menu_catalog` y `save_menu_catalog` además de las seis capacidades previas. API pasó lint, typecheck, suite completa y build; CodeGraph quedó actualizado en 180 archivos, 3,267 nodos y 12,795 relaciones. El auditor global nuevo aún no se ha ejecutado remotamente: requiere una quinta autorización rollback-only antes de considerar el apply separado.
- Validación rollback-only de menú 2026-09-02: la tercera y única ejecución autorizada compiló las 41 sentencias de `20260902000100` y avanzó a `catalog_audit`, donde devolvió SQLSTATE `42883`; revirtió correctamente. El auditor contenía la segunda y última aparición de `pg_catalog.coalesce`, ya sustituida por `coalesce`, y la regresión cubre migración y auditor. API pasó nuevamente lint, typecheck, suite completa y build. No hubo apply ni cuarta ejecución; validar ambos fixes requiere autorización nueva.
- Diagnóstico SQL de menú 2026-09-02: la segunda y única ejecución autorizada del rollback-only devolvió `statementIndex=9`, SQLSTATE `42883`, y revirtió. La sentencia declaraba `pg_catalog.coalesce(...)`; PostgreSQL trata `COALESCE` como expresión especial, no como función calificable. La migración reemplaza esa única aparición por `coalesce(...)` y `schema-contract.test.ts` impide reintroducirla. API volvió a pasar lint, typecheck, suite completa y build. No hubo apply ni tercera ejecución; validar el fix requiere autorización nueva.
- Rollback-only de menú 2026-09-02: la única ejecución autorizada contra `zwbyiefqeujstyzysydn` alcanzó PostgreSQL y devolvió `SCHEMA_VERIFICATION_MIGRATION_FAILED` antes de la auditoría. El runner conservó el error de migración, lo que confirma que `ROLLBACK` y cierre no fallaron; no quedó esquema parcial ni se repitió la conexión. Como el runner anterior ocultaba también el punto seguro de fallo, ahora ejecuta las sentencias secuencialmente dentro de la misma transacción y puede exponer únicamente índice 1-based y SQLSTATE validado, nunca mensaje/URL/credenciales. Lint, typecheck y la suite API quedaron verdes; una nueva ejecución requiere autorización expresa.
- Menú P1, corte local 2026-09-02: los contratos compartidos v1 validan y congelan el catálogo completo con UUIDs, referencias, moneda/precios exactos, impuestos y límites de modificadores. `20260902000100` modela releases Restaurant-scoped normalizados e inmutables, un head atómico, auditoría branch-authorized/idempotente y únicamente dos capacidades `app_api`; no se aplicó remotamente. Nest expone GET/PUT con `catalog.read`/`catalog.manage` y el web agrega `/app/menu`, editor responsive, modo lectura y confirmación interna al retirar entidades. El pipeline global pasó lint 6/6, typecheck 9/9, tests 9/9 y build 6/6; tras reforzar el diagnóstico, CodeGraph quedó al día en 179 archivos, 3,252 nodos y 12,755 relaciones. El siguiente límite es una nueva autorización para el verificador rollback-only ya reforzado; apply, E2E y smoke requieren autorizaciones posteriores y no se tocaron Data API, credenciales ni `app_api`.
- Incidente operativo 2026-09-02: los rechazos temporales de Supavisor se debieron a su circuit breaker tras intentos rápidos, no a que Emmanuel copiara incorrectamente las credenciales. Después de una ventana sin reintentos, la contraseña administrativa y la clave secreta moderna quedaron validadas sin imprimir valores; `app_api` se restauró mediante el aprovisionador auditado y el postcheck quedó `runtime`, catálogo post-mesas íntegro y cero sesiones.
- La única E2E autorizada de mesas/layout emitió `runId=eb22bd0c-7b1a-4245-aae8-b353fb771c93` y falló en la etapa `dining_tables`. El cleanup del runner y la recuperación exclusiva de ese `runId` dejaron `fixtureRowsRemoved=0`, `fixtureUsersRemoved=0`; un postcheck fresco confirmó de nuevo catálogo íntegro, `app_api` runtime y cero sesiones. No se repitió la E2E ni se ejecutó smoke visual.
- Causa confirmada del fallo: `tenancyFixtureName` construía nombres de mesa de aproximadamente 73 caracteres, mientras `parseCreateDiningTableCommandV1` y PostgreSQL limitan el nombre a 40. El runner abortaba al construir el primer comando de mesa, antes de crearla. Los nombres de fixture de mesa ahora usan el `runId` completo y un código estable, miden 40 caracteres, siguen siendo únicos y son compartidos por ejecución y recuperación; una regresión cubre el contrato. P1 permanece `BLOCKED` únicamente hasta obtener autorización para una segunda ejecución remota del runner corregido y, si queda verde, completar smoke desktop/móvil.
- Segunda E2E autorizada 2026-09-02: una primera invocación fue rechazada localmente antes de `runId` porque la URL derivada de `app_api` heredaba `sslrootcert`; al limitarla en memoria a `sslmode=verify-full`, la E2E real emitió `runId=4183c6ca-2c98-42b4-be6d-8ae88487090a`. Terminó en `cleanup`; la recuperación run-scoped eliminó 19 filas y 2 usuarios. El primer postcheck vio solo la sesión retenida y el siguiente confirmó `runtime`, catálogo íntegro y cero sesiones. No hubo repetición ni smoke.
- El grafo recuperado contiene el prefijo exacto hasta mesa creada y un único audit de mesa, sin evento de actualización. El cleanup asumía que toda mesa creada también había sido actualizada, por lo que su error reemplazó el fallo funcional anterior. `FixturePlan` registra ahora `diningTableUpdated` y exige el segundo audit solo después de una actualización confirmada; la recuperación tiene regresión para el prefijo create-only. El error funcional original permanece sin identificar y no puede validarse remotamente sin autorización nueva.
- El runner de mesas publica ahora 16 checkpoints estrictamente allowlisted y sin secretos, uno antes de cada subpaso funcional. La selección de audits de cleanup se extrajo a `requiredDiningTableAuditEventIds` y tiene pruebas directas para cero progreso, create-only, create+update y estado imposible. El pipeline global quedó verde y CodeGraph confirmó el helper, sus pruebas y los cuatro consumidores del harness compartido sin referencias huérfanas.
- Tercera E2E diagnóstica autorizada 2026-09-02: precheck y postcheck confirmaron `app_api` runtime, catálogo íntegro y cero sesiones. `runId=fa6f05fd-1948-40d7-9277-9d6babc275d8` avanzó hasta `manager_layout_read` después de validar create/persistencia/replay/conflicto/rechazos y antes de `viewer_layout_read`; la recuperación exclusiva eliminó 19 filas y 2 usuarios. No se repitió la E2E ni se ejecutó smoke visual.
- El cleanup aún tenía una segunda expectativa duplicada en `verifyDiningTableFixtureOwnership` que exigía create+update cuando solo existía create. Ahora ownership y borrado consumen `requiredDiningTableAuditEventIds`. La lectura manager se subdividió en checkpoints de respuesta, status, cache, parseo y contenido; un smoke HTTP local nuevo confirma el layout poblado completo, por lo que la brecha restante está en la integración con la respuesta PostgreSQL real y no en el controlador/parser bajo el doble local.
- El último checkpoint de la tercera E2E fue `manager_layout_read` y nunca se emitió `manager_layout_response_received`; por tanto el fallo observado ocurrió durante el transporte del GET, antes de status, cache o cuerpo. El runner ahora clasifica únicamente categorías allowlisted (`connection_refused`, `connection_reset`, `socket_closed`, `timeout`, `network_unreachable`, `other`) y nunca publica mensajes, URLs ni causas del proveedor. Si sí recibe respuesta, discrimina status, cache, JSON, contrato, scope, zona, mesa y coincidencia mediante booleanos tri-state sin payloads ni IDs.
- El fallo funcional se publica con `status=failed_before_cleanup` inmediatamente al capturarse, mientras la salida final sigue indicando un eventual fallo de limpieza; así ambos hechos quedan visibles sin alterar la recuperación. Las pruebas cubren las diez salidas de lectura, códigos de transporte directos/anidados/hostiles y límites del status. El pipeline global quedó verde y CodeGraph actualizado en 169 archivos, 3,066 nodos y 12,076 relaciones; el diagnóstico alcanza solo el baseline de mesas, el harness compartido, sus cuatro entrypoints operativos y pruebas.
- Cuarta E2E diagnóstica autorizada 2026-09-02: un preflight local sin CA se detuvo antes de red y `runId`; con la CA ignorada configurada, el precheck confirmó `app_api` runtime, catálogo íntegro y cero sesiones. `runId=09fe6f56-e9c7-47be-a53c-21beec5543ca` pasó create/persistencia/replay/conflicto/rechazos y recibió la respuesta del GET manager, que fue HTTP 503. El cleanup interno retiró los fixtures; la recuperación exclusiva devolvió 0 filas/0 usuarios. Tras liberarse una sesión retenida, el postcheck final volvió a confirmar runtime, catálogo íntegro y cero sesiones. No hubo repetición ni smoke visual.
- Causa exacta y corrección local: `PostgresDiningTableAdapter` ejecutaba `select layout from app_private.list_dining_layout(...)`, aunque la función es escalar `jsonb` y no declara una columna `layout`; PostgreSQL devolvía `42703` y Nest lo traducía a 503. Una consulta estrictamente read-only contra la misma base confirmó que `select app_private.list_dining_layout(...) as layout` conserva la forma esperada. El adapter y su regresión usan ahora esa forma. El pipeline global pasó lint 6/6, typecheck 9/9, tests 9/9 y build 6/6; CodeGraph quedó actualizado en 169 archivos, 3,066 nodos y 12,076 relaciones, con impacto limitado al adapter, su prueba y el wiring de `AppModule`.
- Quinta E2E de validación autorizada 2026-09-02: `runId=19489012-145d-4b2e-94e0-6c209f2e617b` completó 151 checks y devolvió `diningTablesVerified=true`, `diningZonesVerified=true`, `fixtureRowsRemoved=true`, `fixtureUsersRemoved=2`, `status=ok`. No necesitó recovery; después de liberarse la sesión retenida del pool, el postcheck read-only confirmó catálogo post-mesas íntegro, `app_api` runtime y cero sesiones.
- El smoke protegido debe cargar el audit post-mesas, activar `verifyDiningTables` y publicar el fixture al navegador después de crear la zona y la mesa, pero antes de revocar la membresía. Una regresión contractual fija ese orden. Un primer smoke con el audit post-zonas obsoleto (`runId=7c450eed-b488-4b7e-ac9d-5658b9b40256`) falló antes de crear fixtures y su recovery exacto devolvió 0 filas/0 usuarios; un lanzamiento posterior fue rechazado en configuración antes de `runId` por una derivación local incompleta del certificado y tampoco creó fixtures.
- Smoke protegido final: `runId=db4ce441-a375-4076-be04-e8a358949266`, 151 checks, zonas/mesas verificadas, `protectedWebSmoke=true`, limpieza completa y dos usuarios eliminados. El navegador real validó editor poblado, formulario de edición sin guardar, revocación visible y layout responsive en desktop y 390×844; consola sin errores/warnings y solicitudes `/login`/`/app` en 200. El postcheck final quedó `runtime`, catálogo íntegro y cero sesiones. Emmanuel aprobó P1 como `DONE` el 2026-09-02.
- Proyecto greenfield: existe el monorepo moderno, paquetes funcionales de dominio/contratos, Auth/API productiva inicial y migraciones versionadas de tenancy/membresías/zonas/mesas. `20260830000100`, `20260830000200`, `20260831000100`, `20260901000100` y `20260901000200` están aplicadas persistentemente en `zwbyiefqeujstyzysydn`; el historial remoto contiene exactamente diez versiones y `app_api` está aprovisionado como `LOGIN` mínimo con auditoría runtime post-mesas verde y cero sesiones.
- Git local está inicializado.
- origin apunta por HTTPS a https://github.com/RenanGomez/superRestaurant.git.
- El fetch de origin/main y origin/master fue exitoso.
- SSH registró la huella de GitHub, pero la autenticación falló por falta de una clave pública autorizada.
- HTTPS de Git quedó verificado. El checkpoint `36946ca002c69931fba35e03bd559f5ecdf2fb60` (`feat: establish phase 0 application foundations`) fue publicado por fast-forward en `origin/main` el 2026-08-30, partiendo exactamente de `f0091f30324263ad253d5ea395733f0d1f56200a`. No hubo merge, rebase, force-push ni reescritura remota.
- El checkpoint `0274e660c0464d34bab2fffe7c576ad459b6d552` (`fix: harden app api provisioning lifecycle`) fue publicado por fast-forward en `origin/main` el 2026-08-31. Conserva el catálogo endurecido y documenta la migración/provisión remotas; no hubo merge, rebase ni force-push.
- El checkpoint remoto vigente es `610613eebfa0536e3ef94f349d8b2de0b84435ff` (`feat(api): add RBAC dining zone creation`), publicado en `origin/main`. La continuación de auditorías/apply e integración FE-0.1 parte de ese árbol exacto y permanece local, sin commit ni push.
- CodeGraph está indexado y al día; la reconsulta posterior a la E2E de zonas reporta 152 archivos, 2,790 nodos y 10,802 relaciones. `runTenancyVerification` alcanza solo sus dos runners y `recoverTenancyFixtures` solo su ejecutor, runner y prueba. No quedan escritores frontend concurrentes. La verificación local del API y el pipeline global están documentados en HANDOFF; la máquina local ejecuta Node 22.20.0 y emite el aviso esperado porque el repositorio fija Node 24.19.0 para CI/producción.
- Existe un `.gitignore` raíz, pendiente de revisión humana, que excluye dependencias, secretos, builds, cobertura, caches, `.codegraph`, artefactos móviles y credenciales de firma; conserva versionables las plantillas `.env*.example`.
- El monorepo raíz usa pnpm 11.19.0 y Turborepo 2.10.12 con workspaces `apps/*` y `packages/*`; Node 24.19.0 y las versiones de herramientas quedaron fijadas, y `pnpm-lock.yaml` es reproducible.
- `frontend.md` define la secuencia FE-0–FE-7 y un contrato operativo de trabajo simultáneo. El 2026-08-31 Claude recibió asignación explícita de FE-0.1, limitada a `apps/web/**`, después de confirmar migración de membresías, `app_api`, E2E remota, endpoints, contrato enlazado y checkpoint `610613e`. FE-1 y posteriores siguen sin autorización. Codex conserva backend, dominio, esquema, contratos, documentos operativos y lockfile/manifests raíz.
- `apps/web` (FE-0/FE-0.1, REVIEW) usa Next.js 16.3.3 (App Router, Turbopack), React 19.2.8, Tailwind CSS 4.3.3 (`@theme` en `globals.css`, sin `tailwind.config.js`) y `@supabase/ssr` 0.12.5. Consume `@super-restaurant/shared-types` mediante `workspace:*`, obtiene scopes exclusivamente del directorio Nest, selecciona/revalida Restaurant/Branch y bloquea revocaciones. El smoke remoto real pasó con 119 checks y cleanup total. No depende de `packages/domain`. El archivo de convención es `src/proxy.ts` (Next.js 16 renombró `middleware` a `proxy.ts`), no `middleware.ts`.
- La dirección visual de FE-0 ("Operational Crisp": Sora + IBM Plex Sans, acentos oklch azul-teal) se decidió con el usuario mediante un mockup de Claude Design antes de escribir código, comparándola contra dos alternativas ("Warm Hospitality" y "Dark Kitchen-Ready") que quedaron descartadas.
- `apps/web` no tiene cliente Supabase de navegador en ningún archivo; `src/lib/supabase-server.ts` es el único punto que construye `@supabase/ssr`, con cookies host-only (`path: "/"`, sin `domain`), `httpOnly`, `sameSite: "lax"` y `secure` derivado de `WEB_ORIGIN`. Claude trasladó la revalidación Nest y el `signOut()` de rechazo a `proxy.ts`, que sí puede persistir cookies, copia las mutaciones al redirect fijo y deja el layout como lectura defensiva. `verifyRemoteSession` tiene parser UUID exacto y siete pruebas de respuesta válida, rechazo, red, JSON y forma hostil.
- El slice FE-0 pasó lint, typecheck, 23/23 pruebas y build Next. Las pruebas incluyen 11 casos de entorno, siete de sesión/parser y cinco funcionales directos del proxy real mediante un cargador ESM y doble Supabase exclusivos de test, sin dependencias nuevas ni cambios a imports productivos. Demuestran limpieza de cookies, redirect fijo, ausencia de ciclo, una sola llamada Nest y `private, no-store`. Solo falta el smoke humano contra Auth/Nest reales; por ello permanece IN_PROGRESS.
- TypeScript quedó fijado en 6.0.3, la última 6.0.x estable compatible con `typescript-eslint` 8.67.0; TypeScript 7.0.2 se descartó porque el linter declara soporte solo para versiones `<6.1.0`.
- `packages/config` publica presets compartidos de TypeScript y ESLint; `packages/shared-types` define identificadores opacos Restaurant/Branch y sus alcances sin filtrar modelos de persistencia.
- `packages/domain` implementa Money con minor units enteros seguros, BigInt interno y redondeo `half-away-from-zero` por defecto, más máquinas de estados puras de Order/OrderItem.
- `apps/api/src/persistence/order-persistence-codec.ts` define el contrato v1 JSON-safe de Order/OrderAuditEvent sin introducir ORM ni esquema definitivo. Serializa dinero como minor units+ISO y ratios bigint como enteros decimales canónicos, rehidrata objetos profundamente congelados y delega las invariantes a `packages/domain`. Rechaza versiones/campos extra, accessors, proxies, ciclos, prototipos hostiles, enteros inseguros y mutaciones cuyo evento diverge del agregado. Cuatro regresiones cubren round-trip, cancelación enlazada y entradas hostiles; con el directorio de membresías la suite API total queda en 94 pruebas.
- `packages/shared-types` publica `BranchMembershipListV1`, nueve roles canónicos congelados y un parser exacto/fail-closed. El contrato limita 500 scopes, exige UUID, nombres limpios, roles conocidos/únicos/ordenados, orden determinista Restaurant+Branch y devuelve una copia profundamente congelada; rechaza extras, símbolos, accessors, campos ocultos, proxies, duplicados y prototipos hostiles.
- RBAC v1 publica 18 códigos de permiso canónicos en `packages/shared-types` y mantiene la matriz autoritativa en Nest. No existe herencia implícita: cada uno de los nueve roles tiene una lista exacta, congelada y revisable. Múltiples roles activos combinan permisos solo dentro de la misma membresía/scope; entradas desconocidas, duplicadas o hostiles se deniegan. `docs/architecture/RBAC_MATRIX_V1.md` conserva la propuesta completa y requiere aprobación humana antes de enlazar el primer endpoint de negocio restringido.
- `MembershipAuthorizationService` ya recibe un permiso tipado, no una lista arbitraria de roles. Primero revalida actor, Restaurant/Branch y membresía mediante PostgreSQL privado y después aplica RBAC v1. La selección de Branch usa `branch.select`, concedido explícitamente a los nueve roles; el primer `403` por permiso de acción queda reservado al siguiente endpoint real y deberá demostrar false pairs, revocación y cero efectos.
- `GET /api/v1/access/memberships` deriva al actor exclusivamente del guard global y consulta una sola capacidad PostgreSQL parametrizada. Devuelve `200` incluso con lista vacía, `Cache-Control: private, no-store`, y un 503 genérico sin lista parcial ante cualquier fallo o fila malformada. El endpoint no selecciona scope, no expone IDs internos/timestamps y no sustituye la revalidación de `POST /api/v1/access/branch`.
- `20260830000200_list_active_branch_memberships.sql` crea una función `SECURITY DEFINER` con owner `postgres`, `search_path=''`, límite 501 y filtrado de membresía/roles/Restaurant/Branch activos. `app_api` recibe únicamente `EXECUTE`; Data API y roles cliente permanecen sin acceso. Tras la aplicación, las auditorías pinneadas se cerraron al estado expandido obligatorio: exigen las cinco funciones exactas y rechazan grants de `anon`, `authenticated` y `service_role` sobre ambas capacidades privadas.
- `verify:membership-directory-schema:rollback` reutiliza el parser transaccional y la configuración TLS fail-closed para aplicar solo `20260830000200`, exigir el resumen expandido de cinco funciones y emitir siempre `ROLLBACK`. El verificador anterior conserva por defecto el contrato de cuatro funciones. Tres regresiones nuevas cubren el estado expandido, expectativas inválidas antes de red y respuestas con conteo/campos hostiles; la suite API asciende a 97 casos.
- El despliegue persistente usó un staging temporal fuera del árbol con copias y hashes exactos de las cinco migraciones del spike y las dos productivas. El historial previo mostró seis versiones; el dry-run anunció exclusivamente `20260830000200`, la aplicación oficial terminó con código 0 y el postcheck confirmó siete versiones exactas. No hubo SQL ad hoc, reparación de historial, seed, roles, Vault, `config push` ni `db push` desde la raíz; el staging fue eliminado.
- La exposición productiva de Data API no usa `supabase config push`: un runner server-only llama únicamente `GET/PATCH /v1/projects/{ref}/postgrest`, envía solo `db_schema=public,graphql_public,app` y exige pre/postcheck exactos sin drift en los campos PostgREST no objetivo. Acepta solo baseline, el histórico `adr010_b` o el estado final; rechaza schemas privados/desconocidos, respuestas hostiles y fallos ambiguos no reconciliados. Requiere opt-in, confirmación exacta y `SUPABASE_ACCESS_TOKEN` con permisos `data_api_config_read/write`, nunca imprime bodies, token o `jwt_secret`.
- El checkpoint local del runner dirigido y del enlace `shared-types` pasó la compuerta global (lint, typecheck, tests y build) y una revisión independiente sin hallazgos P0/P1.
- Emmanuel autorizó expresamente exponer persistentemente solo `app` y ejecutar la E2E controlada. El runner dirigido confirmó el estado exacto `public,graphql_public,app`, sin `app_private`, `app_rls`, `config push` ni cambios de Auth. La matriz remota terminó con 97 checks, cleanup de todas las filas temporales y eliminación de dos usuarios Auth. La autorización quedó consumida; no repetir escrituras remotas sin una nueva aprobación.
- Continuación desde otro equipo: partir únicamente del `origin/main` publicado, fast-forward y checkout limpio; consultar CodeGraph antes de escribir. No repetir la migración `20260830000200`, el provisioning de `app_api`, la exposición Data API ni la E2E. FE-0.1 ya cumple prerrequisitos técnicos una vez publicado este checkpoint, pero Claude necesita una asignación explícita de Emmanuel y queda restringido a `apps/web/**`; Codex puede avanzar en paralelo solo fuera de esa frontera, empezando por el siguiente slice server-side autorizado por rol. FE-0 conserva pendiente el smoke humano real.
- `packages/domain` calcula líneas y órdenes desde snapshots inmutables: precio/modificadores × cantidad, descuento de línea, impuesto incluido/excluido por línea, descuento de orden y propina. No usa float ni muta inputs del caller.
- `packages/domain` modela Payment inmutable con estados explícitos, intento lógico idempotente y Refund compensatorio referenciado. Un refund parcial conserva `captured`; solo agotar el saldo cambia a `refunded`.
- `packages/domain` modela una sesión CashRegister pura: apertura/cierre auditados, saldo esperado derivado, diferencia `contado - esperado`, movimientos append-only y compensaciones acumuladas que nunca exceden el original. `localSequence` exige un cursor autoritativo por dispositivo suministrado por la futura capa de aplicación/persistencia.
- Los snapshots monetarios conservan catálogo, producto/SKU, estación, unidad, precios, modificadores, quantity, regla fiscal/tasa/modo y versiones de descuentos. Todos los resultados contienen clones profundamente congelados.
- Las reglas puras de modificadores validan un grupo versionado y activo ligado a un producto: los límites mínimo/máximo cuentan la suma de cantidades por unidad de producto, cada opción debe pertenecer al allowlist activo, los duplicados se rechazan y un cap por opción es explícito cuando existe.
- La selección produce orden canónico por ID y snapshots profundamente congelados con identidad/nombre/versión del grupo, opción, precio y cantidad. El impuesto se hereda del `OrderItem` en esta etapa; no se inventó una regla fiscal por modificador.
- El descuento de orden usa la estrategia versionada `largest-remainder-post-tax-line-total-v1`: prorratea sobre totales post-impuesto, distribuye unidades residuales por mayor resto y desempata por `orderItemId` con comparación binaria estable.
- ADR-007 queda PROPUESTA: fija invariantes técnicas neutrales, pero país, moneda inicial, zona predeterminada, tasas/régimen, tratamiento fiscal de propinas y CFDI permanecen bloqueados hasta decisión humana/asesoría fiscal.
- ADR-004 queda PROPUESTA y documenta Fases 0–2 online-first, Fase 3 offline por dispositivo con nube/API autoritativa y Fase 3B LAN fuera de v1 condicionada a ADR-009.
- `docs/architecture/OFFLINE_SYNC_CONTRACT.md` fija el envelope, secuencia global monotónica por dispositivo, versiones, push idempotente, pull con cursor opaco separado, outbox durable, tombstones, revocación y aislamiento. Es contrato de diseño: no existe aún `packages/sync-engine` ni almacenamiento local productivo.
- `docs/architecture/OFFLINE_CAPABILITY_AND_CONFLICT_MATRIX.md` bloquea por defecto operaciones offline de alto riesgo, prohíbe LWW para dinero/estados críticos y envía ambigüedades financieras a revisión humana con correcciones compensatorias.
- Una respuesta de push `accepted`/`duplicate` confirma únicamente su evento de outbox; nunca avanza el cursor de pull. Solo aplicar atómicamente un lote pull puede persistir el siguiente cursor.
- `localSequence` es global por `deviceId`, no se reinicia al cambiar de Restaurant/Branch y no establece causalidad entre dispositivos. Scope, `baseVersion` y versión canónica del servidor siguen siendo controles independientes.
- Una cancelación de ítem después de enviarlo devuelve evidencia auditable inmutable con actor, sucursal, dispositivo, timestamp, motivo y autorización; la capa de aplicación deberá persistir ese registro.
- Una orden `partially_paid` puede aceptar nuevas líneas en este nivel de dominio; el plan solo prohíbe explícitamente añadirlas en `paid`/`closed`, y las reglas de ajuste financiero posteriores podrán restringir el caso de uso.
- CI neutral usa GitHub Actions con permisos `contents: read`, instalación frozen y etapas lint/typecheck/test/build; las acciones fijan majors oficiales vigentes y las versiones Node/pnpm coinciden con `package.json`.
- ADR-010 tiene un harness común en `spikes/adr-010` con fixtures deterministas 2×2 y gates ejecutables de aislamiento, atomicidad, idempotencia, auth/scope/revocación, recuperación KDS, migraciones, backup/restore, secretos y reproducibilidad; la frontera única exige además inspección humana.
- El adaptador en memoria solo valida el harness y no puntúa A/B/C. La evidencia real de B, los diez gates y el scoring final ya quedaron demostrados; A y C conservan sus limitaciones documentadas.
- La opción A incluye un adapter real de PostgreSQL, migración thin-slice, frontera Nest y runner opt-in. Su función `create_order` concentra scope, revocación, atomicidad e idempotencia; el restore lógico usa tablas/columnas allowlisted, exige destino vacío y no oculta conflictos. Sin PostgreSQL ejecutado y con `issueSession` como doble, A permanece NO-GO y sin score.
- La opción B implementa `Adr010Adapter`: usa sesiones desechables de Supabase Auth, valida el access token mediante `auth.getUser(token)`, deriva `actorId` del usuario verificado y mantiene NestJS/PostgreSQL server-only como única frontera de escritura crítica. Order, Payment, Refund y CashMovement pasan por servicios Nest y funciones SQL privadas; el refund exige un segundo token de supervisor y persiste su autorización. Incluye guards de proyecto/TLS, idempotencia ligada a actor/scope/payload, cleanup reintentable y backup/restore lógico conservador. B obtuvo GO 75/100 y Emmanuel la seleccionó el 2026-08-29.
- B separa `adr010_b` (único schema expuesto, tablas de lectura con RLS) de `adr010_b_private` (todas las funciones `SECURITY DEFINER`, sin exposición ni grants Data API, `search_path=''`). La secret key queda para Auth Admin; `ADR010_DATABASE_URL` queda solo en Nest/CI para funciones privadas.
- El bootstrap B crea dos usuarios Auth desechables mediante Admin API server-only, usa un `bootstrap_run_id` único, adjunta memberships 2×2 por PostgreSQL privado y permite limpieza concurrente/reintentable sin imprimir credenciales. Es preparación remota, no evidencia del gate Auth.
- El probe B opt-in usa clientes independientes con publishable key y sesiones reales para comprobar lecturas propias, ausencia cross-scope y revocación inmediata. Pasó contra `ndblkcmdgpxsxylacutx`; el runner ahora prepara fixtures estructurales mediante el adapter y siempre limpia/cierra en `finally`.
- El proyecto Supabase `cxcnnhafchqslvgvkeye` quedó enlazado y recibió tres migraciones versionadas del spike: thin slice B, hardening read-only C y hardening RLS/ACL de `bootstrap_users`. `adr010_b` es el único schema experimental expuesto; `adr010_b_private` permanece privado. La auditoría SQL remota, estrictamente read-only y limitada a ambos schemas experimentales, pasó 24/24 checks.
- El primer `config push` materializó defaults no deseados de Auth además de exponer `adr010_b`. Los valores previos de URL/redirecciones, TOTP, confirmación de email, frecuencia y longitud OTP se restauraron inmediatamente y una verificación posterior confirmó API, DB, Auth y Storage al día; la única diferencia intencional es la exposición de `adr010_b`.
- El advisor de seguridad detectó errores preexistentes fuera del spike: `public.clients`, `public.client_addresses` y `public.client_categories` tienen RLS deshabilitado, además de una función pública con `search_path` mutable. Esto demuestra que el proyecto no es nuevo/aislado; sus errores no se atribuyen a `adr010_b`, pero impiden usarlo para reset, Auth desechable, gates comunes o restore.
- La opción C implementa únicamente lecturas acotadas de órdenes y recuperación KDS con URL + publishable key. No expone mutaciones, pagos, caja ni `Adr010Adapter`; es NO-GO para backend/core financiero y solo podría considerarse para lecturas o CRUD no financiero tras probar RLS real.
- Los preflights locales de B/C validan configuración, límites del API y SQL estático; no demuestran aislamiento remoto, concurrencia, atomicidad, revocación, Realtime, migraciones ni backup/restore.
- Para Supabase nuevo se usarán URL + publishable key en comprobaciones públicas y secret key solo en servidor/CI. `anon`/`service_role` se consideran legacy y no se adoptan para configuración nueva.
- El proyecto antiguo `cxcnnhafchqslvgvkeye` no es apto para pruebas destructivas. Gate 7 ya se demostró en el segundo proyecto vacío `zwbyiefqeujstyzysydn`; no se necesita otro proyecto para cerrar ADR-010. La limitación local de Docker/PostgreSQL/Redis solo afectó la opción A, que no fue seleccionada.
- El proyecto aislado nuevo `ndblkcmdgpxsxylacutx` (`superrestaurant-adr010-spike-20260827`) fue verificado antes de migrar: 0 relaciones de usuario, 0 usuarios Auth, 0 objetos Storage y 0 migraciones. Recibió las tres migraciones ADR, expone `adr010_b` sin exponer `adr010_b_private`, pasó auditoría 24/24 y el security advisor no reportó problemas.
- Las claves publishable/secret del proyecto nuevo se obtienen solo en memoria mediante la CLI oficial. Los gates PostgreSQL esperan una contraseña en `.env.adr010.local`, archivo ignorado por Git; no se almacena ni se comparte por chat.
- El runner B corrigió cuatro defectos descubiertos por evidencia remota: arrays JS enviados como arrays PostgreSQL a parámetros `jsonb`, claves de bootstrap `camelCase` incompatibles con `jsonb_to_recordset`, probe RLS sin fixtures estructurales y una segunda limpieza del adapter con la misma serialización inválida. Los contratos quedaron cubiertos por pruebas y los diagnósticos PostgreSQL se redactan para no exponer secretos.
- `test:option-b:rls` y `test:option-b:gates` pasaron contra el proyecto aislado. Las 15 evidencias remotas verifican principal Auth, rotación de access/refresh token, escritura con la sesión renovada, rechazo del refresh después de revocación global, aislamiento, atomicidad, idempotencia/binding, Payment/Refund de efectivo, compensación, ledger/auditoría, recuperación KDS, revocación de scope, preservación de migración, backup/restore lógico, guard de destino vacío y superficie de secretos. Gate 7 quedó demostrado en el segundo proyecto fresco y Emmanuel aceptó Gate 4 el 2026-08-29; el reporte versionado ahora declara `eligibleForAdr010Go: true`. El restore físico pertenece a recuperación de desastre y RPO/RTO antes de producción; no sustituye ni bloquea el gate 8 lógico del spike, que ya pasó.
- La CA oficial `Supabase Root 2021 CA` descargada por el usuario fue validada como certificado CA vigente hasta 2031. La URL exacta del adapter conectó al pooler con `sslmode=verify-full`, `sslrootcert`, cifrado activo, certificado autorizado, hostname `*.pooler.supabase.com` y sin error de autorización. El runner completo B pasó nuevamente con esta configuración; el certificado permanece fuera del repositorio y no es secreto.
- Las conexiones administrativas aceptan únicamente endpoints directos o pooler de sesión en puerto 5432, `sslmode=verify-full` y un `sslrootcert` opcional, único, no vacío y acotado. El puerto 6543 de transaction pooling se rechaza porque el ciclo operativo depende de advisory locks ligados a sesión. Los parámetros TLS se eliminan de la URL antes de entregarla a `pg`, que recibe la CA ya cargada y validada por separado; cualquier otro parámetro o duplicado se rechaza.
- El verificador read-only de ciclo de vida de `app_api` comparte el advisory lock y clasifica únicamente `safe_disabled`, `temporary`, `expired`, `runtime` o `partial`, sin imprimir credenciales, fechas, PIDs ni errores nativos. Las sesiones activas producen `attention` conservando el booleano y sin fingir una auditoría quiescente; el fingerprint exige `CONNECTION LIMIT -1`. El 2026-09-01 confirmó el estado final `runtime`, auditoría endurecida post-zonas correcta y cero sesiones en `zwbyiefqeujstyzysydn` mediante TLS `verify-full`, después de rotar la credencial.
- Emmanuel autorizó el 2026-08-30 habilitar persistentemente `LOGIN` de `app_api`. La contraseña privada pasó el contrato de fuerza y separación sin revelarse. El 2026-08-31 el aprovisionamiento quedó completado con SCRAM, vigencia infinita y mínimos privilegios mediante un corte quiescente compatible con Supavisor: `NOLOGIN PASSWORD NULL`, drenaje acotado y publicación atómica de credencial+auditoría+`LOGIN`.
- Rotación `app_api` del 2026-09-01: por autorización expresa de Emmanuel se añadió un perfil operativo separado, fijado por hashes SHA-256, para recovery/provisioning contra el catálogo posterior a `20260831000100`. El recovery remoto confirmó `NOLOGIN`, contraseña nula y sesiones drenadas antes de generar la credencial nueva; el provisioner probó una conexión privada real, permisos permitidos/denegados, corte quiescente y auditoría runtime antes de promover a vigencia infinita. La credencial solo se escribió en `.env.adr010.local`, ignorado; no se imprimió ni versionó.
- El smoke protegido FE-0.1 reutiliza `runTenancyVerification` en vez de crear otro sistema de fixtures. Hooks opcionales pausan el grafo marcado antes/después de la revocación, Nest escucha en un puerto local fijo solo para esa ejecución y un coordinador intercambia credenciales/acks mediante archivos `.tmp` exactos, ignorados y eliminados en `finally`. El lease posterior a revocación omite credenciales; timeout, mismatch o fallo del navegador conducen al mismo cleanup Auth/FK-safe. Preparar el runner no autoriza su ejecución remota.
- El smoke protegido FE-0.1 terminó remoto con 119 checks y cleanup total en `zwbyiefqeujstyzysydn`. El runner debe activar explícitamente `verifyDiningZones` cuando usa el auditor post-zonas. Durante una sesión viva no se ejecuta logout antes del último ack, porque un sign-out global invalida la sesión paralela del harness; el producto quedó con logout `scope=local`. Si el proxy rechaza un Server Action protegido, limpia la sesión local y deja continuar ese POST para que el action produzca su redirect propio, evitando que Next reciba un redirect HTML inesperado.
- Incidente local cerrado: una URL administrativa quedó expuesta solo en salida técnica el 2026-08-30 por un lector ad hoc; no fue versionada ni copiada a documentación. Emmanuel rotó la credencial y la conexión nueva autenticó el 2026-08-31. Desde entonces todas las operaciones remotas usan salidas allowlisted y no reveladoras.
- `GET /api/v1/session` y `POST /api/v1/access/branch` responden `Cache-Control: private, no-store`; el smoke Nest verifica ambos encabezados para impedir caché de identidad/scope autorizado.
- El runner `test:option-b:fresh-push` queda preparado para un segundo proyecto vacío: exige opt-ins independientes, identidad URL/DB/project-ref/enlace CLI exacta, cero migraciones/usuarios/storage/relaciones de usuario, dry-run primero, revalidación anti-TOCTOU antes de aplicar y confirmación de las cinco versiones. Nunca crea, enlaza ni elimina proyectos.
- El proyecto aislado tiene cinco migraciones ADR. Las dos últimas añaden Payment/Refund/CashMovement/auditoría financiera, cursor privado y unicidad global `(device_id, local_sequence)`; la segunda corrige de forma versionada los objetivos `ON CONFLICT` sin editar la migración aplicada. La auditoría posterior pasó 27/27 y `db lint` no encontró errores en `adr010_b`/`adr010_b_private`.
- La auditoría adversarial neutral del 2026-08-28 cerró tres clases de continuidad/integridad: Order y snapshots ya rechazan getters heredados/prototipos hostiles y no cierran con ítems incompletos; modificadores incluyen `restaurantId` para impedir mezclar catálogos homónimos entre tenants; Payment/Refund conservan `eventId` explícito y evidencia de Branch, mientras CashRegister rechaza cierres rehidratados con diferencia sin motivo. Las APIs públicas de estados ya no filtran `TypeError` ante estados runtime malformados.
- La segunda auditoría adversarial neutral cerró los límites runtime restantes: calculadoras de totales y modificadores normalizan por descriptores propios y rechazan DTOs/accessors/proxies/prototipos hostiles sin `TypeError`; Refund no puede reutilizar una idempotency key de transición; cierre de caja no puede reutilizar event/key de un movimiento. `shared-types` exporta `SCOPE_SCHEMA_VERSION = 1` y parsers exactos/fail-closed de RestaurantScope/BranchScope que devuelven copias congeladas y no amplían un scope inválido.
- Pipeline global posterior: lint, typecheck, 77 pruebas de dominio, 57 del spike, contrato runtime + prueba compile-time de shared-types y build exitosos. CodeGraph indexó 56 archivos, 1,281 nodos y 3,481 relaciones; no existen consumidores productivos de estos contratos todavía.
- Una instalación limpia reveló que `typecheck` y `test` podían consumir `@super-restaurant/shared-types` antes de que existiera su `dist`; el build posterior ocultaba el defecto. Turbo ahora exige `^build` además de `^typecheck`/`^test`. La reproducción sin salidas, cachés ni `tsbuildinfo` previos pasó antes de ejecutar el pipeline global.
- El fresh-push de Gate 7 usa una configuración de proyecto+PostgreSQL separada y ya no requiere ni carga `ADR010_SUPABASE_SECRET_KEY`. Su preflight rechaza relaciones, rutinas, dominios, enums y rangos inesperados en los schemas de aplicación, y el postcheck exige que las cinco versiones ADR sean todo el historial remoto de migraciones.
- Los certificados locales `.crt`, `.cer` y `.pem` quedaron excluidos de Git junto con las credenciales de firma. La CA `Supabase Root 2021 CA` fue validada localmente y usada con `sslmode=verify-full`; no se versionó ni se imprimió su contenido.
- CodeGraph se inicializó localmente porque el índice está ignorado por Git; la reconsulta posterior al código reportó 56 archivos, 1,290 nodos y 5,203 relaciones. El fresh-push afecta solo su runner y preflight, sin consumidores productivos.
- Gate 7 quedó demostrado en `zwbyiefqeujstyzysydn`: la sesión CLI correcta enumeró y enlazó el ref exacto; el runner verificó el destino vacío, dry-ran y aplicó las cinco migraciones con revalidación previa, y confirmó que son todo el historial remoto. `adr010_b` quedó expuesto y `adr010_b_private` privado; auditoría 27/27 y `db lint` pasaron.
- El runner de fresh-push ya no depende de un binario global: resuelve `npx`/`npx.cmd` y fija `supabase@2.109.1`. Los preflights validan ambas plataformas y una ejecución Windows inocua devolvió exactamente `2.109.1`; la descarga por `npx` requiere red durante el gate remoto.
- La integración posterior pasó lint, typecheck, 70 pruebas de dominio y 57 del spike, y build global. Los archivos operativos siguen siendo la fuente durable entre sesiones. ADR-006 quedó aceptada: `ceaffcb` creó la raíz greenfield y `f9f4428` conectó la historia previa mediante merge `ours`; `main` y `codex/modernizacion-fase0` publican el mismo árbol moderno por fast-forward, sin force-push ni cambio de `origin/master`.
- Tras las pruebas quedaron 0 usuarios Auth, bootstrap rows, memberships, órdenes, líneas, snapshots, eventos KDS, pagos, refunds, movimientos, auditorías financieras y cursores de dispositivo; permanecen únicamente los 2 restaurantes y 4 sucursales estructurales deterministas del spike.
- `packages/domain` ya contiene un agregado funcional e inmutable `Order`: declara canal `table/counter/takeout/delivery`, mesa obligatoria solo para `table`, scope `restaurantId`/`branchId` también en cada línea, snapshots históricos, estados y totales mediante el calculador compartido. Cancelar la orden exige evidencia completa, bloquea líneas activas de cocina hasta cancelarlas individualmente, cancela pendientes atómicamente y deja total pagable cero; órdenes cerradas/canceladas no aceptan mutaciones de ítems. Toda operación rechaza accessors y revalida agregados rehidratados.
- El menú neutral de `packages/domain` modela una versión coherente por Restaurant y moneda, con categorías/productos ordenables, estados activos, SKU opcional único, estación/unidad/precio/impuesto congelados y allowlists de grupos de modificadores. La selección reusa exclusivamente las reglas de `modifiers.ts`, exige grupos obligatorios y produce `OrderItemPriceSnapshot` compatible con los calculadores y el agregado Order. Los valores se desprenden del catálogo vivo, se congelan profundamente y las fronteras rechazan prototipos, accessors, proxies, scopes/versiones/referencias incompatibles, precios negativos y monedas cruzadas. Branch, horarios, precios por canal, combos, recetas, persistencia y UI no forman parte de esta unidad neutral.
- Las fronteras puras financieras revalidan historiales rehidratados antes de replay, transición, refund, balance o cierre. Payment comprueba secuencia/estado/refunds/evidencia; CashRegister comprueba semántica de movimientos, compensaciones acumuladas y coherencia del cierre. Transición Payment y cierre CashRegister conservan `eventId`/`idempotencyKey`, reproducen el mismo intento y rechazan reutilización divergente. La razón de una variación se exige y devuelve como evidencia al cerrar, pero no forma parte del agregado rehidratado; su persistencia corresponde al audit log inmutable de la aplicación.
- Money rechaza modos de redondeo runtime desconocidos y los snapshots fiscales exigen ratios bigint no negativos con denominador positivo; entradas malformadas producen errores de dominio, no `TypeError`.
- Las propiedades financieras usan PRNG con semilla fija y `node:test`, sin nueva dependencia: 96 casos Money contra oráculo bigint, 61 escenarios de descuento con cuatro permutaciones y 48 secuencias de compensación/replay. Verifican exactitud, overflow, redondeo, conservación de minor units, estabilidad por `orderItemId`, saldos e idempotencia.
- El agregado puro no puede demostrar por sí solo que `tableId` pertenece a `branchId` ni que pagos persistidos autorizan `partially_paid/paid`; esas verificaciones son obligatorias en la frontera de aplicación/persistencia ahora desbloqueada por ADR-010.
- `pg@8.23.0` y `@types/pg@8.23.1` quedaron fijados para el adapter A; sus versiones y licencia MIT fueron verificadas contra npm por el subagente responsable.
- La separación B sigue la guía oficial actual de Supabase: grants y RLS controlan conjuntamente schemas expuestos, las funciones `SECURITY DEFINER` no deben exponerse y una secret key debe permanecer server-only.
- La inspección técnica y la auditoría independiente de Gate 4 confirmaron que Order, cash Payment y cash Refund derivan actor mediante `auth.getUser` antes de mutar y llaman únicamente tres funciones PostgreSQL privadas. El único uso Data API de tablas en el adapter son lecturas `.select`; no hay `insert`, `update`, `upsert`, `delete` o `rpc` de negocio ni aplicaciones productivas. En refunds, un token separado de supervisor se verifica y la función SQL vuelve a exigir membresía activa `owner`/`manager` en el Restaurant/Branch exacto. Emmanuel aceptó esta frontera el 2026-08-29, incluyendo que `restore/reset` son excepciones administrativas privadas del runner y nunca rutas de aplicación. Los hooks `induceFailureAfter*` y puertos exportados del spike tampoco pueden formar parte de DTOs/bundles productivos; Gate 4 debe repetirse cuando existan aplicaciones reales.
- `docs/adr/ADR-010.md` refleja los diez gates demostrados y la selección humana de B. `B_SCORING.md` asigna un puntaje conservador de 75/100: seguridad 20/25, dominio/frontera 20/25, compatibilidad offline futura 12/15, operación 9/15, mantenibilidad 8/10 y costo/portabilidad 6/10. Cumple el umbral sin margen; recuperación física, RPO/RTO, observabilidad y reinspección Gate 4 siguen pendientes antes de producción.
- El pipeline global posterior al menú pasó instalación frozen, lint 4/4, typecheck 6/6, 87 pruebas de dominio, 57 del spike, contratos runtime/compile-time de shared-types y build 4/4. CodeGraph quedó actualizado con 58 archivos, 1,383 nodos y 5,562 relaciones; los únicos consumidores del nuevo contrato son su barrel y sus pruebas, y la integración con Order usa el contrato `OrderItemPriceSnapshot` existente sin modificarlo.
- La auditoría neutral de Order es un contrato específico y versionado, no una abstracción compartida prematura con Payment/Cash. Cada mutación del agregado exige `OrderAuditContext` y devuelve `OrderMutation { order, auditEvent }`; Restaurant/Branch/entidad/operación/transición se derivan de la Order validada. Toda cancelación retiene evidencia ligada por `eventId` e `idempotencyKey`, incluido el origen de la transición; una línea pending requiere motivo y una línea enviada/preparando/lista exige además supervisor. La futura capa autoritativa debe verificar identidad/RBAC, añadir `receivedAt`, comparar la evidencia rehidratada con el audit log, imponer unicidad y persistir agregado+evento en una transacción. El timestamp del dispositivo y `{ approved: true }` no prueban por sí solos tiempo autoritativo ni rol.

## Decisión durable: primera escritura de zonas

- `POST /api/v1/dining/zones` es la primera escritura de negocio que consume RBAC v1. El cliente aporta UUID para zona, evento, dispositivo e idempotencia y un timestamp UTC canónico; Nest deriva el actor del token, exige `tables.manage` en el Restaurant/Branch exacto y PostgreSQL vuelve a validar la membresía activa.
- La función privada crea zona y evento de auditoría en una sola transacción. El replay solo es válido cuando actor, scope, evento, zona, dispositivo, timestamp y nombre coinciden; cualquier reutilización divergente, nombre duplicado o identidad duplicada es conflicto sin efecto parcial.
- `app.dining_zones` y `app.dining_zone_audit_events` quedan sin grants de tabla para clientes ni `app_api`, con RLS + FORCE RLS. `app_api` recibe únicamente `EXECUTE` sobre `app_private.create_dining_zone`.
- La migración `20260831000100_create_dining_zones.sql` quedó aplicada persistentemente con autorización expresa de Emmanuel. Un staging temporal reunió copias byte a byte de las cinco migraciones históricas y tres productivas; el historial previo mostró siete versiones y el dry-run anunció solo `20260831000100`, sin seeds ni roles. El postcheck confirmó ocho versiones exactas, catálogo post-zonas runtime válido, cero sesiones de `app_api` y `db lint` limpio en `app`, `app_private` y `app_rls`. La E2E remota posterior ejecutó 119 checks: manager 201/replay, conflicto 409, viewer/false pair/revocación 403 con token vivo y cero efectos rechazados; cleanup y postcheck idempotente confirmaron cero fixtures/usuarios. El slice permanece IN_PROGRESS solo hasta completar mesas/layout/UI.

## Decisión durable: mesas y geometría de layout v1

- El layout usa coordenadas enteras en una cuadrícula estable de 24 columnas por 100 filas; cada mesa ocupa 2–8 unidades por eje y debe quedar dentro del canvas. El contrato no guarda píxeles ni dimensiones dependientes del dispositivo.
- `GET /api/v1/dining/layout` exige `tables.read`; `POST /api/v1/dining/tables` y `PATCH /api/v1/dining/tables/layout` exigen `tables.manage`. Nest y las funciones privadas revalidan actor y scope exacto; clientes y `app_api` no reciben grants de tabla.
- El movimiento usa `expectedVersion` y aumenta una versión por mesa. Alta y cambio de geometría conservan evento, actor, dispositivo, timestamp, idempotency key y snapshot de resultado; replay divergente o versión obsoleta es conflicto sin efecto parcial.
- `20260901000100_create_dining_tables_layout.sql` permanece solo local. Su verificador transaccional espera el catálogo resultante de 5 políticas, 9 tablas RLS+FORCE RLS y 9 funciones privilegiadas, siempre con rollback. Ejecutarlo contra Supabase, aplicar la migración o crear fixtures requiere autorización nueva.
- La autorización rollback-only del 2026-09-01 terminó satisfactoriamente después de propagarse la nueva contraseña administrativa. Con TLS `verify-full` y la `Supabase Root 2021 CA`, el runner aplicó migración + auditoría dentro de una transacción y devolvió 5 políticas, 9 tablas RLS+FORCE RLS y 9 funciones endurecidas antes de revertir. El postcheck read-only confirmó el catálogo post-zonas previo intacto, `app_api` runtime y cero sesiones; no hubo cambio persistente ni fixtures.
- El perfil `post_dining_tables_v1` conserva los perfiles históricos y pinnea por SHA-256 una auditoría global exacta de nueve tablas y nueve funciones. El fingerprint de `app_api` permite únicamente las seis capacidades privadas esperadas post-mesas y rechaza funciones extra, ownership o grants de tabla. La auditoría global pasó dentro de un segundo rollback-only real; API pasó lint, typecheck y toda su suite.
- `20260901000100` quedó aplicada persistentemente con autorización exacta: staging temporal de nueve migraciones hash-idénticas, historial previo de ocho, dry-run de una sola versión y postcheck de nueve versiones. El auditor global post-mesas pasó y `app_api` siguió runtime sin sesiones. `db lint` detectó `42702` por columnas sin alias en replay de `create_dining_table` y `update_dining_table_layout`; no se corrigió remotamente ad hoc ni se editó la migración aplicada. `20260901000200_qualify_dining_table_function_columns.sql` reemplaza únicamente esas dos funciones con aliases explícitos y conserva firmas/owner/seguridad/grants; está verde localmente y pendiente de autorización rollback-only.
- El rollback-only autorizado de `20260901000200` compiló ambas funciones corregidas y conservó el resumen exacto de 5 políticas, 9 tablas y 9 funciones antes de revertir. El postcheck global devolvió runtime/zero-sessions y el historial read-only confirmó que el remoto todavía termina en `20260901000100`. `db lint` no puede observar cambios no confirmados desde su conexión independiente; debe repetirse inmediatamente después de un eventual apply de `00200`.
- `20260901000200` quedó aplicada con autorización separada después de un staging de diez migraciones hash-idénticas y dry-run de una única versión. El historial remoto contiene diez versiones exactas; auditor global post-mesas runtime/zero-sessions y `db lint --level error` sobre `app`, `app_private`, `app_rls` quedaron verdes con cero issues. El defecto `42702` queda cerrado sin editar historia aplicada.

## Estado del remoto legado

- origin/main contiene un prototipo previo con app.js, index.html, style.css, supabase.js y otros archivos.
- main y master versionan node_modules; se contaron 10,427 rutas bajo ese directorio en cada rama.
- El prototipo debe preservarse hasta que el humano elija una estrategia.
- Auditoría exacta de licencia: `origin/main@293a1551e88d7956ac64d2545448d9a9d6346bc7` y `origin/master@f87d5b255695ef28767088c4d0f7dc6a0dc62951` no contienen licencia, NOTICE, COPYING, README ni campo `license` del paquete para el código propio. Las licencias bajo `node_modules` pertenecen solo a dependencias. Hasta recibir autorización/licencia escrita del titular, el prototipo puede orientar requisitos pero no aportar código copiado al greenfield.
- Recomendación: archivar el prototipo en una rama/tag, crear una rama limpia de modernización, añadir .gitignore y publicar esa rama para revisión antes de cambiar main.
- No retirar archivos versionados ni reescribir historia sin autorización explícita.

## Producto

superRestaurant será un POS para restaurantes con:

- web administrativa/POS;
- mobile para meseros;
- KDS;
- mesas, canales, menú, órdenes, pagos y caja;
- inventario/reportes en fases posteriores;
- operación offline por dispositivo en v1;
- servidor local LAN opcional y separado.

## Arquitectura base aceptada

- pnpm + Turborepo;
- TypeScript;
- NestJS + REST/OpenAPI como frontera de aplicación y única ruta de escrituras críticas;
- Supabase Auth + PostgreSQL administrado; Storage cuando el módulo defina políticas;
- migraciones SQL versionadas de Supabase como autoridad única del schema;
- ORM, cola y transporte Realtime todavía pendientes; Realtime nunca sustituye recuperación durable por cursor;
- Next.js;
- Expo/React Native;
- React para KDS;
- packages/domain para lógica pura;
- packages/shared-types;
- packages/sync-engine;
- adaptadores para pagos, PAC, impresión y terceros.

Esta es la alternativa B aceptada por ADR-010 y delimitada por ADR-001. Data API directa solo puede servir lecturas RLS/CRUD no financiero expresamente permitido; no existe una segunda ruta para escrituras críticas.

- `apps/api` usa configuración fail-closed de Auth y PostgreSQL, `auth.getUser(token)`, identidad derivada/congelada, `APP_GUARD` global y scope UUID exacto. Solo `/api/v1/health` es público; `/api/v1/session` exige identidad y `POST /api/v1/access/branch` revalida membresía efectiva mediante una función PostgreSQL privada y devuelve 403 genérico ante cualquier rechazo.
- La URL runtime PostgreSQL solo acepta el rol dedicado `app_api` ligado al mismo project ref, TLS `verify-full`, CA vigente y host directo/Supavisor autorizado. `postgres`, parámetros de conexión inesperados y CA inválida se rechazan. Nest no recibe grants de tablas: `app_api` solo puede ejecutar el lookup privado.
- La migración productiva inicial vive en `supabase/migrations`, separada del spike. Modela Restaurant/Branch activos, membresías exactas históricas y múltiples roles históricos; usa FK compuesta, índices parciales, RLS+FORCE RLS, helpers caller-bound y grants de lectura explícitos. `app_private`/`app_rls` no se exponen. ADR-002 mantiene esta frontera como propuesta hasta evidencia PostgreSQL real.
- Las funciones `SECURITY DEFINER` quedan explícitamente en propiedad de `postgres` y la migración aborta si ese owner no tiene `BYPASSRLS`. `app_api` nace `NOLOGIN`, sin contraseña ni privilegios elevados; una colisión aborta. PostgreSQL 16 concede obligatoriamente a su creador no-superusuario una fila ADMIN con grantor bootstrap: en Supabase es `app_api` → `postgres`, otorgada por `supabase_admin`, sin INHERIT ni SET. Las auditorías aceptan solo esa relación inerte y rechazan cualquier otra. Su aprovisionamiento privado posterior está documentado y exige prueba de mínimos privilegios.
- Cuarenta y dos pruebas de API cubren configuración Auth/DB, principal, autorización, adapter hostil-resistente, contratos de migración/config, verificador transaccional y smoke Nest 200/401/403. El verificador fresh aplica migración+catálogo dentro de una transacción y siempre revierte; su CLI y la del E2E solo emiten códigos sanitizados.
- El runner remoto de tenancy crea usuarios desechables y fixtures 2×2, comprueba Data API para anon/authenticated/service role, conexión mínima `app_api`, Nest real, false pairs, constraints y revocación con el mismo token, y limpia en orden FK-safe. La auditoría pre-provisioning exige `NOLOGIN`; la auditoría runtime exige el `LOGIN` necesario y conserva todos los demás mínimos. La recuperación standalone exige UUID y confirmación repetida, opt-in propio, mismo advisory lock, metadata/email exactos, grafo 2×2 completo o Auth-only y deletes por IDs con `RETURNING`; aborta ante estados parciales o contaminados.
- La contraseña administrativa comprometida fue rotada sin publicarla. La nueva credencial conectó por el pooler al proyecto exacto `zwbyiefqeujstyzysydn` usando la CA local y TLS `verify-full`. Tras demostrar primero rollback seguro, Emmanuel autorizó la aplicación persistente de `20260830000100`: quedó como sexta migración remota, sin seeds/roles/Vault/config push adicionales. La auditoría posterior confirmó 5 políticas, 5 tablas RLS+FORCE RLS y 4 funciones `SECURITY DEFINER`; `db lint` quedó sin errores.
- La evidencia remota detectó dos incompatibilidades antes de aplicar: el `postgres` administrado no es superusuario, y `pg` devuelve un arreglo de resultados para SQL multi-statement. La migración evita ALTERs reservados, audita la concesión ADMIN obligatoria de PostgreSQL 16 sin relajar otras membresías, y el verificador normaliza resultados únicos/múltiples. CodeGraph quedó actualizado en 84 archivos, 1,825 nodos y 7,255 relaciones; `runSchemaVerification` continúa limitado a su CLI y pruebas.
- El historial del ref aislado contiene exactamente las versiones `20260825000100` a `20260830000100`; las primeras cinco pertenecen al spike y la sexta es `20260830000100_create_tenancy_memberships.sql`, primera migración productiva.
- El `db push --dry-run` desde la raíz rechazó correctamente la ausencia local de las cinco migraciones remotas y no se siguió su sugerencia destructiva de reparar historial. Un staging temporal con copias exactas de esas cinco migraciones más la productiva confirmó que aplicaría solo `20260830000100`; tras autorización explícita se ejecutó desde ese staging y luego se eliminó. No se reparó historial ni cambió el enlace.
- CodeGraph posterior a la recuperación indexó 90 archivos, 1,949 nodos y 7,744 relaciones. El entrypoint destructivo solo consume configuración administrativa y el recovery; comparte marcadores/lock con el runner y no es alcanzable por controladores o clientes. Sus cinco pruebas de recuperación y tres de configuración elevan la suite API a 50 casos locales.
- El aprovisionador privado de `app_api` permanece local y no ejecutado. Valida proyecto/host/CA/TLS y contraseña sin imprimirla, fija SCRAM, toma un advisory lock global, concede `LOGIN` con expiración de diez minutos, prueba una conexión nueva y sus permisos permitidos/denegados, audita el estado runtime y solo entonces promueve la credencial a vigencia indefinida. Cualquier fallo posterior al intento de cambio abre una sesión administrativa nueva y compensa a `NOLOGIN PASSWORD NULL`; SQL y auditorías están fijados por hash.
- La suite API asciende a 74 casos e incluye lock ocupado, commit ambiguo, identidad privada incorrecta, auditoría alterada, fallo de compensación, estados de recovery aplicados/no aplicados, OID cambiado y sesiones que reaparecen. CodeGraph no estuvo disponible para la reconsulta de este corte; la inspección manual dirigida confirmó que los CLI son los únicos consumidores de aprovisionamiento/recovery, el lock se comparte con el runner E2E y no existe referencia desde controladores o clientes.
- La recuperación standalone de `app_api` usa configuración/confirmación destructiva propias sin contraseña de aplicación. Acepta estado temporal, expirado, runtime o ya deshabilitado solo con fingerprint de rol exacto; confirma primero `NOLOGIN PASSWORD NULL`, después termina sesiones en hasta tres barridos y exige desde una conexión nueva el mismo OID, cero sesiones y la auditoría pre-provisioning fijada por hash. Un commit ambiguo se reconcilia y como máximo se repite una vez.
- Incidente de credencial posterior: un diagnóstico read-only falló antes de conectar porque la URL local estaba entre comillas, pero `ERR_INVALID_URL` imprimió la URL administrativa completa en la salida de la tarea. El script temporal se eliminó, no hubo mutación remota ni secreto versionado y esa contraseña no se reutilizará. Emmanuel confirmó una segunda rotación el 2026-08-30; la nueva credencial no se leyó ni imprimió en esta unidad local.
- La publicación oficial incompleta de NestJS 11.2.3 omitió un módulo runtime requerido; `apps/api` quedó fijada coherentemente en 11.2.2 y la suite volvió a pasar. El spike histórico conserva su versión original.

## AGENTS.md

El archivo heredado del ATS fue reemplazado el 2026-08-25 con autorización humana.

La versión nueva es específica para el POS y cubre:

- flujo operativo y CodeGraph;
- estados de tareas;
- arquitectura y límites;
- dinero, pagos y caja;
- Restaurant/Branch y RBAC;
- offline y sincronización;
- migraciones;
- seguridad y privacidad;
- UI y accesibilidad;
- pruebas, observabilidad, backups y Definition of Done;
- Git, artefactos y delegación.

Queda en REVIEW.

## Plan maestro v2.2

PLAN_MODERNIZACION_POS_RESTAURANTE.md está listo para iniciar Fase 0 neutral y ejecutar ADR-010.

Mejoras principales:

- separa piloto online v0.1 de v1 offline;
- fija el stack y lleva alternativas a ADR;
- define invariantes monetarias y snapshots;
- trata pagos/caja como operaciones inmutables e idempotentes;
- formaliza envelope, protocolo y conflictos de sync;
- explicita que KDS multi-dispositivo sin WAN necesita servidor local;
- adelanta seguridad, tenancy, backups y observabilidad;
- añade criterios de aceptación por fase;
- registra riesgos y estrategia Git.
- ADR-010 ahora compara stack propio, alternativa híbrida Supabase+NestJS y Data API restringida mediante un spike bloqueante.
- `card_manual` quedó separado de la integración real con terminal/pasarela. El corte server-side P1 conserva únicamente confirmación externa, proveedor, terminal y referencia opcional; rechaza datos de tarjeta y no selecciona ni invoca proveedor.
- La secuencia autoritativa de dispositivo cubre todos los intentos financieros. Como el agregado `CashRegister` solo conserva movimientos de efectivo, sus secuencias deben ser estrictamente crecientes pero pueden tener saltos correspondientes a eventos no monetarios en caja, por ejemplo `card_manual`; reutilizaciones y retrocesos permanecen inválidos.
- La persistencia local propuesta para caja/pagos usa funciones privadas `app_api`, bloqueo transaccional de Order/caja/dispositivo, versiones optimistas y un ledger inmutable. El total se recalcula en Nest desde snapshots históricos y PostgreSQL vuelve a comprobar versión, saldo capturado, scope e importe antes del commit atómico.
- La tabla de referencias se corrigió y dejó de usar estrellas o afirmaciones de producción como evidencia.
- El spike tiene timebox de 4 días hábiles y hard stop al quinto.
- Cada opción tiene gates, scoring y condiciones GO/NO-GO.
- packages/domain puede avanzar en paralelo sin framework, ORM, red ni almacenamiento.

## Decisiones humanas pendientes

- Ratificar que el v1 incluye Fases 0–3.
- Confirmar mercado, moneda, zona horaria, impuestos y CFDI.
- Confirmar modelo de licencia/comercial.
- Seleccionar hardware de piloto.
- Mantener el prototipo remoto intacto; cualquier reutilización exige licencia/autorización escrita y tarea explícita conforme a ADR-006.
- Confirmar si KDS LAN durante caída total es requisito de v1.

## Riesgos duraderos

- doble cobro o pérdida de venta;
- fuga de datos entre restaurantes;
- conflictos offline silenciosos;
- alcance v1 excesivo;
- dependencias de hardware/PAC/pagos;
- incumplimiento de licencias de referencias/dependencias;
- historia Git contaminada por artefactos;
- migraciones sin rollback.
- 2026-09-01: la E2E remota de mesas/layout se prepara como extensión del arnés de tenancy existente, con `verifyDiningTables` implicando zonas, auditor global post-mesas, denegación Data API/app_api, idempotencia, concurrencia optimista y revocación. La recuperación descubre y valida únicamente la mesa marcada y su prefijo de auditoría `created`/`layout_updated`, y elimina en orden auditorías de mesa → mesas → auditorías de zona → zonas. La ejecución remota sigue requiriendo autorización expresa para crear/eliminar fixtures temporales.
- 2026-09-01: el lifecycle de `app_api` incorpora perfil fijado `post_dining_tables_v1`. La validación de una credencial nueva por Supavisor usa hasta tres conexiones frescas con espera acotada de 1 s y 2 s; cualquier fallo final conserva la compensación a `NOLOGIN PASSWORD NULL`. Dos intentos previos sin esta tolerancia fallaron en `connection` y ambos postchecks confirmaron `safe_disabled`, cero sesiones y catálogo íntegro.
- 2026-09-01: el tercer intento autorizado del aprovisionador corregido completó `app_api` con `appApiLogin=true`, protección de expiración y auditoría runtime; el postcheck quedó `runtime`, catálogo válido y cero sesiones. La clave secreta moderna real se guarda únicamente en `.env.adr010.local` ignorado. La E2E post-mesas no llegó a fixtures: el primer preflight usó una clave CLI enmascarada y fue rechazado en configuración antes de `runId`; el usuario pausó después de guardar la clave completa.
- 2026-09-02: la clave `sb_secret_` completa fue validada contra la administración de Auth con HTTP 200 sin exponer usuarios ni secretos. Tras dejar expirar el circuit breaker y usar conexiones frescas, `app_api` volvió a runtime con auditoría y cero sesiones. La E2E posterior falló únicamente porque el nombre local del fixture excedía el contrato de 40 caracteres; no fue un fallo de credenciales, migraciones ni catálogo remoto.
- 2026-09-02: la segunda E2E real confirmó que el nombre corregido permite crear la mesa, pero un fallo posterior anterior a la actualización quedó enmascarado por una precondición demasiado fuerte del cleanup. La recuperación eliminó el grafo completo conocido y el postcheck remoto quedó verde; no se alcanzó un resumen con `diningTablesVerified`/`diningZonesVerified`.
