# HANDOFF

## Cierre 2026-09-03 — caja y pagos simples aprobados

- Aprobación: Emmanuel aprobó el corte financiero; “Implementar caja y pagos simples: apertura, efectivo, tarjeta manual y cierre” pasa REVIEW → DONE. Se mantiene el alcance aprobado: servidor/API y migración versionada local, sin UI/cortes X-Z, decisiones fiscales, CFDI, moneda implícita, refunds ni proveedor integrado.
- Evidencia conservada: domain 101 pruebas, API 203, KDS 9, web 40, lint/typecheck/builds verdes; rollback-only PostgreSQL final `status=ok` con 5 políticas, 23 tablas RLS+FORCE RLS y 21 funciones `SECURITY DEFINER`; CodeGraph actualizado en 220 archivos, 4,023 nodos y 16,095 relaciones. `20260903000200` no está aplicada y el rollback no dejó estado persistente.
- Git/siguiente acción: Emmanuel autorizó commit y push del corte actual en `main`, sin rebase ni force-push. La siguiente tarea elegible es “Implementar cobro y cortes X/Z en web”. Antes de editar, inspeccionar el shell y flujos POS web, los contratos/endpoints financieros actuales y todos sus consumidores; definir alcance, riesgos, aceptación y verificación. No repetir KDS, rollback financiero ni aplicar la migración sin autorización nueva. Mantener dinero exacto, idempotencia, auditoría, aislamiento por Restaurant/Branch y correcciones compensatorias; no inventar moneda, fiscalidad, CFDI o proveedor. Subagentes: ninguno; razonamiento alto.
- Publicación Git: el fetch previo confirmó `HEAD=origin/main=83935bfff5bd0232d8b5b064b1d5b6c433ef3b77`. El corte funcional y documental se publicó por fast-forward en `origin/main` como `b421c917e01f75b222cb3391876df145f9aff1ee` (`feat: add simple cash register payments`). No hubo merge, rebase, force-push ni descarga de cambios ajenos.

Última actualización: 2026-09-03

## Estado actual

El plan v2.3 está en implementación. Emmanuel aprobó ADR-010 opción B y ADR-011; ADR-001 registra sus límites. Las migraciones productivas hasta `20260903000100` están aplicadas persistentemente en `zwbyiefqeujstyzysydn`; el último postcheck confirmó 13 versiones exactas, `app_api` runtime, catálogo global post-KDS íntegro y cero sesiones. Mesas/layout, menú y KDS mínimo P1 están DONE por aprobación humana; Realtime permanece en REVIEW. El siguiente trabajo elegible es el slice separado de caja y pagos simples, sin repetir migraciones/E2E KDS ni asumir decisiones fiscales todavía pendientes.

## Continuación 2026-09-03 — KDS aprobado por el humano

- Estado: Emmanuel aprobó expresamente el corte KDS resumido desde la evidencia ya verificada. “Crear KDS mínimo por estación” pasa de REVIEW a DONE; no se repitieron migraciones, rollback-only, E2E, recovery ni provisioning.
- Evidencia conservada: `runId=f63ca754-f1e4-4bb3-b574-ecd88e4fc45a`, 150 checks, aislamiento por tenant/sucursal/estación, Realtime, cursor durable, revocación, cleanup total, navegador desktop/móvil limpio, API 194 pruebas y KDS 9 pruebas.
- Git/CodeGraph: el árbol se confirmó limpio con `HEAD=origin/main=83935bfff5bd0232d8b5b064b1d5b6c433ef3b77`. El índice local estaba materialmente atrasado aunque declaraba estar al día; se resincronizó sin tocar fuentes y quedó en 213 archivos, 3,829 nodos y 15,096 relaciones. La exploración enlazó el cliente KDS, controlador/servicio PostgreSQL, agregado Order, recuperación por cursor y arnés de verificación esperados.
- Siguiente acción: inspeccionar ADR-007, `Payment`, `CashRegister` y todos sus consumidores; definir alcance, riesgos, aceptación y pruebas antes de marcar caja/pagos simples IN_PROGRESS. Subagentes: ninguno; razonamiento alto por dinero, idempotencia, auditoría y aislamiento.

## Continuación 2026-09-03 — alcance de caja y pagos simples

- CodeGraph/consumidores: `Payment`, `createOrReplayPayment`, `transitionPayment` y `refundPayment` solo alcanzan `packages/domain/src/index.ts` y `payment.test.ts`; `CashRegister`, `openCashRegister`, `appendCashMovement`, `closeCashRegister` y `expectedCashBalance` solo alcanzan el index y `cash-register.test.ts`. No existe consumidor productivo ni persistencia financiera fuera del spike ADR-010. El slice debe integrar esos agregados con los patrones vigentes de contratos fail-closed, Nest, funciones PostgreSQL privadas y RBAC; no crear otra máquina de estados.
- Alcance: contratos v1 y API server-side para abrir una sesión de caja, cobrar una Order `open`/`partially_paid` mediante `cash` o `card_manual`, y cerrar la sesión. El servidor deriva actor/scope autorizado, recalcula el total desde snapshots de Order, impide exceder el saldo, transiciona a `partially_paid` o `paid`, y persiste atómicamente Payment, auditoría, estado de Order y, solo para efectivo, CashMovement. `card_manual` exige evidencia explícita de confirmación externa y snapshot textual de proveedor/terminal, sin seleccionar ni invocar proveedor. La UI de cobro y cortes X/Z sigue en la tarea posterior.
- Límites: sin moneda/zona predeterminadas, impuestos/tasas nuevas, CFDI, propina fiscal, conversión, terminal integrada, PAN/CVV, refunds ni offline. La moneda se hereda de la Order y debe coincidir con la sesión. No se edita ni borra historia financiera; una corrección futura solo podrá añadir refund/movimiento compensatorio referenciado y auditado.
- Riesgos principales: doble cobro/replay divergente; sobrepago concurrente; apertura doble de la misma caja; commit parcial entre Order/Payment/CashMovement; actor/scope o totales manipulados; secuencia de dispositivo reutilizada; cerrar con diferencia sin motivo; tratar `card_manual` como autorización del POS o aceptar datos de tarjeta.
- Criterios de aceptación: (1) parsers exactos rechazan extras, importes no enteros/seguros, UUID/timestamps inválidos y métodos distintos de `cash`/`card_manual`; (2) apertura y cierre tienen replay exacto y conflicto divergente; (3) una sola sesión abierta por caja/scope; (4) cobro revalida `payments.collect`, Order y caja en el scope exacto, usa el total histórico, bloquea sobrepago/doble envío y soporta liquidación parcial/completa; (5) efectivo crea exactamente un movimiento `cash_sale` enlazado y secuenciado, tarjeta manual no altera efectivo y conserva confirmación/proveedor/terminal/referencia opcional; (6) cierre congela esperado, contado y diferencia, exigiendo motivo si no es cero; (7) tablas quedan RLS+FORCE RLS, sin grants cliente y con funciones privadas mínimas para `app_api`; (8) pruebas cubren dominio, contratos, servicio/adaptador, aislamiento, idempotencia, concurrencia, atomicidad y migración PostgreSQL rollback-only antes de REVIEW.
- Estrategia de verificación: pruebas unitarias de dominio existentes más regresiones necesarias; pruebas runtime de contratos; integración del servicio con doble de puerto; verificador transaccional de la migración y auditor global preparados localmente; lint, typecheck, suites y build del alcance; CodeGraph posterior. No se ejecutará migración, rollback remoto, provisioning, E2E ni Git remoto sin autorización nueva.
- Estado: caja/pagos simples pasa TODO → IN_PROGRESS. Subagentes: ninguno; razonamiento alto.

## Continuación 2026-09-03 — implementación local de caja y pagos simples

- Estado: el slice server-side/API está implementado y verificado; pasa IN_PROGRESS → REVIEW, pendiente de aprobación humana. `20260903000200_create_cash_registers_simple_payments.sql` no se aplicó persistentemente en Supabase. Emmanuel autorizó únicamente su verificación transaccional rollback-only.
- Implementación: `packages/shared-types/src/payments.ts` añade comandos/respuestas v1 exactos para apertura, cobro y cierre; `Payment` conserva evidencia explícita y no sensible de `card_manual`; `CashRegister` admite saltos estrictamente crecientes respaldados por el cursor autoritativo cuando hubo eventos financieros sin movimiento de efectivo. `apps/api/src/payments.ts` y `payments.controller.ts` exponen apertura, cobro parcial/completo y cierre con RBAC, actor derivado, totales históricos, moneda exacta, versiones optimistas y errores estables.
- Persistencia/migración: la migración local crea sesiones de caja, pagos capturados, ledger de movimientos, auditoría financiera y cursor privado de dispositivo. Solo cinco funciones `security definer` son ejecutables por `app_api`, incluida una consulta exacta de replay que antecede a la validación de versiones; las tablas quedan RLS+FORCE RLS sin CRUD cliente. Apertura única, replay exacto, bloqueo de sobrepago concurrente, actualización Order/Payment/CashMovement atómica y cierre con diferencia/motivo están codificados. `supabase/tests/cash_registers_simple_payments_catalog.sql` y el runner rollback-only quedaron preparados; rollback esperado: la transacción completa, sin estado persistente.
- Límites conservados: sin UI ni cortes X/Z, refunds, offline, CFDI, reglas fiscales nuevas, moneda implícita, integración de terminal, PAN/CVV, cambios de credenciales, commit o push. `card_manual` solo registra una confirmación externa textual y no pretende autorizar/capturar una tarjeta desde el POS.
- Verificación: lint global directo verde; typecheck y builds verdes para config, domain, shared-types, API, KDS y web; Next y Vite produjeron builds limpios. Pasaron las suites completas afectadas: domain 101 pruebas, API 203 pruebas, shared-types runtime, KDS 9 y web 40. La regresión nueva confirma que reintentos exactos de pago/cierre devuelven el resultado histórico antes de leer agregados con versiones avanzadas y que un replay divergente sigue en conflicto. La primera ejecución PostgreSQL efectiva llegó al auditor y terminó en rollback con SQLSTATE `42883` por dos usos inválidos de `pg_catalog.coalesce`; corregido el auditor, la segunda ejecución autorizada terminó `status=ok` con 5 políticas, 23 tablas protegidas y 21 funciones `SECURITY DEFINER`. El contrato estático pasó nuevamente sus 25 pruebas y `git diff --check` quedó verde. El wrapper Turbo no fue evidencia válida porque el runtime Codex intenta purgar `node_modules` al detectar otra versión de Node; se restauraron exactamente las dependencias del lockfile (278 reutilizadas y 9 descargadas, sin scripts) y todas las verificaciones se ejecutaron con los binarios instalados.
- CodeGraph posterior: índice actualizado en 220 archivos, 4,023 nodos y 16,095 relaciones. `FinancialService` alcanza únicamente su controlador, módulo y pruebas; `CardManualEvidence` alcanza Payment, codec y servicio; `CollectPaymentCommandV1` alcanza parser, servicio/adaptador y pruebas. No aparecieron consumidores productivos alternos ni referencias huérfanas.
- Riesgo/siguiente acción: la migración sigue sin aplicar y la UI/cortes X-Z pertenecen a la tarea posterior. El rollback-only probó sintaxis y catálogo real; las reglas de replay, concurrencia, atomicidad y aislamiento están cubiertas por la combinación de invariantes SQL, contratos estáticos y pruebas de dominio/servicio, pero aún no existe una E2E financiera poblada. Emmanuel aprobó el corte como DONE; aplicar la migración o ejecutar una E2E requiere autorización separada. Subagentes: ninguno; razonamiento alto. No hubo cambios de credenciales, provisioning, fixtures ni Data API.

## Continuación 2026-09-03 — E2E KDS y smoke visual verdes

- Ejecución: precheck `post_kds_v1` verde, exactamente una clave pública cargada en memoria y una sola ejecución funcional autorizada. `runId=f63ca754-f1e4-4bb3-b574-ecd88e4fc45a` terminó `status=ok` con 150 checks, `kdsTicketsVerified=true`, `ordersRealtimeVerified=true`, `menuCatalogVerified=true`, `protectedKdsSmoke=true`, `fixtureRowsRemoved=true` y `fixtureUsersRemoved=2`; no correspondió recovery ni repetición.
- Cobertura: confirmó lectura KDS 401/200/403, empty state, exclusión `pending`, snapshots de producto/modificador, cantidad, versión optimista y estados `sent/preparing/ready`, retiro después de `delivered`, aislamiento por estación, sucursal, tenant y false pair, notificación Socket.IO, recuperación REST paginada por cursor y rechazo de socket/cursor/listado tras revocar la membresía con token vivo.
- Navegador: con confirmación puntual se enviaron únicamente el correo/password temporales a Supabase Auth del proyecto autorizado. En 1280×720 y 390×844 se verificaron ticket nuevo con acción `Iniciar`, cambio Realtime a `Lista` sin acción, desaparición al entregar y retorno al login tras `Actualizar` con acceso revocado; consola sin errores/warnings. La habilidad Browser detectó overflow móvil de 1,110 px causado por mínimos intrínsecos del selector. `apps/kds/src/styles.css` acota controles/labels y usa `minmax(0,1fr)`; el mismo run revalidó `scrollWidth=clientWidth=390`, targets de 46/54 px y desktop sin overflow. La pestaña temporal se cerró y el servidor Vite ya no escuchaba al finalizar.
- Postcheck/seguridad: el runner borró exactamente todas las filas marcadas y dos usuarios. El postcheck posterior devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`. No quedaron leases/acks; capturas temporales, `dist` y `.test-dist` se eliminaron antes del commit. No hubo cambios adicionales de esquema, Data API, credenciales persistentes, provisioning, offline ni integración externa.
- Verificación final: API lint/typecheck/build verdes y 194 pruebas en 30 archivos; KDS lint/typecheck/build verdes y 9 pruebas en cuatro archivos; `git diff --check` verde salvo avisos LF→CRLF. CodeGraph se reconsulta antes de publicar. KDS pasa de IN_PROGRESS a REVIEW, pendiente de aprobación humana para DONE.
- Siguiente conversación: leer los cuatro archivos operativos una vez y la sección P1 del plan; confirmar `HEAD=origin/main` en el commit publicado y CodeGraph al día. No repetir rollback/apply/E2E KDS. Presentar el corte KDS para aprobación humana; si Emmanuel lo aprueba, cambiar REVIEW→DONE y empezar un plan acotado para “caja y pagos simples” consultando dominio financiero existente, ADR-007 y todos los lectores/escritores antes de tocar persistencia. Mantener dinero exacto, idempotencia, movimientos compensatorios y al menos pruebas unitarias de dominio + integración PostgreSQL. Las decisiones humanas de mercado/impuestos/CFDI siguen pendientes y no deben inventarse.
- Subagentes: ninguno; razonamiento alto por tenancy, revocación, recuperación, esquema compartido y verificación visual.
- Git remoto: el fetch previo confirmó `HEAD=origin/main=297176c`. El corte KDS se publicó por fast-forward como `f49d691` (`feat: complete kitchen display workflow`) en `origin/main`; no hubo merge, rebase, force-push ni cambios remotos ajenos. Este registro se publica en un segundo commit exclusivamente documental para dejar el árbol limpio y el hash funcional trazable.

## Continuación 2026-09-03 — primer lanzamiento E2E KDS rechazado antes de fixtures

- Ejecución: el precheck read-only devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false`; Supabase CLI confirmó cuatro entradas y exactamente una clave `publishable`, cuyo valor se mantuvo solo en memoria. La configuración completa y el coordinador pasaron un preflight local. El lanzamiento autorizado terminó `TENANCY_VERIFICATION_CONFIGURATION_REJECTED` antes de emitir `runId`; no se repitió.
- Causa/corrección: el wrapper KDS activaba Orders/Realtime, pero `validateRuntimeAuditSql` elegía por esa bandera las marcas `POST_ORDERS_*` y rechazaba el auditor correcto `POST_KDS_*`. `RunTenancyVerificationOptions` ahora declara `verifyKdsTickets`; este perfil implica Orders/Realtime y selecciona exclusivamente `POST_KDS_SURFACE_REJECTED` y `POST_KDS_APP_API_OBJECT_PRIVILEGE_REJECTED`. El runner Orders puro conserva el perfil anterior y el KDS lo activa solo cuando corresponde.
- Verificación local: la compuerta aislada con el auditor real alcanzó `onStart` (`POST_KDS_AUDIT_ACCEPTED`), demostrando que el rechazo previo quedó resuelto antes de cualquier conexión/fixture. API pasó lint, typecheck, build y los 30 archivos/190 pruebas. CodeGraph actualizado conserva 213 archivos, 3,822 nodos y 11,012 relaciones; `runTenancyVerification` impacta seis símbolos esperados en el mismo arnés.
- Postcheck/residuos: lectura administrativa y Auth acotada devolvió `markedRows=0`, `markedUsers=0`. El postcheck oficial final volvió a reportar `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`. No correspondió recovery porque nunca existió `runId` ni plan de fixtures.
- Estado/siguiente acción: KDS sigue IN_PROGRESS. Solicitar autorización nueva para una sola ejecución del runner corregido; si entonces emite `runId` y falla, ejecutar únicamente recovery para ese UUID y no repetir automáticamente. No hubo navegador funcional, cambio de esquema, Data API, credenciales persistentes, offline, commit, push, merge o rebase. Subagentes: ninguno; se usó la habilidad Browser para preparar la validación visual, pero no se transmitieron credenciales porque el runner nunca alcanzó la primera pausa.

## Continuación 2026-09-03 — E2E KDS preparada localmente

- Alcance/arquitectura: se extendió `runOrdersRealtimeTenancyVerification` mediante `runKdsTenancyVerification`; no se creó un grafo de fixtures paralelo. El mismo Order marcado comprueba listado KDS vacío y poblado, snapshots/versiones exactos, estados `sent/preparing/ready`, retiro al quedar `delivered`, cursor/Socket.IO existentes y aislamiento por estación, sucursal, tenant y false pair. Tras revocar la membresía, la lectura KDS exige 403.
- Smoke visual seguro: `run-kds-protected-smoke.ts` y su coordinador pausan el mismo run recuperable en `sent`, `ready` y `revoked`. La lease inicial contiene solo el scope, nombres esperados y credenciales Auth temporales; las fases posteriores contienen únicamente `phase` y `runId`. Doble opt-in, ref exacto, ack exacto, timeout y limpieza de tres artefactos temporales son obligatorios. `apps/kds` admite un proxy configurable, pero únicamente a `http://127.0.0.1` o `http://localhost`, para apuntar al puerto aislado 4312 sin ampliar CORS ni exponer credenciales.
- Archivos principales: `apps/api/src/operations/orders-realtime-tenancy-verification.ts`, `tenancy-verification-progress.ts`, `run-kds-tenancy-verification.ts`, `kds-protected-smoke-coordinator.ts`, `run-kds-protected-smoke.ts`, pruebas/contrato y `apps/api/package.json`; `apps/kds/src/kds-proxy-target.ts`, su prueba, `vite.config.ts`, configuración de tests y package script.
- Verificación: API lint/typecheck/build verdes y suite completa verde con 190 pruebas en 30 archivos; KDS lint/typecheck/build verdes y 9 pruebas verdes. `git diff --check` no reportó errores (solo avisos informativos LF→CRLF). Los `dist` y `.test-dist` generados por la verificación se eliminaron. CodeGraph actualizado: 213 archivos, 3,822 nodos y 10,995 relaciones; `runKdsTenancyVerification` solo alcanza sus runners, el coordinador queda acotado a su runner/pruebas y `readLocalProxyTarget` solo a Vite/pruebas, sin dependencias rotas conocidas.
- Estado/riesgo/siguiente acción: KDS continúa IN_PROGRESS porque la UI poblada y la revocación aún no se observaron contra el servicio remoto real. Solicitar autorización separada para una única ejecución de `verify:kds-protected-smoke:remote` contra `zwbyiefqeujstyzysydn`, que creará dos usuarios Auth y filas marcadas temporales; validar desktop y 390×844 en las tres pausas, exigir cleanup de filas y dos usuarios, y terminar con el postcheck read-only `post_kds_v1`. Si el runner emite `runId` y falla, ejecutar solo recovery para ese UUID y no repetir automáticamente.
- Límites: no hubo ejecución remota, fixtures, usuarios, cambios de esquema adicionales, Data API, credenciales persistentes, implementación offline ni interacción Git. Subagentes: ninguno; razonamiento alto por tenancy, revocación, recuperación e integración de navegador.

## Continuación 2026-09-03 — apply KDS verde

- Destino/guardas: el enlace CLI raíz se descubrió apuntando a `cxcnnhafchqslvgvkeye`, no al proyecto productivo autorizado, así que se rechazó como fuente de destino y no se alteró. La conexión administrativa TLS `verify-full` a `zwbyiefqeujstyzysydn` confirmó directamente las 12 versiones previas. Un staging temporal reunió las cinco migraciones ADR y las ocho productivas con hashes de fuente; el dry-run mediante URL explícita anunció únicamente `20260903000100_create_kds_ticket_read_model.sql`, sin seeds ni roles.
- Apply: con autorización separada de Emmanuel, `supabase db push` aplicó exclusivamente `20260903000100`. La CLI no reportó otra migración o cambio. El staging se eliminó después de verificar su ruta absoluta dentro del workspace.
- Postchecks: el runner read-only `post_kds_v1` devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`. Una lectura directa exigió las 13 versiones exactas y confirmó `latest=20260903000100`; `supabase db lint --schema app,app_private,app_rls --level error --fail-on error` terminó sin errores.
- Estado/siguiente acción: KDS continúa IN_PROGRESS solo por evidencia funcional remota. Preparar localmente una E2E recuperable que publique tickets, valide filtrado por estación, transiciones `sent → preparing → ready`, cursor/refresh y revocación; su ejecución con usuarios y fixtures temporales necesita autorización separada. No hubo Data API, credenciales, provisioning, commit, push, merge o rebase. Subagentes: ninguno; razonamiento alto por migración y aislamiento del destino.

## Continuación 2026-09-03 — primer rollback KDS revertido y perfil post-KDS

- Segundo rollback-only: un precheck limpio precedió la única ejecución autorizada. `verify:kds-ticket-schema:rollback` terminó `status=ok` con 5 políticas, 19 tablas protegidas y 16 funciones `SECURITY DEFINER`; migración, auditor específico y auditor global se ejecutaron en la misma transacción y se revirtieron. El postcheck posterior devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`, confirmando que el remoto conserva el baseline post-órdenes. No hubo apply persistente.

- Ejecución remota: el precheck read-only devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`. Los dos intentos de lanzamiento mediante el gestor de paquetes fallaron localmente con acceso denegado y no alcanzaron la red; el runner compilado ejecutó una sola prueba remota efectiva. La migración llegó hasta `catalog_audit`, que falló con `SCHEMA_VERIFICATION_CATALOG_AUDIT_FAILED`, SQLSTATE `42883`; el cierre transaccional hizo rollback. No se repitió bajo la misma autorización.
- Causa/corrección: PostgreSQL no permite calificar la expresión especial `COALESCE` como `pg_catalog.coalesce`. Se sustituyeron las dos apariciones de la migración y las cuatro de los auditores KDS por `coalesce`; `schema-contract.test.ts` cubre los tres archivos contra regresión. El postcheck read-only posterior confirmó nuevamente el catálogo post-órdenes, `app_api` runtime y cero sesiones, por lo que no quedó cambio remoto persistente.
- Ciclo de vida: se añadió `post_kds_v1` a verificación, aprovisionamiento y recuperación de `app_api`, con hash SHA-256 pinneado del auditor global y allowlist exacta que suma únicamente `app_private.list_kds_tickets(uuid,uuid,uuid,text)`. El nuevo runner `verify:post-kds-app-api-state:remote` queda preparado para el post-apply. La revisión detectó además que provisioning/recovery aceptaban `post_orders_realtime_v1` pero elegían el hash post-menú; ahora tienen ramas explícitas y pruebas para post-órdenes y post-KDS.
- Verificación local: API pasó lint, typecheck, build y sus 187 pruebas oficiales ejecutadas en serie; las 41 pruebas específicas de provisioning/recovery/state también quedaron verdes. La ejecución agregada con procesos hijos fue bloqueada por el `spawn EPERM` conocido del sandbox, sin fallos de aserción. CodeGraph quedó al día con 207 archivos, 3,747 nodos y 10,805 relaciones; `readAppApiLifecycleTarget` impacta ocho símbolos esperados y el nuevo runner es el único consumidor nuevo. `git diff --check` se ejecutará al cierre.
- Estado/siguiente acción: esta nota conserva el estado previo al apply; la continuación anterior registra la aplicación ya completada y sus postchecks. KDS continúa IN_PROGRESS hasta E2E poblada/revocación. En este corte rollback no hubo fixtures, Data API, credenciales, provisioning, commit, push, merge o rebase. Subagentes: ninguno; razonamiento alto por esquema, tenancy y lifecycle del rol privado.

## Continuación 2026-09-03 — E2E Realtime verde y primer corte KDS local

- Realtime remoto: la única ejecución autorizada emitió `runId=e3e3cfca-2ed1-4743-b51a-33c526642509` y terminó `status=ok` con 150 checks, `ordersRealtimeVerified=true`, `menuCatalogVerified=true`, `fixtureRowsRemoved=true` y `fixtureUsersRemoved=2`. No se repitió ni requirió recovery. El postcheck final confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false` y `status=ok`; Realtime pasa a REVIEW.
- Git/TLS: el pull del usuario dejó `HEAD=origin/main=297176c` y árbol inicialmente limpio. Los avisos de Windows al desvincular tres `.idx` se diagnosticaron con `git fsck --full`: no existe corrupción, solo objetos dangling recuperables. La CA local ignorada faltaba después del pull; se restauró desde la copia validada `Supabase Root 2021 CA`, SHA-256 `700723581420DD1AC98FD7E9AC529F0EF210EADCAF87FC868A3AD7D114C2F3B7`. No se borraron packs ni se publicó Git.
- KDS local: `packages/shared-types` incorpora tickets v1 y parsers exactos; Nest añade la lectura autorizada `GET /api/v1/kds/tickets`; `20260903000100_create_kds_ticket_read_model.sql` proyecta únicamente Orders abiertas de la estación desde snapshots históricos y devuelve la versión actual para concurrencia optimista. `apps/kds` implementa login Supabase, selección de sucursal/estación, recuperación durable REST, refresco por notificación Socket.IO y acciones `sent → preparing → ready`; una falla de red nunca encola silenciosamente la acción.
- Seguridad/esquema: la función KDS es `STABLE SECURITY DEFINER`, `search_path=''`, owner `postgres` y solo concede `EXECUTE` a `app_api`; revalida membresía/rol/scope y limita la respuesta a 500 tickets. Se añadieron auditor específico, auditor global exacto post-KDS y runner rollback-only. Esta nota describe el corte previo a la ejecución; la continuación anterior registra el primer rollback remoto fallido y revertido. La migración no está aplicada y no hubo fixtures KDS nuevos, cambios de Data API, credenciales o provisioning.
- Verificación local: la compuerta global completó lint, typecheck, tests y build para sus siete workspaces; el build requirió salir del aislamiento por el `spawn EPERM` conocido de Windows. Tras el endurecimiento final del ack WebSocket, KDS volvió a pasar lint, typecheck, sus siete pruebas y build Vite. Navegador real verificó login en escritorio y 390×844, `scrollWidth === clientWidth`, target principal de 46 px y consola final sin errores/warnings; se corrigió la creación doble del cliente Auth bajo `StrictMode`. CodeGraph quedó sincronizado con 206 archivos, 3,730 nodos y 10,770 relaciones; el flujo KDS alcanza únicamente contratos, controlador/servicio/adaptador y cliente esperados. `git diff --check` pasa salvo avisos informativos LF→CRLF.
- Estado/siguiente acción: KDS permanece IN_PROGRESS. Esta nota describe el estado previo al primer intento; la continuación anterior registra su rollback y la corrección local. Siguiente paso mínimo vigente: con autorización nueva, ejecutar una sola segunda verificación rollback-only; si queda verde, solicitar aparte el apply persistente y luego implementar/ejecutar la E2E KDS poblada y revocación antes de REVIEW. Subagentes: ninguno; razonamiento alto por aislamiento tenant, esquema y concurrencia.

## Cierre 2026-09-03 — E2E Orders/Realtime preparada

- Alcance: se añadió `verify:orders-realtime:remote`, basado en el harness de tenancy existente y el auditor global `tenancy_memberships_post_orders_realtime.sql`. El flujo valida rechazo anónimo, creación, replay exacto y conflicto idempotente, permisos viewer/cross-tenant/false-pair, snapshots de catálogo, apertura, transición `pending → sent → preparing → ready → delivered`, notificación Socket.IO, suscripción única, recuperación REST paginada desde `v1:0`, aislamiento por estación/tenant y rechazo de recuperación/suscripción después de revocar la membresía.
- Seguridad y limpieza: las tres tablas Order/KDS se prueban cerradas por Data API para anon, authenticated y secret key. Los checkpoints no incluyen tokens ni fixtures. El hook `finally` valida actor, scope, versión y secuencia exacta de eventos antes de borrar KDS → auditoría → Order → cursor. La recuperación independiente por `runId` ahora acepta únicamente los prefijos transaccionales válidos 1–7, comprueba marcadores `tenancy-orders-v1:<runId>:*`, actores, cursor y secuencia KDS, y aborta ante contaminación.
- Archivos principales: `apps/api/src/operations/orders-realtime-tenancy-verification.ts`, `run-orders-realtime-tenancy-verification.ts`, `tenancy-verification.ts`, `tenancy-verification-progress.ts`, `tenancy-fixture-recovery.ts`, sus pruebas/contratos, `apps/api/package.json` y `pnpm-lock.yaml`. Se añadió `socket.io-client@4.8.1`; no se editaron artefactos generados.
- Verificación: `pnpm lint`, `pnpm typecheck`, `pnpm test` y `pnpm build` pasaron para todo el monorepo; `git diff --check` pasó. CodeGraph fue resincronizado y quedó `up to date` con 195 archivos, 3,627 nodos y 14,409 relaciones. `runOrdersRealtimeTenancyVerification` afecta solo su implementación y runner; `validateTenancyFixtureRecoverySnapshot` afecta recovery, store, orquestación y su prueba, todos esperados.
- Remoto/estado: no se ejecutó `verify:orders-realtime:remote`, no se crearon usuarios ni filas, y no hubo cambios adicionales de esquema, Data API, credenciales o `app_api`. Realtime P1 permanece IN_PROGRESS. Menú P1 pasa a DONE porque Emmanuel lo aprobó expresamente el 2026-09-02. Subagentes: ninguno; razonamiento alto por seguridad, aislamiento, idempotencia y recuperación durable.
- Git remoto: Emmanuel autorizó commit y push. El corte funcional/documental se publicó por fast-forward desde `main@93d44ed` como `1845c786749839f9b23e1f597efdb1deae6a5da0` (`feat: add menu and realtime order foundations`) en `origin/main`; no hubo merge, rebase, force-push ni descarga de cambios ajenos.
- Instrucciones para la siguiente conversación: leer una sola vez los cuatro archivos operativos y la sección P1 del plan; verificar árbol limpio, `main`/`origin/main` en el commit publicado y CodeGraph actualizado; hacer precheck estrictamente read-only de catálogo post-órdenes, `app_api` runtime y cero sesiones; cargar secretos solo desde `.env.adr010.local` sin imprimirlos y obtener por CLI únicamente la clave publishable. Solicitar autorización expresa para una sola ejecución de `pnpm --filter @super-restaurant/api verify:orders-realtime:remote` contra `zwbyiefqeujstyzysydn`, con sus dos usuarios y fixtures temporales. Exigir `ordersRealtimeVerified=true`, `menuCatalogVerified=true`, `fixtureRowsRemoved=true`, `fixtureUsersRemoved=2` y `status=ok`. Si aparece un `runId` y falla, ejecutar únicamente `recover:tenancy:remote` acotado a ese UUID y no repetir automáticamente. Si queda verde, hacer postcheck read-only y mover gateway Realtime a REVIEW; luego iniciar la tarea separada de KDS mínimo y su futuro smoke visual.

## Realtime P1 — corte local y rollback verde (2026-09-02)

- Implementación: `packages/shared-types/src/orders.ts` define contratos runtime versionados. `apps/api/src/orders.ts` y `orders.controller.ts` implementan persistencia atómica, mutaciones REST con concurrencia optimista, snapshots desde el catálogo y recuperación KDS por cursor. `apps/api/src/realtime.gateway.ts` incorpora Socket.IO `/realtime/v1`, WebSocket-only, un scope por socket y notificación `state.changed.v1`; REST/PostgreSQL continúan siendo la autoridad durable.
- Seguridad: el handshake solo acepta `auth.accessToken`; cada suscripción valida Restaurant/Branch/estación y `kds.read`, cada entrega reautoriza el principal y una revocación desconecta el socket. No existen handlers de mutación por WebSocket. La persistencia rechaza transiciones financieras/cancelaciones fuera del alcance y notifica únicamente después del commit.
- Esquema/apply: después del rollback verde, Emmanuel autorizó aplicar solo `20260902000200_create_orders_realtime.sql`. El staging temporal reunió 12 copias hash-idénticas. El historial previo contenía 11 versiones y el dry-run anunció exclusivamente esa migración, con `seeds=[]`, `roles=[]` y `--skip-vault`; la aplicación oficial terminó `status=ok` y reportó únicamente esa versión. El staging fue eliminado de forma acotada y es reproducible desde el repositorio.
- Postcheck: el historial remoto contiene las 12 versiones exactas. El nuevo verificador read-only post-órdenes, con auditor pinneado y allowlist exacta, devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`; el catálogo comprende 5 políticas, 19 tablas protegidas y 15 funciones `SECURITY DEFINER`. `db lint --level error` no encontró errores en `app`, `app_private` ni `app_rls`. Dos prechecks anteriores fueron rechazados en configuración local por el mapeo de URL pública y la codificación de espacios en la ruta de CA; no conectaron ni mutaron la base.
- Verificación local/CodeGraph: el runner post-órdenes y su perfil pasaron lint, typecheck, suite API completa y build. CodeGraph quedó al día en 193 archivos, 3,562 nodos y 14,082 relaciones; `verifyAppApiState` alcanza únicamente los cinco runners de estado y su prueba. `git diff --check` queda como cierre documental final.
- Estado/siguiente acción: P1 permanece IN_PROGRESS. Preparar la E2E recuperable de órdenes/Realtime localmente y solicitar autorización separada antes de crear usuarios o fixtures; solo después corresponderá smoke visual de KDS. No hubo cambios de Data API, credenciales, provisioning de `app_api`, fixtures, offline, commit o push. Subagentes: ninguno; razonamiento alto por esquema, aislamiento, revocación y entrega durable.

## Menú P1 en REVIEW — smoke protegido final (2026-09-02)

- Estado y alcance: la única ejecución autorizada de `verify:menu-web-protected-smoke:remote` emitió `runId=0347895d-f216-492b-8cae-d41c728b78ab` y terminó `status=ok`, `checks=141`, `protectedMenuWebSmoke=true`, `menuCatalogVerified=true`, `fixtureRowsRemoved=true` y `fixtureUsersRemoved=2`. No se ejecutó recovery ni una repetición.
- Verificación visual: navegador real sobre el web local y API temporal remota en puerto 4311. Desktop y 390×844 mostraron categoría, producto, moneda/precio, impuesto, grupo y opción del fixture; selección manager y revocación terminaron correctamente. La consola quedó sin errores/warnings y las solicitudes observadas de `/login`, `/app`, `/app/menu` y la pantalla revocada respondieron 200. Dos mensajes de refresh token pertenecían a la cookie previa al login y desaparecieron al establecer la sesión nueva.
- Corrección durante el mismo smoke: la primera captura 390×844 reveló overflow horizontal por mínimos intrínsecos del shell y controles. `apps/web/src/app/app/layout.tsx`, `apps/web/src/app/app/menu/page.tsx` y `apps/web/src/app/app/menu/menu-catalog-editor.tsx` ahora contienen correctamente el ancho, fluidifican inputs/selects, quiebran nombres largos y compactan el header móvil. La misma página se recargó con los mismos fixtures y confirmó `scrollWidth === clientWidth` arriba y en los controles inferiores; no hubo otra E2E.
- Postcheck remoto read-only: la primera invocación fue rechazada en configuración local antes de conectar porque faltaba derivar `SUPABASE_URL` y `DATABASE_CA_CERT_PATH`; ya corregida, una lectura vio únicamente la sesión transitoria del pool y la lectura final devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`. Los tres archivos temporales lease/ack/lock fueron eliminados por el runner.
- Verificación local: web lint, typecheck, 40 pruebas y build de producción pasaron. La API ya había pasado lint, typecheck, suite completa y build después de añadir el runner específico. `git diff --check` pasó. CodeGraph resincronizó los tres archivos responsive y quedó al día con 182 archivos, 3,318 nodos y 13,086 relaciones; `MenuCatalogEditor` alcanza únicamente su página y `AppLayout` permanece autocontenido.
- Archivos principales de este cierre: `apps/api/src/operations/run-menu-web-protected-smoke.ts`, `apps/api/package.json`, `apps/api/src/schema-contract.test.ts`, los tres archivos web responsive anteriores y `TODO.md`/`PROJECT_NOTES.md`/`HANDOFF.md`. No se editaron artefactos generados; los `AGENTS.md`/`CLAUDE.md` creados automáticamente por Next durante desarrollo fueron retirados.
- Riesgos/siguiente acción: no queda brecha conocida dentro del slice; disponibilidad por sucursal/horario, precios por canal, combos y recetas siguen fuera de alcance. Siguiente acción mínima: revisión humana de P1 y, si Emmanuel lo aprueba, transición a DONE; commit/push requieren autorización separada.
- Operación: no hubo nuevas migraciones, cambios de Data API, rotación de credenciales, provisioning de `app_api`, cambios offline ni interacción Git remota. Subagentes: ninguno; razonamiento alto por aislamiento multi-tenant, esquema y E2E remota.

## Trabajo realizado

Las viñetas de esta sección conservan la evolución cronológica; cuando una cifra histórica difiere, prevalecen el estado actual y las entradas posteriores.

- Git inicializado en la carpeta local.
- origin configurado por HTTPS hacia RenanGomez/superRestaurant.
- fetch exitoso de origin/main y origin/master.
- AGENTS.md reemplazado por reglas específicas de superRestaurant.
- PLAN_MODERNIZACION_POS_RESTAURANTE.md actualizado a versión 2.2.
- TODO.md y PROJECT_NOTES.md actualizados.
- `.gitignore` raíz creado para dependencias, secretos, builds, cobertura, caches, CodeGraph, artefactos móviles y credenciales de firma; las plantillas `.env*.example` siguen siendo versionables.
- Monorepo raíz inicializado con pnpm 11.19.0, Turborepo 2.10.12, workspaces `apps/*`/`packages/*`, scripts orquestados y lockfile reproducible.
- README greenfield/API-first creado y el plan maestro copiado byte a byte en `docs/`.
- `packages/config`, `packages/shared-types` y `packages/domain` creados con ESLint/TypeScript compartidos, contratos tenant, Money, estados Order/OrderItem y pruebas.
- Reglas puras de modificadores añadidas en `packages/domain`: validan catálogo/grupo/producto, actividad, min/max por suma de cantidades, caps por opción, duplicados, moneda y precios; generan snapshots canónicos profundamente inmutables con identidad de grupo compatibles con totales históricos.
- ADR-007 propuesto y `packages/domain/order-totals.ts` implementado: snapshots financieros versionados, impuestos incluidos/excluidos, descuentos de línea/orden, propina, cálculo exacto e inmutabilidad profunda.
- ADR-004 propuesto y acompañado por contrato offline/sync y matriz de capacidades/conflictos auditados. Define protocolo, outbox, deduplicación, causalidad limitada, cursores pull, tombstones, autorización y políticas conservadoras para dinero/KDS sin implementar todavía Fase 3.
- `.github/workflows/ci.yml` creado con instalación frozen, lint, typecheck, tests y build, permisos mínimos y concurrencia por ref.
- `frontend.md` comenzó como plan coordinado FE-0–FE-7. Posteriormente Emmanuel autorizó FE-0 y Claude reconstruyó `apps/web`; el protocolo activo limita a Claude a FE-0 dentro de `apps/web/**` y reserva backend, esquema, contratos, lockfile y documentos operativos al integrador.
- Harness ADR-010 creado en `spikes/adr-010`, con ADR propuesta, fixtures 2×2, contrato de adapters, control en memoria y gates comunes reforzados.
- Opción A preparada en `spikes/adr-010/options/a-own-stack`: adapter PostgreSQL que implementa el contrato, migración aislada, frontera Nest, runner opt-in y restore lógico endurecido. El runner sale con código 2/NO-GO incluso si pasan checks PostgreSQL porque no existe Auth real.
- Opción B completada localmente en `spikes/adr-010/options/b-supabase-nest`: implementa `Adr010Adapter`; Nest verifica sesiones reales con Supabase Auth, deriva el actor y delega una única escritura crítica por PostgreSQL privado. Idempotencia, cleanup, guards de proyecto/TLS y backup/restore lógico fueron endurecidos. El runner permanece opt-in y no puede declarar GO.
- La migración B separa `adr010_b` expuesto para lecturas RLS de `adr010_b_private`, no expuesto, donde viven todas las funciones privilegiadas sin grants Data API.
- Bootstrap Auth B server-only añadido: crea dos usuarios desechables por ejecución, memberships 2×2 y cleanup FK-safe/concurrente/reintentable; `app_metadata` con run ID permite redescubrir usuarios si falla antes del marcador SQL. No imprime credenciales ni prueba el gate Auth.
- Probe RLS B ejecutado: dos sesiones publishable independientes demostraron lecturas propias, filtrado cross-scope y revocación efectiva en el siguiente read; el runner prepara fixtures estructurales y siempre cierra/limpia.
- Opción C preparada en `spikes/adr-010/options/c-supabase-direct`: cliente público encapsulado, solo lecturas scoped de órdenes/KDS, hardening SQL contra escrituras y preflights no probatorios. No contiene mutaciones críticas ni consume `Adr010Adapter`.
- El proyecto Supabase proporcionado `cxcnnhafchqslvgvkeye` fue enlazado. Se aplicaron `20260825000100_adr010_b_thin_slice.sql`, `20260826000100_adr010_c_read_only_hardening.sql` y `20260827000100_adr010_b_bootstrap_rls_hardening.sql`; el rollback está documentado, pero no se ejecutó.
- La auditoría remota read-only de catálogo/historial, acotada a `adr010_b` y `adr010_b_private`, pasó 24/24 checks. No inspecciona ni valida tablas ajenas del schema `public`.
- Al exponer `adr010_b`, un primer push de configuración materializó defaults no deseados de Auth. Se restauraron inmediatamente los valores remotos previos y la verificación posterior confirmó API, DB, Auth y Storage al día; `adr010_b` es la única diferencia intencional y el schema privado nunca fue expuesto.
- No se crearon usuarios Auth ni se ejecutaron bootstrap, probe, gates comunes, concurrencia, reset o restore contra el proyecto proporcionado.
- El proyecto nuevo `ndblkcmdgpxsxylacutx` fue confirmado vacío (0 relaciones de usuario/Auth/Storage/migraciones), recibió las tres migraciones ADR y configuró únicamente la exposición adicional de `adr010_b`; Auth/DB/Storage permanecieron sin cambios.
- Su auditoría estructural pasó 24/24 y el security advisor oficial quedó sin hallazgos. La contraseña restablecida permitió conectar por el pooler; las llaves publishable/secret se obtuvieron solo en memoria y `.env.adr010.local` siguió ignorado.
- La ejecución remota reveló y permitió corregir contratos JSONB, nombres snake_case, preparación de fixtures y cleanup duplicado. El probe RLS y las compuertas completas finalizaron con código 0. El reporte verificó 11 capacidades y mantuvo deliberadamente `eligibleForAdr010Go: false` por brechas conocidas.
- La comprobación final quedó en 0 usuarios marcados, 0 bootstrap rows, memberships, órdenes, líneas, snapshots, idempotencia, auditoría y eventos KDS. Permanecen 2 restaurantes y 4 sucursales deterministas del spike. No quedaron identidades ni datos operativos temporales.
- Limitación TLS: el pooler funcionó con cifrado requerido y compatibilidad libpq, pero sin CA del proyecto; no cuenta como evidencia de `verify-full` para producción.
- Payment/Refund y CashRegister/CashMovement puros quedaron implementados y auditados: estados/idempotencia/scope, refunds parciales y totales, cierre con diferencia preservada, compensaciones acumuladas y cursor global por dispositivo.
- La opción B recibió `20260828000100_adr010_b_financial_write_boundary.sql` y `20260829000100_adr010_b_financial_conflict_fix.sql`. La frontera Nest/Auth/PostgreSQL privada crea pagos y refunds de efectivo con idempotencia, supervisor verificado, movimientos inmutables, auditoría y cursor global por dispositivo.
- La primera auditoría remota descubrió una expectativa de FK demasiado débil en el auditor y `db lint` detectó objetivos `ON CONFLICT` incompatibles. Se corrigieron el auditor y una nueva migración versionada; no se editó la migración ya aplicada. La auditoría final pasó 27/27 y `db lint` quedó sin hallazgos.
- `test:option-b:gates` verificó 14 capacidades remotas y `test:option-b:rls` volvió a pasar. La limpieza final confirmó 0 usuarios Auth y 0 filas operativas/financieras/cursor; quedaron solo 2 restaurantes y 4 sucursales estructurales.
- El gate Auth B conserva refresh tokens únicamente en el mapa privado del adapter, deshabilita auto-refresh/persistencia, rota explícitamente con el SDK, valida identidad y `token_type`, reemplaza access+refresh de forma atómica, prueba una escritura crítica con el nuevo access token y exige rechazo del refresh vigente tras `signOut(..., "global")`. El reporte solo emite seis booleanos. La ejecución remota pasó y elevó la evidencia B a 15 capacidades; la limpieza posterior volvió a confirmar 0 usuarios y 0 datos.
- El intento TLS `verify-full` sin CA falló con `SELF_SIGNED_CERT_IN_CHAIN`. El usuario descargó `prod-ca-2021.crt`; se validó como `Supabase Root 2021 CA`, la conexión obtuvo certificado/hostname autorizados y el runner completo volvió a pasar usando `sslmode=verify-full&sslrootcert=...`. Por instrucción expresa no se volverá a controlar su navegador.
- Se añadió `B_SCORING.md`: gates 1,2,3,5,6,8,9,10 demostrados; gate 4 pendiente de humano y gate 7 parcial hasta ejecutar las cinco migraciones completas en otro proyecto fresco/CI. No asigna score antes de elegibilidad.
- Se añadió un runner fail-closed para proyecto remoto fresco. Verifica enlace CLI al mismo project ref, ausencia total de migraciones/usuarios Auth/Storage/relaciones de usuario, hace dry-run, repite checks anti-TOCTOU y solo aplica con un segundo opt-in. No crea ni elimina proyectos/cuentas.
- `packages/domain/src/order.ts` añadió el agregado puro `Order`/`OrderItem`: canal/mesa, scope explícito por línea, snapshots inmutables, altas, transiciones, cancelación total auditada y totales compartidos. La auditoría corrigió bypasses de líneas KDS, mutaciones después del cierre, total de orden cancelada y accessors alternantes.
- `packages/domain/src/order-audit.ts` añadió eventos inmutables schema v1 para creación, alta de línea y transiciones de Order/OrderItem. Todas las mutaciones del agregado requieren contexto y devuelven la Order junto con su evento; scope, entidad y transición no se aceptan del caller. Toda línea cancelada conserva evidencia enlazada, pending exige motivo y post-KDS también supervisor. La creación y rehidratación fallan cerradas ante accessors, proxies, prototipos, timestamps no canónicos, evidencia ausente u origen imposible.
- Payment/CashRegister ahora crean snapshots estables y revalidan agregados/historias rehidratadas, incluidos refunds, secuencias, referencias, compensaciones acumuladas y saldos de cierre. Transición Payment y cierre CashRegister exigen `eventId`/`idempotencyKey`, soportan replay determinista y detectan conflicto. Money/TaxSnapshot también rechazan formas runtime inválidas mediante errores de dominio.
- El pipeline inicial de esta sesión reprodujo un fallo limpio: el spike resolvía `@super-restaurant/shared-types` desde `dist` antes de que Turbo lo construyera. `typecheck` y `test` ahora dependen también de `^build`; una repetición sin `dist`, caché ni `tsbuildinfo` previos pasó y conserva el contrato público del paquete.
- El fresh-push de Gate 7 separa la configuración exclusiva de migraciones de la configuración Auth: requiere URL pública, conexión PostgreSQL privada y confirmación exacta, pero no una secret key. La frescura ahora incluye relaciones, rutinas y tipos de aplicación inesperados, y el postcheck rechaza cualquier historial distinto de las cinco migraciones ADR exactas.
- `.gitignore` excluye `.crt`, `.cer` y `.pem`. `.certs/prod-ca-2021.crt` existe localmente, fue validado para TLS `verify-full`, permanece ignorado y no se versionó ni imprimió.
- El fresh-push resuelve Supabase CLI mediante `npx`/`npx.cmd` con la versión exacta `2.109.1`; no depende de instalación global. La ruta Windows fue comprobada con `--version` y no amplía los comandos permitidos por el runner.
- FE-0 2026-08-30: antes de escribir código se publicó un mockup (Claude Design) con tres direcciones visuales para login/shell; Emmanuel aprobó "Operational Crisp" (Sora + IBM Plex Sans, acentos oklch azul-teal) y descartó las otras dos. Con esa dirección y autorización explícita de iniciar FE-0, se creó `apps/web` (Next.js 16.3.3 App Router, React 19.2.8, Tailwind CSS 4.3.3, `@supabase/ssr` 0.12.5 — versiones verificadas contra el registro npm, no de memoria): `/login` con Server Action de contraseña y error genérico, `/app` protegido que exige tanto `supabase.auth.getUser()` como una revalidación server-to-server contra `GET /api/v1/session` en `apps/api` (cualquiera de los dos que falle cierra sesión y redirige), `src/proxy.ts` (Next.js 16 renombró `middleware.ts` a `proxy.ts`) que refresca la sesión y fuerza `Cache-Control: private, no-store` en `/app/*`, y `src/env.ts` fail-closed para `SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY`/`API_BASE_URL`/`WEB_ORIGIN` (ninguna `NEXT_PUBLIC_`). No hay cliente Supabase de navegador, recuperación de contraseña, signup ni redirect con destino dinámico en ningún archivo. `apps/web` no depende de `packages/domain` ni `packages/shared-types` en esta unidad (FE-0 no maneja scope de Restaurant/Branch todavía).
- Verificación FE-0: `corepack pnpm --filter @super-restaurant/web` pasó `lint`, `typecheck`, `test` (7/7 en `env.test.ts`) y `build` (`next build` con Turbopack) en verde. El `pnpm lint/typecheck/test/build` agregado por Turbo en la raíz no pudo ejecutarse porque este entorno solo expone `pnpm` a través de `corepack` (no en el PATH), y Turbo necesita invocar el binario directamente; el mismo síntoma se reprodujo contra `packages/domain` ya existente, confirmando que es una limitación del entorno y no algo introducido por esta unidad. La instalación corrió con Node 22.20.0 local (`engines` fija `24.19.0`); pnpm solo emitió una advertencia, no bloqueó la instalación. No se ejecutó smoke humano en navegador (no hay `apps/api`/Supabase reales conectados en esta sesión); permanece IN_PROGRESS por esa razón, no REVIEW. CodeGraph no está disponible como herramienta en esta sesión; la inspección manual dirigida confirmó que ningún archivo fuera de `apps/web/**` cambió y que ningún paquete existente depende de `apps/web`.
- Continuación RBAC 2026-08-31: el checkout partió limpio de `HEAD=origin/main=bf41eb31cd95a34bd83d8618fd36c3122044c7e6`. `packages/shared-types` añadió `RBAC_MATRIX_VERSION=1`, 18 permisos canónicos y parser exacto; Nest añadió una matriz congelada para los nueve roles sin herencia implícita y un evaluador que une roles activos solo después de revalidar la membresía exacta. La selección de Branch migró de listas de roles suministradas por el caller a `branch.select`.
- La matriz documentada separa cancelación pending/sent y reserva privilegios de membresías, catálogo, mesas, pagos, refunds, caja y reportes. Sigue en REVIEW y no sustituye motivo/evidencia/supervisor exigidos por dominio. El primer endpoint restringido ya consume `tables.manage` localmente; su aceptación requiere todavía evidencia PostgreSQL de HTTP 403, false pairs, revocación con token vivo y ausencia de efectos.

## Cierre seguro 2026-08-31 — RBAC v1 y creación de zonas

- Estado: RBAC v1 queda en REVIEW; el slice de mesas/zonas sigue IN_PROGRESS. No marcar DONE sin revisión humana ni elevar todo el módulo de mesas/layout por este corte parcial.
- Código: `packages/shared-types` incorpora el comando/respuesta exactos schema v1; `apps/api` incorpora política RBAC, autorización por permiso, controlador/servicio/adaptador de zonas y errores HTTP estables 400/403/409/503. El actor proviene exclusivamente del principal verificado.
- Esquema: `20260831000100_create_dining_zones.sql` añade `app.dining_zones`, `app.dining_zone_audit_events` y `app_private.create_dining_zone`; zona+auditoría se escriben atómicamente, el replay exige binding exacto y los clientes no reciben grants. `supabase/tests/dining_zones_catalog.sql` audita owner, `search_path`, RLS/FORCE RLS y ACL.
- Verificación local final: lint 6/6, typecheck 9/9, tests 9/9 y build 6/6 pasaron. La primera pasada global detectó una regresión en una opción heredada del verificador; se corrigió fail-closed y la segunda pasada global quedó verde, incluida la suite API completa de 117 tests.
- CodeGraph posterior: 144 archivos, 2,668 nodos y 7,397 relaciones. `DiningZoneService` tiene impacto contenido en su módulo; `MembershipAuthorizationService` afecta 13 símbolos conocidos en acceso, zonas y pruebas, sin objetos huérfanos detectados.
- Remoto de aplicación: no se ejecutó migración, verificador rollback-only, Data API, aprovisionamiento ni E2E remota en este corte. El usuario autorizó únicamente commit y push Git al cerrar.
- Riesgo conocido en ese corte, ya resuelto por la continuación posterior: se versionaron auditores post-zonas exactos para siete tablas y seis funciones sin relajar el perfil anterior.
- Siguiente acción de ese corte, ya completada por la continuación posterior: `verify:dining-zones-schema:rollback` pasó contra `zwbyiefqeujstyzysydn`; la aplicación persistente y la E2E real todavía requieren autorización separada.
- Subagentes: ninguno en este corte; razonamiento alto por cambios de autorización y esquema compartido.

## Continuación 2026-08-31 — auditor global post-zonas y FE-0.1

- Estado: mesas/zonas continúa IN_PROGRESS. FE-0.1 quedó asignado explícitamente a Claude con ownership exclusivo de `apps/web/**`; FE-1 y posteriores siguen prohibidos.
- Esquema local: se añadieron snapshots separados `tenancy_memberships_post_dining_zones_catalog.sql` y `tenancy_memberships_post_dining_zones_runtime_catalog.sql`. No aceptan ambos estados a la vez: el primero exige `NOLOGIN` sin contraseña/sesiones y el segundo LOGIN SCRAM estable sin sesiones. Ambos exigen exactamente siete tablas, cinco políticas y seis funciones, incluidos grants mínimos de `create_dining_zone`.
- Verificador: `verifyAppApiState` selecciona perfiles pinneados por hashes SHA-256; el nuevo entrypoint `verify:post-dining-zones-app-api-state:remote` reutiliza el mismo lock/configuración y falla antes de red si un auditor cambia. El perfil anterior permanece intacto para el catálogo remoto vigente.
- Verificación local: API lint, typecheck, 127 pruebas y build pasaron después de construir `shared-types`; pipeline global lint 6/6, typecheck 9/9, tests 9/9 y build 6/6 pasó. El entrypoint sin configuración rechazó con `APP_API_STATE_CONFIGURATION_REJECTED`. Node local 22.20.0 mantiene el aviso esperado frente al engine 24.19.0.
- CodeGraph posterior: 145 archivos, 2,690 nodos y 10,420 relaciones. `verifyAppApiState` afecta cuatro símbolos; el nuevo runner no tiene consumidores productivos adicionales y las pruebas afectadas son las dos suites ejecutadas.
- Remoto/esquema: Emmanuel autorizó `verify:dining-zones-schema:rollback` contra `zwbyiefqeujstyzysydn`. El primer intento rechazó antes de red porque la URL local contenía el parámetro redundante `sslrootcert`; se normalizó únicamente una copia en memoria, conservando `sslmode=verify-full` y pasando la CA por su variable dedicada. La ejecución reforzada aplicó `20260831000100`, la auditoría específica y la auditoría global runtime dentro de una transacción, devolvió `policies=5`, `securedTables=7`, `securityDefinerFunctions=6` y terminó en rollback. El postcheck read-only confirmó `app_api` runtime, catálogo vigente válido y cero sesiones. No hubo cambio persistente. Siguiente acción mínima: solicitar autorización separada para aplicar `20260831000100`; solo después ejecutar el auditor global post-zonas y la E2E real de RBAC/idempotencia.
- Subagentes: ninguno; razonamiento alto por esquema compartido y ciclo de vida del rol. No hubo interacción Git remota ni cambios offline; solo se ejecutó la migración dentro de una transacción remota con rollback, sin cambio persistente del esquema.

## Continuación 2026-08-31 — apply de zonas e integración FE-0.1

- Aplicación persistente: Emmanuel autorizó `20260831000100` en `zwbyiefqeujstyzysydn`. El staging temporal contenía ocho migraciones con hashes idénticos; el historial previo mostró siete y el dry-run anunció exclusivamente `20260831000100`, sin seeds ni roles. `db push` aplicó solo esa versión y el postcheck confirmó las ocho versiones exactas.
- Auditoría remota: el SQL global post-zonas pasó directamente y `db lint` no encontró errores en `app`, `app_private` o `app_rls`. El runner oficial detectó una incompatibilidad local: `readAppApiLifecycleTarget` todavía trataba la nueva capacidad como extra. Se añadió selección de allowlist por perfil, manteniendo el perfil anterior estricto; API lint/typecheck/127 pruebas/build pasaron y el runner oficial confirmó `state=runtime`, `catalogAudit=true`, cero sesiones y `status=ok`.
- FE-0.1: Claude terminó el alcance asignado en `apps/web/**`: consumo del directorio Nest, selector exacto Restaurant/Branch, preferencia HTTP-only no autoritativa, revalidación al recuperar foco y bloqueo ante revocación. El integrador revisó el diff y añadió rechazo de respuestas 200 ligadas a otro scope y de roles duplicados. Web pasó lint/typecheck/36 pruebas/build.
- Verificación integrada: pipeline global verde — lint 6/6, typecheck 9/9, tests 9/9 y build 6/6. CodeGraph quedó actualizado en 151 archivos, 2,764 nodos y 10,630 relaciones; `authorizeBranch` afecta 11 símbolos y `listMemberships` ocho, contenidos en las fronteras web/API y sus pruebas. El smoke de navegador confirmó `/login` sin errores de consola en escritorio y 390×844; el selector protegido no se verificó visualmente por falta de una sesión real local.
- Estado: zonas y FE-0.1 permanecen IN_PROGRESS. Falta autorización separada para una E2E remota que cree y limpie fixtures/usuarios y demuestre manager 201/replay, viewer 403, false pair, revocación con token vivo, conflicto 409 y cero efectos rechazados; también falta smoke protegido de FE-0.1. No hubo push/merge/rebase ni cambios offline.
- Agentes: ningún subagente Codex. Claude trabajó en paralelo exclusivamente en `apps/web/**`; nivel de razonamiento no informado. Codex integró y verificó con razonamiento alto por esquema/autorización.

## Continuación 2026-08-31 — E2E remota de zonas

- Estado: la aceptación del primer slice de `POST /api/v1/dining/zones` queda en REVIEW; la tarea amplia de mesas/zonas/layout continúa IN_PROGRESS por mesas, editor y UI. El runner post-zonas reutiliza el grafo 2×2 y los dos usuarios Auth marcados del verificador de tenancy, selecciona el auditor runtime de siete tablas/seis funciones y añade una etapa allowlisted `dining_zones`.
- Cobertura remota: el run exitoso `b4002a5a-10f7-4c46-a669-25d483dbd8b3` completó 119 checks contra `zwbyiefqeujstyzysydn`: anónimo 401 sin efecto; manager 201; replay exacto 201 con el mismo `createdAt`; binding divergente 409; viewer, false pair y membresía revocada con el token todavía vivo 403; tras cada rechazo el conteo administrativo conservó exactamente una zona y un evento válidos. Data API y `app_api` siguieron sin lectura directa de las dos tablas.
- Cleanup/recuperación: un primer run real (`40f2af60-3256-4a71-8f2c-858cb6aaa262`) expuso que el helper nuevo intentaba borrar la auditoría por `id` en vez de `event_id`; la transacción completa se revirtió y no hubo limpieza parcial. El recovery se amplió para validar zona/auditoría marcadas y prefijos legítimos de revocación/deshabilitación, eliminó exactamente 17 filas y dos usuarios y confirmó cero residuos. Tras corregir el helper, el run exitoso limpió en `finally`; una repetición idempotente del recovery devolvió 0 filas/0 usuarios. El postcheck final confirmó `app_api` runtime, catálogo válido y cero sesiones tras el drenaje natural del pooler.
- Código/documentación: se añadieron el runner `run-dining-zones-tenancy-verification.ts`, el script `verify:dining-zones:remote`, marcadores de zona y el perfil opcional del arnés. `tenancy-fixture-recovery.ts` ahora detecta ambas tablas con `to_regclass`, valida ownership/scope/nombre/actor/operación y borra auditoría antes de zona y tenancy. Se actualizaron TODO, PROJECT_NOTES, el runbook de tenancy y el README de migraciones.
- Verificación local: API lint, typecheck, 129 pruebas y build pasaron. Pipeline global verde: lint 6/6, typecheck 9/9, tests 9/9 y build 6/6. CodeGraph quedó completo y actualizado con 152 archivos, 2,790 nodos y 10,802 relaciones. `runTenancyVerification` afecta solo sus runners base/post-zonas; `recoverTenancyFixtures` afecta solo ejecutor, runner y prueba. `git diff --check` pasó; no quedaron `.test-dist` ni archivos privados nuevos versionados.
- Riesgo operativo cerrado el 2026-09-01: durante el diagnóstico del primer rechazo de configuración, una contraseña de `app_api` apareció codificada dentro de la salida local de la herramienta. No se escribió en el repositorio ni se envió a Git/Supabase como log de aplicación; la rotación fail-closed autorizada reemplazó esa credencial y el postcheck confirmó el estado runtime seguro.

## Continuación 2026-09-01 — rotación fail-closed de `app_api`

- Estado: la autorización explícita para rotar `app_api` en `zwbyiefqeujstyzysydn` fue consumida y la exposición local queda cerrada. El rol terminó en `LOGIN`, SCRAM, vigencia infinita, catálogo post-zonas válido y cero sesiones activas.
- Implementación: recovery y provisioning aceptan ahora un perfil explícito `post_dining_zones_v1` con hashes separados para los auditores precheck/runtime; se añadieron runners remotos dedicados sin relajar el perfil histórico. El recovery propaga el perfil también a cada relectura del OID/fingerprint.
- Ejecución remota: dos intentos iniciales fueron rechazados en configuración antes de conectar (separador de argumentos extra y nombres de variables locales asumidos incorrectamente). Con las claves reales del archivo local, recovery devolvió `appApiLogin=false`, `credentialRemoved=true` y `sessionsTerminated=true`. Después se generó una credencial aleatoria nueva, se actualizó únicamente `APP_API_PROVISIONING_PASSWORD` en `.env.adr010.local` y el reprovisionamiento devolvió `appApiLogin=true`, `crashExpiryProtected=true` y `runtimeAudit=true`.
- Verificación: API lint y typecheck pasaron; la suite API completa quedó verde con 131 pruebas, incluidas regresiones para ambos perfiles pinneados. El postcheck remoto read-only devolvió `state=runtime`, `catalogAudit=true` y `activeSessions=false`. CodeGraph quedó actualizado con 154 archivos, 2,813 nodos y 10,859 relaciones; `provisionAppApi` tiene como consumidores solo sus dos runners y pruebas, y `recoverAppApi` solo sus dos runners y pruebas.
- Seguridad/archivos: la contraseña nueva no se imprimió, no se incorporó a archivos versionables y no se modificó ninguna otra configuración secreta. `.env.adr010.local.rotation.tmp` fue transitorio y se eliminó. No hubo cambios de esquema, fixtures, usuarios Auth, interacción Git remota ni subagentes.
- Siguiente acción mínima: continuar mesas/layout/UI y ejecutar el smoke protegido de FE-0.1 con Auth/API reales; no repetir recovery/provisioning sin una autorización humana nueva.

## Continuación 2026-09-01 — preparación del smoke protegido FE-0.1

- Estado: FE-0.1 permanece IN_PROGRESS porque no se crearon usuarios ni fixtures remotos en este corte. Quedó preparado el runner `verify:web-protected-smoke:remote`; la ejecución contra `zwbyiefqeujstyzysydn` requiere autorización humana separada.
- Implementación: `runTenancyVerification` admite un puerto API fijo y hooks opcionales con un único fixture vivo, sin cambiar el comportamiento de los runners base/zonas. `web-protected-smoke-coordinator.ts` publica una lease temporal solo durante selección, espera ack exacto, elimina credenciales para la fase revocada y falla cerrado por timeout, artefactos previos, fase/runId incorrectos o JSON hostil. El runner usa el auditor runtime post-zonas y conserva el cleanup probado en `finally`.
- Verificación local: API lint, typecheck, 135 pruebas y build pasaron; se añadieron tres pruebas del coordinador y una prueba estática del runner. Web pasó lint, typecheck, sus 36 pruebas y build Next. En navegador real, `/login` se revisó a 1440×900 y 390×844, con labels/controles presentes y cero errores o warnings de consola. No se transmitieron credenciales ni se ejecutó login remoto.
- Seguridad/operación: los archivos `.web-protected-smoke-*.tmp` están cubiertos por `.gitignore`, no quedaron residuos locales y nunca se imprimen email/password/token. Una interrupción conserva el `runId` público para `recover:tenancy:remote`; después de recuperar cero fixtures deben eliminarse solo esos tres temporales.
- CodeGraph: índice actualizado con 157 archivos, 2,861 nodos y 10,996 relaciones. `runTenancyVerification` conserva como consumidores únicamente los runners base, zonas y smoke web; el coordinador nuevo solo lo consumen su runner y pruebas. Subagentes: ninguno; el trabajo de Claude visible en `apps/web/**` se preservó.
- Siguiente acción mínima: con autorización nueva, ejecutar el runner, iniciar `apps/web` local contra Nest `127.0.0.1:4311`, validar login/selector/no-store/refresh/logout y bloqueo tras revocación, confirmar ambas fases y comprobar cleanup/postcheck cero.
- Agentes: ningún subagente Codex; Claude ya había terminado y no escribió durante esta unidad. Razonamiento alto por Auth, RBAC, esquema compartido, recuperación destructiva exacta y operación remota. No hubo cambios offline ni interacción Git remota.

## Continuación 2026-09-01 — cierre remoto de FE-0.1

- Estado: FE-0.1 pasa de IN_PROGRESS a REVIEW. Emmanuel autorizó el smoke contra `zwbyiefqeujstyzysydn`; el run final `694b7e52-1e64-4c95-b509-a5215dfed425` terminó `status=ok`, `checks=119`, `diningZonesVerified=true`, `fixtureRowsRemoved=true` y `fixtureUsersRemoved=2`.
- Evidencia de navegador: login inválido devolvió el mensaje estable; login válido abrió el directorio de dos membresías; se seleccionó exactamente la Branch `manager/waiter`; una preferencia de un fixture anterior fue rechazada como obsoleta; recarga mantuvo el contexto sin ciclo; después de revocar la membresía con token vivo apareció “Sin sucursales asignadas”. No hubo warnings ni errores de consola durante el flujo exitoso.
- Correcciones descubiertas: `run-web-protected-smoke.ts` omitía `verifyDiningZones: true` pese a cargar el auditor post-zonas y fallaba antes de red; se añadió el flag y una regresión contractual. Un logout global durante la pausa revocada invalidó la sesión paralela del harness y produjo 401 donde debía demostrar 403; el logout productivo ahora usa `scope: local`. El proxy permite que un Server Action protegido continúe después de limpiar una sesión rechazada, para que el action emita su redirect en el protocolo de Next en vez de recibir HTML inesperado.
- Intentos fallidos y recuperación: `51472446-ddad-4798-a6f0-ba2d0a70d695` expiró esperando confirmación y limpió; `d9f3e8bc-9dcc-4237-b1fb-97776e3807b2` validó toda la UI pero falló tras el logout global y limpió; `38cd5c01-e18a-4605-9f44-8915263a397b` se detuvo en auditoría antes de fixtures por una sesión pooler retenida. El postcheck posterior confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false` antes del run final. No se necesitó recovery manual.
- Verificación: pipeline global verde (`lint` 6/6, `typecheck` 9/9, `test` 9/9 y `build` 6/6); API conserva 135 pruebas y web 37. El postcheck remoto final devolvió `state=runtime`, `catalogAudit=true` y `activeSessions=false`. CodeGraph quedó actualizado con 157 archivos, 2,861 nodos y 11,001 relaciones: `runTenancyVerification` solo tiene los tres runners previstos, `logoutAction` solo el layout protegido y `selectBranchAction` solo su página. `git diff --check` quedó verde. Los tres temporales de coordinación y la copia web aislada fueron eliminados; el puerto 3010 quedó cerrado.
- Avance estimado: 32.9% del roadmap completo (33% redondeado) y 85.3% de Fase 0, usando una métrica explícita de estados (`DONE=100%`, `REVIEW=90%`, `IN_PROGRESS=50%`, demás=0%). Después de esta transición hay 68 tareas: 3 DONE, 21 REVIEW, 1 IN_PROGRESS, 42 TODO y 1 BLOCKED. Es una estimación de implementación, no una declaración de producto vendible.
- Siguiente acción mínima para otra conversación: leer los archivos operativos, confirmar el commit/push de este corte y continuar `Implementar mesas, zonas y editor de layout web` desde su único pendiente: contratos/esquema/API/UI de mesas y editor de layout. Antes de cualquier migración nueva, consultar CodeGraph, marcar el slice IN_PROGRESS, preparar rollback-only y solicitar autorización remota separada. No repetir migraciones, rotación/provisioning, exposición Data API ni E2E FE-0.1.
- Agentes/Git: Claude trabajó FE-0.1 previamente en `apps/web/**` y terminó antes de esta integración; Codex no usó subagentes. No hubo cambios offline. Emmanuel autorizó commit y push de los 42 archivos; el corte se publica en `main` por fast-forward con el mensaje `feat(pos): complete dining zones and branch context`, sin merge, rebase ni force-push.

## Continuación 2026-09-01 — mesas y editor de layout local

- Estado: P1 mesas/zonas/layout permanece IN_PROGRESS. Zonas, FE-0.1, Auth/API, RBAC y el esquema remoto previo no se reabrieron ni se repitieron. El corte nuevo implementa fuentes locales de contratos, esquema, API y UI para mesas/layout; no declara el esquema desplegado.
- Contratos/API: `DINING_LAYOUT_SCHEMA_VERSION=1` define mesa, forma, geometría, plano, alta y actualización optimista con parsers exactos/fail-closed. Nest añade `GET /api/v1/dining/layout`, `POST /api/v1/dining/tables` y `PATCH /api/v1/dining/tables/layout`, separados por `tables.read`/`tables.manage`, con 400/403/409/503 estables y `private, no-store`.
- Esquema/rollback: la migración aditiva local `20260901000100` modela `dining_tables`, auditoría inmutable, soft delete preparado, geometría entera 24×100, snapshots de replay y tres funciones `SECURITY DEFINER` accesibles únicamente a `app_api`. La auditoría específica y `verify:dining-tables-layout-schema:rollback` quedaron preparados. El entrypoint sin configuración rechazó con `SCHEMA_VERIFICATION_CONFIGURATION_REJECTED`; no hubo conexión, SQL remoto, migración, fixture ni cambio de `app_api`/Data API. Rollback persistente está documentado como migración forward-only, condicionado a vaciedad/respaldo/dependencias.
- UI: `/app` consume el plano exclusivamente por Nest, muestra zonas y mesas en cuadrícula responsive, permite alta y edición de coordenadas/tamaño mediante Server Actions, usa IDs/eventos/idempotencia nuevos y un `deviceId` HTTP-only local. Revalida sesión, directorio y Branch antes de cada mutación; roles sin `tables.manage` conservan vista read-only.
- Verificación local: la compuerta global pasó lint 6/6, typecheck 9/9, tests 9/9 y build 6/6. El build final corrió fuera del sandbox después de reproducir el bloqueo esperado `spawn EPERM`; Next generó correctamente `/app`. Las pruebas nuevas cubren parser, autorización, adapter, HTTP y cliente web. El navegador real confirmó `/login` sin errores/warnings en el viewport normal y 390×844; el editor protegido no pudo renderizarse visualmente sin una sesión/fixture real, que no estaba autorizado crear. CodeGraph posterior quedó actualizado en 163 archivos, 2,987 nodos y 8,302 relaciones; `DiningTableService` alcanza módulo/controlador/pruebas y las acciones web alcanzan la página y cliente Nest, sin objetos huérfanos detectados. `git diff --check` pasó salvo avisos de conversión LF/CRLF.
- Brechas/siguiente acción mínima: revisar SQL en PostgreSQL real mediante el verificador rollback-only con autorización expresa; después actualizar el auditor global post-mesas pinneado, hacer smoke protegido real en desktop/móvil y solicitar otra autorización separada para apply/E2E. Hasta entonces no marcar REVIEW ni ejecutar la migración.
- Agentes: ninguno; razonamiento alto por esquema compartido, tenancy, autorización e idempotencia. No hubo cambios offline, interacción Git remota, commit, push, merge ni rebase.

## Continuación 2026-09-01 — intento rollback-only autorizado

- Estado: P1 permanece IN_PROGRESS. Emmanuel autorizó únicamente `verify:dining-tables-layout-schema:rollback`; no autorizó apply, fixtures, provisioning, Data API ni E2E.
- Resultado: el primer intento rechazó en configuración porque la CA ya no estaba en la ruta histórica del repositorio. Se localizó la misma `Supabase Root 2021 CA` validada en `Downloads`; los intentos posteriores no alcanzaron `BEGIN` y terminaron en `SCHEMA_VERIFICATION_CONNECT_FAILED`.
- Diagnóstico seguro: DNS ubicó el endpoint directo en AWS `ca-central-1`; el puerto 5432 del pooler oficial está accesible. La cadena pública contiene leaf `*.pooler.supabase.com`, intermedia `Supabase Intermediate 2021 CA` y raíz `Supabase Root 2021 CA`; su huella raíz coincide con el certificado local. Con la URL normalizada igual que el runner, PostgreSQL respondió solamente `28P01` para los usuarios `postgres.<ref>` y `app_api.<ref>`. No se imprimieron URL, contraseña ni errores nativos con datos sensibles.
- Esquema/rollback: no se autenticó PostgreSQL, no inició ninguna transacción, no ejecutó migración/auditoría/fixture y no hubo cambio remoto persistente. La migración local `20260901000100` sigue sin validación PostgreSQL real ni aplicación.
- Siguiente acción mínima: obtener o guardar localmente una credencial administrativa vigente de `zwbyiefqeujstyzysydn` y repetir exactamente el rollback-only ya autorizado; después actualizar el auditor global post-mesas. Apply y E2E requieren autorizaciones separadas.
- Agentes: ninguno; razonamiento alto. No hubo cambios offline ni interacción Git remota.

## Continuación 2026-09-01 — rollback-only de mesas/layout completado

- Estado: P1 permanece IN_PROGRESS. Tras propagarse la nueva contraseña administrativa guardada únicamente en el archivo ignorado, la autenticación TLS al pooler oficial `ca-central-1` pasó.
- Verificación autorizada: `verify:dining-tables-layout-schema:rollback` ejecutó `20260901000100` y su auditoría dentro de una transacción, obtuvo `status=ok`, `policies=5`, `securedTables=9` y `securityDefinerFunctions=9`, y finalizó con rollback.
- Garantía posterior: `verify:post-dining-zones-app-api-state:remote` fue solo lectura y confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`. Esto demuestra que el catálogo persistente siguió exactamente en post-zonas y que no quedaron sesiones de `app_api`.
- Remoto: no se aplicó ninguna migración, no se crearon fixtures, no se rotó/provisionó `app_api`, no se modificó Data API y no se ejecutó E2E. La autorización rollback-only queda consumida.
- Siguiente acción mínima: preparar localmente el auditor global post-mesas pinneado y su prueba; después solicitar autorizaciones separadas para apply y para smoke/E2E protegido.
- Agentes: ninguno; razonamiento alto. No hubo cambios offline ni interacción Git remota.

## Continuación 2026-09-01 — auditor global post-mesas

- Estado: P1 permanece IN_PROGRESS. Se añadió el perfil local `post_dining_tables_v1` sin modificar los perfiles históricos post-membresías/post-zonas.
- Auditoría: `tenancy_memberships_post_dining_tables_catalog.sql` exige exactamente nueve tablas RLS+FORCE RLS, cinco políticas, nueve funciones `SECURITY DEFINER`, owners/search paths exactos, grants cliente/server-only y ausencia de capacidades extra de `app_api`. El SQL queda pinneado por SHA-256 y se usa tanto para estado `NOLOGIN` como runtime porque el lifecycle verifier clasifica por separado contraseña/login.
- Integración: `readAppApiLifecycleTarget` permite exactamente seis funciones privadas post-mesas; el nuevo runner `verify:post-dining-tables-app-api-state:remote` es solo lectura. Una regresión cubre estados disabled/runtime, fingerprint y auditor pinneado; schema-contract cubre la superficie global.
- Verificación: API pasó lint, typecheck y toda la suite. El runner rollback-only ejecutó migración + auditoría específica + auditoría global en PostgreSQL real y devolvió `policies=5`, `securedTables=9`, `securityDefinerFunctions=9`; el postcheck final volvió a confirmar catálogo post-zonas intacto, `app_api` runtime y cero sesiones.
- Remoto: no hubo apply, fixtures, provisioning, Data API ni E2E. Siguiente acción mínima: autorización inequívoca para aplicar solo `20260901000100`; después ejecutar el nuevo postcheck global y preparar/autorizar por separado el smoke/E2E protegido.
- Agentes: ninguno; razonamiento alto. No hubo cambios offline ni interacción Git remota.

## Continuación 2026-09-01 — apply `20260901000100` y corrección preparada

- Aplicación autorizada: staging temporal con las cinco migraciones ADR-010 y cuatro productivas, todas hash-idénticas. El historial previo contenía ocho versiones exactas; el dry-run anunció únicamente `20260901000100_create_dining_tables_layout.sql` y `db push` aplicó solo esa versión, sin seeds, roles, reparación de historial, fixtures ni configuración.
- Postcheck: el historial remoto contiene nueve versiones exactas. `verify:post-dining-tables-app-api-state:remote` devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`.
- Hallazgo: `db lint --level error` detectó SQLSTATE `42702` en `create_dining_table` y `update_dining_table_layout`: las consultas de replay comparan columnas sin alias contra parámetros PL/pgSQL y son ambiguas. El CLI terminó con código 0 pero reportó dos issues nivel error; se trató como fallo de entrega.
- Corrección local: se añadió `20260901000200_qualify_dining_table_function_columns.sql`, sin editar `00100`. Reemplaza solo las dos funciones con firmas idénticas, aliases explícitos, owner `postgres`, `SECURITY DEFINER` y `search_path=''`; el verificador rollback-only de `00200` está preparado pero no ejecutado. API pasó lint, typecheck y toda la suite, incluida la regresión del contrato SQL.
- Estado: P1 permanece IN_PROGRESS. El esquema post-mesas existe remotamente, pero las mutaciones no deben considerarse verificadas hasta validar/aplicar `00200`. No se ejecutó E2E ni se crearon fixtures.
- Siguiente acción mínima: autorización expresa para ejecutar únicamente `verify:dining-tables-layout-lint-fix-schema:rollback` contra `zwbyiefqeujstyzysydn`. Aplicar `00200` requerirá una autorización posterior separada.
- Agentes: ninguno; razonamiento alto. No hubo cambios offline ni interacción Git remota distinta del `db push` autorizado.

## Continuación 2026-09-01 — rollback-only de `20260901000200`

- Autorización consumida: se ejecutó únicamente `verify:dining-tables-layout-lint-fix-schema:rollback` contra `zwbyiefqeujstyzysydn`; no hubo apply, fixtures ni E2E.
- Resultado: `20260901000200` compiló las dos funciones corregidas dentro de la transacción, pasó el auditor global con `policies=5`, `securedTables=9`, `securityDefinerFunctions=9` y terminó con rollback.
- Garantía posterior: el auditor read-only confirmó `app_api` runtime, catálogo post-mesas válido y cero sesiones. El historial remoto contiene exactamente nueve versiones y termina en `20260901000100`; `00200` no está aplicada.
- Limitación: `db lint` analiza mediante una conexión independiente y no puede ver el reemplazo no confirmado. La regresión local pinnea aliases explícitos; tras aplicar `00200` será obligatorio ejecutar inmediatamente `db lint --level error` y no avanzar a E2E si reporta issues.
- Estado/siguiente acción: P1 permanece IN_PROGRESS. Requiere autorización expresa separada para aplicar únicamente `20260901000200`; después historial, auditor global y `db lint` deben quedar verdes antes de autorizar fixtures/E2E.
- Agentes: ninguno; razonamiento alto. No hubo cambios offline ni interacción Git remota.

## Continuación 2026-09-01 — apply `20260901000200`

- Aplicación autorizada: staging temporal de diez migraciones hash-idénticas; historial previo de nueve versiones y dry-run anunciando únicamente `20260901000200_qualify_dining_table_function_columns.sql`. `db push` aplicó solo esa versión, sin seeds, roles, reparación, fixtures ni configuración.
- Postchecks: historial remoto con diez versiones exactas; auditor global `post_dining_tables_v1` con `state=runtime`, `catalogAudit=true`, `activeSessions=false`; `db lint --level error` sobre `app`, `app_private`, `app_rls` devolvió `No schema errors found` y lista de resultados vacía.
- Estado: el defecto `42702` queda corregido. P1 permanece IN_PROGRESS únicamente porque falta smoke/E2E protegido real del editor; esa ejecución y sus fixtures no estaban autorizados por este permiso.
- Siguiente acción mínima: preparar el runner E2E específico de mesas/layout y solicitar autorización separada para crear/eliminar sus fixtures y ejecutar el smoke visual protegido en desktop/móvil.

## Continuación 2026-09-01 — preparación E2E mesas/layout

- Tarea/estado: P1 continúa `IN_PROGRESS`; no se ejecutaron fixtures ni E2E remotas en esta continuación.
- Implementación: `tenancy-verification` incorpora perfil `verifyDiningTables`, runner dedicado post-mesas y checks HTTP/PostgreSQL para 401, creación manager, replay exacto, conflicto divergente, viewer/false-pair 403, lectura manager/viewer, actualización optimista, replay, versión obsoleta y revocación con el mismo token. También prueba denegación directa para `dining_tables` y `dining_table_audit_events` por Data API y `app_api`.
- Recuperación/cleanup: los marcadores incluyen mesas; el recovery valida exclusivamente una mesa `dining-table-created` vinculada a la zona marcada y uno o dos eventos legítimos (`created`, `layout_updated`). El borrado conserva el orden FK auditorías de mesa → mesas → auditorías de zona → zonas y exige propiedad exacta antes de borrar.
- Verificación local: pipeline global verde — lint 6/6, typecheck 9/9, tests 9/9 y build 6/6—, incluida la nueva regresión de recovery y contrato del runner; `git diff --check` también pasó. CodeGraph quedó actualizado en 166 archivos, 3,027 nodos y 8,518 relaciones; el impacto del recovery queda limitado a su almacén PostgreSQL y orquestador, y el del runner a su implementación/entrypoints operativos.
- Migración/rollback: sin cambios nuevos y sin ejecución remota; `20260901000100`/`20260901000200` permanecen aplicadas. Offline: sin cambios. Git remoto: sin interacción. Subagentes: ninguno; razonamiento alto en esquema/seguridad.
- Siguiente acción mínima: con autorización expresa, ejecutar una sola vez `verify:dining-tables:remote` contra `zwbyiefqeujstyzysydn`, confirmar cleanup total y auditor global; después realizar el smoke visual protegido del editor en desktop/móvil.

## Continuación 2026-09-01 — autorización E2E mesas/layout

- Emmanuel autorizó una sola ejecución remota con fixtures temporales en `zwbyiefqeujstyzysydn`.
- El preflight local no imprimió secretos y confirmó que `.env.adr010.local` conserva únicamente `ADR010_DATABASE_PASSWORD`; las demás variables deben ensamblarse en memoria desde fuentes autorizadas.
- La primera consulta read-only necesaria fue rechazada por el control de permisos de Codex debido al límite de uso de la sesión hasta las 18:09. El proceso no salió a red: no consultó Supabase, no creó usuarios/filas y no inició el runner. La autorización permanece sin consumir.
- Estado: P1 sigue `IN_PROGRESS`, sin cambio remoto. Siguiente acción mínima: después del restablecimiento de uso, repetir el preflight seguro y ejecutar una sola vez la E2E autorizada.

## Continuación 2026-09-01 — rotación `app_api` post-mesas

- Credenciales locales: `.env.adr010.local` contiene contraseña administrativa y `APP_API_DATABASE_PASSWORD`; ambas pasan presencia, longitud, separación y ausencia de espacios sin imprimir valores. La contraseña administrativa autenticó como `postgres`; la nueva no era todavía la credencial vigente de `app_api`.
- Alcance autorizado: rotar únicamente `app_api` en `zwbyiefqeujstyzysydn`, sin migraciones ni Data API. Antes de mutar se añadió el perfil pinneado `post_dining_tables_v1` al aprovisionador y un entrypoint específico.
- Resultado remoto: dos intentos alcanzaron la etapa de conexión y devolvieron `APP_API_PROVISIONING_CONNECTION_FAILED`; en ambos el flujo compensó automáticamente. Los postchecks read-only confirmaron `state=safe_disabled`, `catalogAudit=true`, `activeSessions=false`. No se ejecutaron fixtures ni la E2E.
- Endurecimiento local posterior: la validación del pooler ahora abre hasta tres sesiones nuevas, con espera acotada de 1 s/2 s para propagación de credencial; al agotarse mantiene la compensación existente. `apps/api` lint, typecheck, suite completa (17/17 pruebas de aprovisionamiento) y build pasaron.
- CodeGraph: actualizado en 167 archivos, 3,037 nodos y 8,478 relaciones; el impacto de `provisionAppApi` permanece contenido en el módulo lifecycle y sus entrypoints.
- Estado/siguiente acción: P1 permanece `IN_PROGRESS`; `app_api` está deliberadamente deshabilitado. Requiere autorización nueva para ejecutar una sola vez el aprovisionador corregido; solo si queda runtime/verde se consumirá después la autorización de E2E de mesas/layout.

## Cierre de conversación 2026-09-01 — P1 mesas/layout

- Rotación final: Emmanuel autorizó un único intento del aprovisionador corregido. Terminó con `appApiLogin=true`, `crashExpiryProtected=true`, `runtimeAudit=true`; el postcheck independiente confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false`.
- E2E: la ejecución de mesas/layout previamente autorizada se rechazó en `configuration` antes de emitir `runId`, crear usuarios o insertar filas. Causa confirmada: Management API/CLI devuelve la clave `sb_secret_` enmascarada; `api` y `DATABASE_URL` pasaban configuración, `admin` no. No hubo fixtures ni cleanup pendiente.
- Secreto local: Emmanuel copió después la clave secreta moderna completa a `.env.adr010.local` como `TENANCY_VERIFICATION_SUPABASE_SECRET_KEY`. Validación local: prefijo/formato/límite válidos; el archivo está ignorado y no se versiona. Nunca se imprimió el valor.
- Instrucción humana de cierre: no continuar la E2E en esta conversación; hacer commit, push y dejar el siguiente paso para otra conversación.
- Siguiente conversación: partir del commit publicado, leer los archivos operativos una sola vez, verificar árbol/CodeGraph y confirmar `app_api` read-only en runtime. Cargar desde `.env.adr010.local` las dos contraseñas y la secret key real; obtener solo la publishable key por CLI. Ejecutar una sola vez `apps/api/dist/operations/run-dining-tables-tenancy-verification.js` con los opt-ins exactos contra `zwbyiefqeujstyzysydn`. Exigir `diningTablesVerified=true`, `diningZonesVerified=true`, `fixtureRowsRemoved=true`, `fixtureUsersRemoved=2`; después confirmar catálogo/runtime/cero sesiones. Si falla tras emitir `runId`, usar únicamente el recovery run-scoped antes de cualquier repetición. No repetir migraciones, Data API ni provisioning.
- Después de E2E verde: ejecutar el smoke visual protegido del editor en desktop/móvil, revisar consola/red y mover P1 a `REVIEW`; no marcar `DONE` sin aprobación humana.
- Verificación local final antes del commit: `pnpm lint`, `pnpm typecheck`, `pnpm test` y `pnpm build` terminaron en verde; `git diff --check` no reportó errores. CodeGraph quedó sincronizado en 167 archivos, 3,037 nodos y 8,478 relaciones.
- Agentes: ninguno; razonamiento alto. No hubo cambios offline ni interacción Git remota distinta del `db push` autorizado.

## Continuación 2026-09-02 — bloqueo de credencial `app_api` antes de E2E mesas/layout

- Punto de partida: `HEAD` y `origin/main` coincidían en `80bb9bf2fc0d3cbb5dc0730ffa58ed1ecac8e5e6`; árbol limpio y sin interacción Git remota. CodeGraph estaba falsamente marcado al día en 157 archivos, se sincronizó localmente y quedó completo en 167 archivos, 3,043 nodos y 11,956 relaciones. `DiningTableService` alcanza módulo/controlador/pruebas; el contrato de layout alcanza API, runner y web sin objetos huérfanos detectados.
- Precheck: después de corregir la carga local de comillas y actualizar la contraseña administrativa, el verificador post-mesas devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false`. Supabase CLI identificó exactamente una clave moderna `publishable` sin `--reveal`; el secreto moderno permaneció únicamente en `.env.adr010.local` ignorado y nunca se imprimieron valores.
- Bloqueo inicial: `APP_API_PROVISIONING_PASSWORD` no autenticó por separado como `app_api` (`28P01`). Emmanuel autorizó expresamente rotar solo esa credencial y después autorizó probar el pooler transaccional 6543; no autorizó ni se realizaron migraciones, Data API, otras rotaciones, fixtures ajenos, push o provisioning adicional.
- Rotación: el aprovisionador post-mesas mantuvo candado, auditorías pinneadas, cero sesiones, SCRAM y compensación. Hubo cuatro intentos autorizados: runner estándar 5432; 5432 con esperas 10/20 s; 5432 con esperas 60/120 s tras consultar documentación oficial de Supavisor; y 6543 con esperas 10/20 s. Todos terminaron `APP_API_PROVISIONING_CONNECTION_FAILED` en `connection` y compensaron. La conexión directa `db.zwbyiefqeujstyzysydn.supabase.co:5432` no resolvió DNS desde este host (`ENOTFOUND`).
- Estado remoto final: postcheck read-only `state=safe_disabled`, `catalogAudit=true`, `activeSessions=false`. La E2E de mesas/layout nunca se inició, no emitió `runId`, no creó usuarios ni filas y su autorización de una sola ejecución sigue sin consumir. No corresponde recovery run-scoped.
- Verificación local: se compiló `apps/api` para regenerar `dist` ignorado; no hubo cambios de fuente. CodeGraph continúa actualizado en 167 archivos, 3,043 nodos y 11,956 relaciones. No se ejecutó pipeline global ni smoke visual porque el prerrequisito remoto quedó bloqueado. Git seguía limpio antes de estas actualizaciones documentales.
- Estado/siguiente acción: P1 pasa de `IN_PROGRESS` a `BLOCKED`. Resolver la autenticación de roles personalizados en Supavisor o habilitar una ruta PostgreSQL directa/IPv4 autorizada; después restaurar `app_api` a `runtime`, confirmar catálogo/cero sesiones, ejecutar la E2E una sola vez y solo si queda verde realizar smoke desktop/móvil. Subagentes: ninguno; razonamiento alto. Sin cambios de esquema, offline o Git remoto.

## Continuación 2026-09-02 — restauración, E2E única y corrección del runner de mesas

- Corrección del diagnóstico anterior: Emmanuel sí había actualizado correctamente las tres credenciales. Los reintentos consecutivos mantuvieron abierto temporalmente el circuit breaker de Supavisor. Tras 135 segundos sin conexiones PostgreSQL, la contraseña administrativa autenticó; la clave secreta moderna respondió HTTP 200 en Auth Admin sin imprimir valores ni datos de usuario. El único dato obtenido por Supabase CLI fue la clave pública `publishable` requerida.
- Restauración autorizada: el precheck partió de `safe_disabled`, catálogo íntegro y cero sesiones. El aprovisionador auditado usó conexiones frescas con una espera acotada adicional y devolvió `appApiLogin=true`, `crashExpiryProtected=true`, `runtimeAudit=true`. Un postcheck fresco confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false`.
- Preparación local: la primera invocación no importó el runner porque `dist` de `shared-types` estaba obsoleto; no arrancó el runner ni emitió `runId`. Se regeneraron `shared-types` y API desde fuentes y se validó el import. Esta falla de arranque no consumió la ejecución E2E.
- E2E autorizada consumida: `run-dining-tables-tenancy-verification.js` se ejecutó una sola vez con los opt-ins exactos y emitió `runId=eb22bd0c-7b1a-4245-aae8-b353fb771c93`. Terminó `TENANCY_VERIFICATION_DINING_TABLES_FAILED` en `dining_tables`; por tanto no produjo los cuatro resultados exigidos y no se repitió automáticamente.
- Recuperación y postcheck: se ejecutó exclusivamente el recovery acotado al `runId` anterior. Devolvió `fixtureRowsRemoved=0`, `fixtureUsersRemoved=0`, lo que confirma que el cleanup interno ya había eliminado sus dos usuarios y todas las filas. Después de cerrar la sesión retenida por Supavisor, el postcheck read-only volvió a `runtime`, catálogo íntegro y cero sesiones.
- Causa raíz y cambio: `tenancyFixtureName` reutilizaba el marcador largo de Restaurant/Branch/Zone para mesas. Con UUID y sufijo producía ~73 caracteres; `parseCreateDiningTableCommandV1` limita el nombre a 40, por lo que `diningTableCommand` lanzaba exactamente el error observado antes de crear la primera mesa. Los nombres de mesa ahora son `t_<runId>_<código>`, conservan el UUID completo, miden 40, son únicos entre los cinco casos y siguen compartidos con el recovery. Se añadió regresión en `tenancy-fixture-recovery.test.ts`.
- Verificación local exacta: `@super-restaurant/api` pasó lint, typecheck, suite completa y build. El monorepo pasó `pnpm lint` (6/6), `pnpm typecheck` (9/9), `pnpm test` (9/9) y `pnpm build` (6/6). Única advertencia: el host usa Node 22.20.0 frente al engine 24.19.0 fijado; no hubo fallos. No se ejecutó smoke visual porque su prerrequisito E2E no quedó verde.
- CodeGraph: se resincronizaron los dos archivos cambiados. `tenancyFixtureName` sigue consumido únicamente por el runner, la recuperación y sus pruebas; el código compacto nuevo no tiene consumidores externos y `recoverTenancyFixtures` conserva su enlace de prueba. No quedaron referencias rotas u objetos huérfanos detectados.
- Estado y siguiente acción mínima: P1 permanece `BLOCKED`, no REVIEW. Requiere autorización expresa para ejecutar una segunda y única E2E con el runner corregido; si devuelve `diningTablesVerified=true`, `diningZonesVerified=true`, `fixtureRowsRemoved=true`, `fixtureUsersRemoved=2`, realizar smoke protegido desktop/móvil con consola/red y entonces mover P1 a REVIEW. Sin migraciones, Data API, fixtures ajenos, rotaciones adicionales, push, cambios offline ni interacción Git remota. Subagentes: ninguno; razonamiento alto.

## Continuación 2026-09-02 — segunda E2E autorizada y recuperación

- Autorización: Emmanuel autorizó expresamente la segunda y única E2E. El precheck read-only confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false`.
- Preflight no consumido: la primera invocación fue rechazada en `configuration` antes de `runId`, red del runner o fixtures porque la URL de `app_api` derivada en memoria conservaba el parámetro administrativo `sslrootcert`. Los lectores independientes confirmaron `api=true`, `admin=true`, `database=false`; al conservar únicamente `sslmode=verify-full`, la configuración quedó válida. No se expusieron valores.
- E2E consumida: el runner corregido emitió `runId=4183c6ca-2c98-42b4-be6d-8ae88487090a` y terminó `TENANCY_VERIFICATION_CLEANUP_FAILED`. No hubo otra ejecución. La recuperación exacta para ese UUID devolvió `fixtureRowsRemoved=19`, `fixtureUsersRemoved=2`, `status=ok`.
- Postcheck: el primero encontró una sesión retenida de `app_api`; después de 30 segundos, un segundo postcheck estrictamente read-only confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false`. No quedaron fixtures ni usuarios.
- Diagnóstico: 19 filas corresponden a Restaurant/Branch/membresías/grants, zona+audit y mesa+audit de creación, sin audit de actualización. El fallo funcional ocurrió después de crear la mesa y antes de confirmar su actualización. `cleanupFixtures` exigía tanto `created` como `layout_updated` cuando solo conocía `diningTableCreated`; al fallar, sustituyó el error anterior por `CLEANUP_FAILED`.
- Corrección local: `FixturePlan` incorpora `diningTableUpdated=false`, se activa solo después del PATCH exitoso y el cleanup exige el audit de actualización únicamente en ese estado. La prueba de recuperación cubre ahora explícitamente el prefijo create-only. `@super-restaurant/api` pasó lint, typecheck, toda su suite y build; CodeGraph resincronizó los dos archivos y no detectó dependencias externas nuevas.
- Diagnóstico local posterior: se extrajo `requiredDiningTableAuditEventIds` como regla pura y se añadieron pruebas para cero progreso, create-only, create+update y el estado inválido update-sin-create. El runner específico de mesas emite 16 checkpoints allowlisted antes de cada subpaso, sin URLs, credenciales, tokens, UUID de fixtures ni payloads. Esto permitirá distinguir el error funcional original en una ejecución futura sin que cleanup lo enmascare.
- Verificación final local: el monorepo pasó `pnpm lint` (6/6), `pnpm typecheck` (9/9), `pnpm test` (9/9) y `pnpm build` (6/6); solo apareció el aviso esperado de Node 22.20.0 frente al engine 24.19.0. CodeGraph sincronizó cuatro archivos (dos nuevos y dos modificados) y confirmó el helper, la lista de checkpoints, sus pruebas y los cuatro consumidores de `runTenancyVerification`; no quedaron referencias huérfanas conocidas.
- Estado: no se obtuvieron `diningTablesVerified=true`, `diningZonesVerified=true`, `fixtureRowsRemoved=true` ni el resumen final; no se ejecutó smoke visual y P1 permanece `BLOCKED`. Cualquier tercera E2E requiere autorización nueva y separada después de aislar el checkpoint funcional. Sin migraciones, Data API, rotaciones, provisioning, push, fixtures ajenos, cambios offline ni subagentes; razonamiento alto.

## Continuación 2026-09-02 — tercera E2E diagnóstica autorizada

- Precheck y claves: el precheck read-only devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false`. La CLI de Supabase listó las clases de claves sin `--reveal`; se materializó en memoria únicamente la entrada moderna `publishable`. Las dos contraseñas y la secret key se cargaron desde `.env.adr010.local` sin imprimir valores.
- E2E consumida: el runner emitió `runId=fa6f05fd-1948-40d7-9277-9d6babc275d8`. Pasó los checkpoints de create no autenticado, persistencia vacía, create manager, persistencia creada, replay, conflicto idempotente, rechazo viewer y rechazo false-pair. El último checkpoint fue `dining_tables.manager_layout_read`; no alcanzó `viewer_layout_read` y terminó en cleanup. No hubo repetición.
- Recuperación y postcheck: la recuperación exclusiva de ese UUID devolvió `fixtureRowsRemoved=19`, `fixtureUsersRemoved=2`, `status=ok`. El postcheck estrictamente read-only confirmó después `state=runtime`, `catalogAudit=true`, `activeSessions=false`.
- Hallazgo de cleanup: `verifyDiningTableFixtureOwnership` conservaba una segunda expectativa duplicada de create+update aunque el flujo solo hubiera confirmado create. Ahora usa `requiredDiningTableAuditEventIds`, igual que el borrado, eliminando la divergencia que volvió a ocultar el fallo primario.
- Diagnóstico preparado: `manager_layout_read` quedó subdividido en respuesta recibida, status 200, cache-control, parseo y presencia de la mesa creada, todos checkpoints allowlisted sin datos variables. El smoke Nest local ahora conserva zona+mesa en el doble PostgreSQL y demuestra por HTTP el layout poblado completo.
- Verificación: `@super-restaurant/api` pasó lint, typecheck, suite completa y build; solo apareció el aviso esperado de Node 22.20.0 frente a 24.19.0. CodeGraph se resincronizó y confirmó que la regla pura de progreso sigue compartida por cleanup y pruebas. P1 permanece `BLOCKED`; no hubo smoke visual, migraciones, Data API, rotaciones, provisioning, push, fixtures ajenos, cambios offline ni subagentes.
- Siguiente acción mínima: una cuarta E2E diagnóstica requeriría autorización humana nueva y separada. Si sus checkpoints atraviesan la lectura manager y el resumen cumple los cuatro resultados exigidos, ejecutar entonces el smoke protegido desktop/móvil y mover P1 a REVIEW.

## Continuación 2026-09-02 — hardening diagnóstico antes de otra E2E

- Alcance y evidencia: no se ejecutó otra E2E ni hubo conexión/mutación remota. La ausencia de `dining_tables.manager_layout_response_received` después de `manager_layout_read` en la tercera ejecución demuestra que el GET falló en transporte antes de producir una respuesta HTTP; credenciales, create, replay y persistencia ya habían pasado.
- Diagnóstico seguro: `tenancy-verification-progress.ts` modela las salidas `fetch_failed`, status inesperado, cache inválida, JSON inválido, contrato inválido, scope divergente, zona ausente, mesa ausente, mesa divergente y éxito. Para transporte solo admite seis categorías cerradas y transforma códigos desconocidos, mensajes o proxies hostiles en `other`. El reporte contiene únicamente un status acotado y booleanos tri-state; no incluye URL, token, UUID, cuerpo ni error del proveedor.
- Error primario preservado: `runTenancyVerification` emite un aviso allowlisted `failed_before_cleanup` al capturar el fallo funcional. El runner específico lo registra antes del `finally`; si cleanup falla, la salida final puede seguir ordenando recuperación sin volver a ocultar cuál fue el stage/código original. Los callbacks diagnósticos no pueden cambiar el resultado del verificador.
- Lectura granular: el manager GET comprueba en orden transporte, respuesta, status 200, `private, no-store`, decodificación JSON, contrato V1, scope exacto, zona esperada, mesa esperada y igualdad con la respuesta create. Solo emite un diagnóstico final o el primer fallo, y checkpoints posteriores a cada condición satisfecha. No se modificó SQL, migraciones ni contratos productivos.
- Pruebas y verificación: `@super-restaurant/api` pasó lint, typecheck, suite completa y build. El monorepo pasó `pnpm lint` (6/6), `pnpm typecheck` (9/9), `pnpm test` (9/9) y `pnpm build` (6/6); `git diff --check` pasó. CodeGraph sincronizó cuatro archivos y quedó al día en 169 archivos, 3,066 nodos y 12,076 relaciones. `diagnoseDiningLayoutRead` afecta el baseline de mesas, el harness compartido, sus cuatro entrypoints y la prueba; no aparecieron consumidores productivos ni referencias huérfanas.
- Estado: P1 continúa `BLOCKED`, no REVIEW. No se ejecutó smoke visual porque la E2E aún no está verde. Siguiente acción mínima: autorización humana nueva y explícita para una cuarta y única E2E diagnóstica; no repetir automáticamente. Si produce los cuatro resultados exigidos, ejecutar smoke protegido desktop/móvil, revisar consola/red, actualizar documentos y mover P1 a REVIEW. Sin migraciones, Data API, rotaciones, provisioning, push, fixtures ajenos, cambios offline ni subagentes; razonamiento alto.

## Continuación 2026-09-02 — cuarta E2E diagnóstica y causa SQL confirmada

- Preparación: una primera invocación local se detuvo con `LOCAL_CA_REQUIRED` antes de abrir red o emitir `runId`; no consumió la autorización. Con `.certs/prod-ca-2021.crt`, el precheck estrictamente read-only devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false`. Las tres credenciales se cargaron desde `.env.adr010.local` sin imprimir valores y la CLI de Supabase seleccionó únicamente la clave moderna `publishable`, sin `--reveal`.
- E2E consumida: `runId=09fe6f56-e9c7-47be-a53c-21beec5543ca`. Pasó create no autenticado, persistencia vacía, create manager, persistencia creada, replay, conflicto idempotente y rechazos viewer/false-pair. Alcanzó `manager_layout_response_received`; el diagnóstico allowlisted registró HTTP 503 y terminó `TENANCY_VERIFICATION_ASSERTION_FAILED` en `dining_tables`. No se repitió.
- Recuperación y postcheck: se ejecutó únicamente la recuperación acotada al UUID emitido; devolvió `fixtureRowsRemoved=0`, `fixtureUsersRemoved=0`, confirmando que el cleanup interno ya había eliminado los fixtures. El primer postcheck vio una sesión retenida y omitió correctamente la auditoría quiescente; tras 30 segundos, un nuevo postcheck read-only confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false`.
- Causa demostrada: la consulta productiva era `select layout from app_private.list_dining_layout($1::uuid, $2::uuid, $3::uuid)`. La función devuelve un escalar `jsonb`, no una tabla con columna `layout`; una comparación estrictamente read-only contra PostgreSQL confirmó código `42703` para la forma vigente y una fila `{ layout: null }` para `select app_private.list_dining_layout(...) as layout`. Por ello el adapter lanzaba y Nest respondía 503; no era un problema de contraseñas, Auth, catálogo ni red.
- Corrección: `apps/api/src/dining-tables.ts` usa ahora el alias del resultado escalar y `apps/api/src/dining-tables.test.ts` fija la consulta exacta como regresión. No se modificaron migraciones, esquema, Data API, credenciales ni provisioning.
- Verificación: `@super-restaurant/api` pasó lint, typecheck, suite completa y build. El monorepo pasó `pnpm lint` (6/6), `pnpm typecheck` (9/9), `pnpm test` (9/9) y `pnpm build` (6/6). CodeGraph resincronizó los dos archivos y quedó al día en 169 archivos, 3,066 nodos y 12,076 relaciones; el impacto de `PostgresDiningTableAdapter` se limita a sus métodos, su prueba y el wiring en `AppModule`, sin referencias rotas conocidas.
- Estado: P1 permanece `BLOCKED`, no REVIEW. No se obtuvieron `diningTablesVerified=true`, `diningZonesVerified=true`, `fixtureRowsRemoved=true`, `fixtureUsersRemoved=2`, por lo que no se ejecutó el smoke visual. Siguiente acción mínima: autorización humana nueva y separada para una única E2E de validación con el adapter corregido; no repetir automáticamente. Solo si queda verde, ejecutar smoke protegido desktop/móvil, revisar consola/red y mover P1 a REVIEW. Subagentes: ninguno; razonamiento alto.

## Continuación 2026-09-02 — quinta E2E verde y smoke protegido de mesas/layout

- E2E final autorizada: el precheck partió de `state=runtime`, `catalogAudit=true`, `activeSessions=false`; la CLI de Supabase aportó únicamente la clave moderna `publishable` y las tres credenciales privadas se cargaron desde `.env.adr010.local` sin imprimir valores. `runId=19489012-145d-4b2e-94e0-6c209f2e617b` terminó `status=ok` con 151 checks, `diningTablesVerified=true`, `diningZonesVerified=true`, `fixtureRowsRemoved=true` y `fixtureUsersRemoved=2`. No hubo recovery ni repetición. Tras el cierre normal del pool, el postcheck quedó runtime, catálogo post-mesas íntegro y cero sesiones.
- Corrección del smoke: el primer intento protegido emitió `runId=7c450eed-b488-4b7e-ac9d-5658b9b40256` y falló en `catalog_audit` antes de crear fixtures porque el entrypoint aún fijaba el audit post-zonas. La recuperación exacta devolvió 0 filas/0 usuarios. `run-web-protected-smoke.ts` usa ahora el audit post-mesas y `verifyDiningTables=true`; el hook `beforeRevocation` se ejecuta después de los baselines de zona y mesa, por lo que el navegador recibe un layout poblado. `schema-contract.test.ts` fija audit, flag y orden. Un lanzamiento posterior fue rechazado antes de `runId` porque el comando local omitió `DATABASE_CA_CERT_PATH` y heredó `sslrootcert` en la URL de `app_api`; no conectó ni creó fixtures. La configuración corregida pasó una validación local sin red antes de solicitar otra autorización.
- Smoke protegido final autorizado: `runId=db4ce441-a375-4076-be04-e8a358949266`, 151 checks, `diningZonesVerified=true`, `diningTablesVerified=true`, `fixtureRowsRemoved=true`, `fixtureUsersRemoved=2`, `protectedWebSmoke=true`, `status=ok`. En navegador real se verificaron login, selección exacta manager/waiter, editor poblado, cuadrícula, zona, mesa v2 y formulario de edición sin guardar; después de revocar la membresía, la misma sesión mostró `Sin sucursales asignadas`. Desktop y 390×844 quedaron legibles y sin recortes; consola sin errores/warnings. Los GET/POST observados para `/login` y `/app` respondieron 200. El navegador, viewport y temporales de coordinación se cerraron y el puerto 3000 quedó sin listener.
- Postcheck: el primero observó la sesión transitoria del pool y omitió correctamente el audit quiescente; 45 segundos después, el postcheck estrictamente read-only devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`. No quedaron usuarios, filas ni archivos temporales del smoke.
- Verificación local: `pnpm lint` 6/6, `pnpm typecheck` 9/9, `pnpm test` 9/9 y `pnpm build` 6/6; `git diff --check` verde. CodeGraph está actualizado en 169 archivos, 3,067 nodos y 12,077 relaciones. `runTenancyVerification` alcanza solo sus cuatro runners y `createWebProtectedSmokeCoordinator` solo su prueba y el entrypoint del smoke, sin referencias huérfanas conocidas.
- Estado: P1 pasó de `BLOCKED` a `REVIEW` con toda la evidencia verde. Emmanuel lo aprobó expresamente como `DONE` el 2026-09-02 y autorizó crear el commit y publicarlo en `main`. El corte parte de `80bb9bf2fc0d3cbb5dc0730ffa58ed1ecac8e5e6` y se publica por fast-forward; no incluye migraciones, Data API, rotaciones, provisioning, fixtures ajenos ni cambios offline. Subagentes: ninguno; razonamiento alto. Se aplicó la skill de navegador para el smoke visual.

## Continuación 2026-09-02 — menú P1 end-to-end local

- Estado: `Implementar menú: categorías, productos, modificadores y reglas de selección` permanece `IN_PROGRESS`. El alcance vigente cubre catálogo Restaurant-scoped y publicación autorizada desde una Branch activa; disponibilidad por sucursal/horario, precios por canal, combos y recetas quedan para sus slices posteriores del plan.
- Contratos/dominio: `shared-types` incorpora schema v1 y parsers fail-closed para categorías, productos, impuesto exacto, grupos/opciones, lectura y publicación optimista. Nest vuelve a validar el catálogo con `packages/domain`, `Money` y ratios bigint antes de persistir.
- Esquema local: `20260902000100_create_menu_catalog.sql` crea siete tablas normalizadas para releases inmutables, entidades, head atómico y auditoría idempotente; RLS forzada y cero grants de tabla, con solo `get_menu_catalog` y `save_menu_catalog` ejecutables por `app_api`. El read exige `catalog.read`; el publish exige owner/admin/manager, serializa por restaurante/evento y conserva el resultado histórico de replay. `menu_catalog_catalog.sql` audita ownership, RLS, funciones y ACL. El rollback persistente será forward-only y deberá decidirse según datos/dependencias; no se creó una migración destructiva.
- API/web: `GET|PUT /api/v1/catalog/menu` usan scope UUID exacto, no-store, RBAC, control optimista e idempotencia. `/app/menu` permite consultar y, para manager, editar moneda, categorías, productos, SKU, precio en unidad menor, estación/unidad, impuesto incluido/adicional y grupos/opciones min/max; retirar entidades exige confirmación dentro de la interfaz y cada publish crea una versión nueva. Viewer queda en solo lectura.
- Verificación local: shared-types y API/web pasaron lint, typecheck, pruebas y build por paquete. La compuerta global pasó lint 6/6, typecheck 9/9, tests 9/9 y build 6/6; Next generó `/app/menu`. El smoke Nest cubre lectura vacía, publicación manager y rechazo viewer; el cliente web cubre URL/scope/no-store, validación previa y respuesta hostil. `git diff --check` pasó.
- CodeGraph: sincronizado y actualizado en 179 archivos, 3,252 nodos y 12,755 relaciones después del refuerzo diagnóstico. `MenuCatalogService` alcanza módulo/controlador/prueba; `saveMenuCatalog` alcanza acción/editor/prueba web; el parser compartido alcanza únicamente shared-types, API, web y sus pruebas. No se detectaron referencias rotas ni consumidores inesperados.
- Límite remoto: se preparó `verify:menu-catalog-schema:rollback`, esperado en 16 tablas seguras, cinco políticas y 12 funciones `SECURITY DEFINER`, pero no se ejecutó. No hubo conexión Supabase, apply de migración, fixtures, E2E, smoke protegido, cambios Data API, rotaciones, provisioning, commit ni push. Siguiente acción mínima: autorización expresa para ejecutar una sola vez el rollback-only contra `zwbyiefqeujstyzysydn`; apply/E2E/smoke requerirán autorizaciones separadas después de un resultado verde.
- Subagentes: ninguno. Razonamiento alto por esquema, autorización multi-tenant e invariantes de precios/impuestos.

## Continuación 2026-09-02 — primer rollback-only de menú

- Autorización/resultado: Emmanuel autorizó una sola ejecución de `verify:menu-catalog-schema:rollback` contra `zwbyiefqeujstyzysydn`. La configuración local validó el ref exacto, pooler de sesión 5432, base `postgres`, TLS `verify-full` y CA local sin imprimir secretos. La única ejecución alcanzó `BEGIN`, falló en la etapa `migration` con `SCHEMA_VERIFICATION_MIGRATION_FAILED` y no llegó a la auditoría.
- Garantía de rollback: el verificador conserva el fallo inicial salvo que `ROLLBACK` o el cierre fallen. Al haberse reportado el código de migración, ambos terminaron correctamente. No quedó esquema parcial, no hubo apply, fixtures, cambios Data API, rotación/provisioning de `app_api` ni repetición automática.
- Diagnóstico local: el runner anterior eliminaba todo detalle del driver, incluido el punto no sensible del fallo. `runSchemaVerification` ahora ejecuta las sentencias extraídas una a una dentro del mismo `BEGIN` y sigue haciendo un único `ROLLBACK`; `SchemaVerificationError` solo admite `statementIndex` 1-based y SQLSTATE con allowlist estructural. El entrypoint de menú puede emitir esos dos campos, nunca el mensaje nativo, URL o credenciales. Regresiones cubren localización, SQLSTATE válido, descarte de diagnósticos hostiles, rollback y cierre.
- Verificación posterior: lint, typecheck y toda la suite API pasaron. Siguiente acción mínima: solicitar una autorización nueva para una sola ejecución diagnóstica rollback-only; con su índice/SQLSTATE se corregirá localmente antes de considerar otra prueba. La autorización consumida no se reutiliza.
- Subagentes: ninguno; razonamiento alto por esquema remoto y manejo seguro de errores. No hubo interacción Git remota.

## Continuación 2026-09-02 — segundo rollback-only diagnóstico de menú

- Resultado autorizado: la segunda y única ejecución diagnóstica alcanzó la sentencia 9 y devolvió únicamente `SCHEMA_VERIFICATION_MIGRATION_FAILED`, `statementIndex=9` y SQLSTATE `42883`. No llegó a la auditoría. Al conservarse el código inicial, `ROLLBACK` y cierre terminaron; no quedó ningún cambio persistente y no hubo repetición automática.
- Causa/corrección: `app_private.jsonb_has_exact_keys` invocaba `pg_catalog.coalesce(...)`. `COALESCE` es una expresión especial de PostgreSQL y no una función calificable, por lo que el lookup produjo `42883`. La única aparición en `20260902000100` usa ahora `coalesce(...)`; una regresión falla si reaparece la forma inválida.
- Verificación local: API pasó lint, typecheck, toda su suite y build después del fix. No hubo apply, fixtures, Data API, rotación/provisioning, E2E, commit ni push. Siguiente acción mínima: autorización nueva para una sola validación rollback-only de la migración corregida; no reutilizar las dos autorizaciones consumidas.
- Subagentes: ninguno; razonamiento alto. No hubo interacción Git remota.

## Continuación 2026-09-02 — tercer rollback-only de validación de menú

- Resultado autorizado: la tercera y única ejecución pasó las 41 sentencias de `20260902000100` y falló después en `catalog_audit` con SQLSTATE `42883`. Esto confirma que el esquema completo ya compila. El verificador conservó el fallo inicial, por lo que `ROLLBACK` y cierre terminaron; no quedó cambio persistente y no hubo repetición automática.
- Causa/corrección: `menu_catalog_catalog.sql` contenía la segunda y última forma `pg_catalog.coalesce(...)`, equivalente a la ya corregida en la migración. El auditor usa ahora `coalesce(...)`; la prueba contractual rechaza la calificación inválida en ambos archivos.
- Verificación local: API pasó nuevamente lint, typecheck, toda su suite y build. No hubo apply, fixtures, Data API, rotación/provisioning, E2E, commit ni push. Siguiente acción mínima: autorización nueva para una sola validación rollback-only de migración+auditor corregidos; no reutilizar las tres autorizaciones consumidas.
- Subagentes: ninguno; razonamiento alto. No hubo interacción Git remota.

## Continuación 2026-09-02 — cuarto rollback-only y perfil global post-menú

- Resultado autorizado: la cuarta y única ejecución de `verify:menu-catalog-schema:rollback` contra `zwbyiefqeujstyzysydn` terminó `status=ok`. Ejecutó las 41 sentencias de `20260902000100` y el auditor específico, que confirmó exactamente 16 tablas seguras, cinco políticas y 12 funciones `SECURITY DEFINER`. El `ROLLBACK` y el cierre terminaron correctamente; no hubo apply ni cambio persistente.
- Perfil global local: se añadió `tenancy_memberships_post_menu_catalog.sql`, que fija el catálogo post-menú completo, el helper privado no ejecutable por clientes y la allowlist exacta de ocho capacidades `app_api` —las seis previas más `get_menu_catalog` y `save_menu_catalog`; `build_menu_catalog_state` permanece negada. Provisioning, recovery y verificación de estado aceptan el perfil explícito `post_menu_v1` sin relajar perfiles anteriores. El runner de esquema concatena ahora el auditor específico y el global dentro de la misma transacción rollback-only.
- Verificación local: API pasó lint, typecheck, toda su suite y build; `git diff --check` pasó. CodeGraph quedó actualizado en 180 archivos, 3,267 nodos y 12,795 relaciones. `verifyAppApiState` afecta seis símbolos y `readAppApiLifecycleTarget` 14, limitados a runners operativos y pruebas conocidos; no se detectaron consumidores inesperados.
- Límite/siguiente acción: la cuarta ejecución ocurrió antes de incorporar el auditor global al runner, por lo que este archivo nuevo todavía no tiene evidencia PostgreSQL real. Solicitar una quinta y única validación rollback-only; no ejecutarla automáticamente. Solo después de quedar verde solicitar una autorización separada para aplicar persistentemente `20260902000100`. No hubo fixtures, E2E, Data API, rotación/provisioning, cambios offline, commit ni push.
- Subagentes: ninguno; razonamiento alto por esquema compartido, catálogo exacto y ciclo de vida de `app_api`. No hubo interacción Git remota.

## Continuación 2026-09-02 — quinta validación global rollback-only de menú

- Resultado autorizado: la quinta y única ejecución concatenó `20260902000100`, `menu_catalog_catalog.sql` y `tenancy_memberships_post_menu_catalog.sql` en la misma transacción contra el ref exacto. Terminó `status=ok` con `securedTables=16`, `policies=5` y `securityDefinerFunctions=12`; el rollback y cierre fueron exitosos. No hubo segunda instancia, apply ni cambio persistente.
- Incidente local: durante el preflight, una búsqueda destinada a confirmar presencia de configuración imprimió el valor completo de `ADR010_DATABASE_URL` en la salida interna de herramienta. El valor no se escribió en archivos versionados ni en esta documentación, pero la contraseña administrativa se considera expuesta y no debe reutilizarse. Debe rotarse y actualizarse el archivo ignorado antes de otra conexión.
- Siguiente acción: después de confirmar la rotación mediante un precheck que no exponga valores, solicitar autorización separada para aplicar persistentemente únicamente `20260902000100`; la autorización rollback-only quedó consumida. E2E, fixtures y smoke siguen fuera de alcance y requerirán autorización posterior.
- Subagentes: ninguno; razonamiento alto. No hubo Data API, provisioning/rotación de `app_api`, cambios offline ni interacción Git remota.

## Continuación 2026-09-02 — rotación administrativa y postcheck previo al apply

- Emmanuel rotó la contraseña administrativa expuesta y actualizó `.env.adr010.local`. La inspección local confirmó sin imprimir valores el host, puerto, base, usuario, TLS, referencia de CA y presencia de contraseña esperados.
- El primer verificador oficial read-only fue rechazado en `connection` durante la propagación. Tras 45 segundos se realizó un único reintento controlado, que terminó `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`. No se ejecutaron SQL mutativo, provisioning, recovery ni cambios de Data API.
- Siguiente acción: solicitar autorización explícita y separada para aplicar persistentemente únicamente `20260902000100` mediante el flujo de migraciones versionadas. La validación rollback-only no autoriza ese apply ni una E2E posterior.
- Subagentes: ninguno; razonamiento alto. No hubo fixtures, cambios offline ni interacción Git remota.

## Continuación 2026-09-02 — apply persistente de `20260902000100`

- Autorización y staging: Emmanuel autorizó aplicar únicamente `20260902000100` contra `zwbyiefqeujstyzysydn`, con staging, dry-run y postcheck. El staging temporal reunió las cinco migraciones ADR históricas y seis productivas en 11 copias byte a byte con hashes SHA-256 coincidentes. El historial previo contenía exactamente diez versiones y la nueva aparecía solo local.
- Compuertas: los dos primeros intentos de listar historial se detuvieron antes del dry-run porque la CLI rechazó TLS al interpretar `sslrootcert`; no hubo mutación. La CA copiada a ruta corta conservó el mismo hash pero confirmó que no era un problema de espacios. Sin degradar `verify-full`, el proceso de CLI confió esa misma CA mediante `SSL_CERT_FILE` y `PGSSLROOTCERT`; el historial quedó exacto y el dry-run anunció exclusivamente `20260902000100_create_menu_catalog.sql`, con `--skip-vault` y sin seed/roles.
- Apply/postcheck: `db push` aplicó únicamente `20260902000100_create_menu_catalog.sql`. La lectura posterior confirmó las 11 versiones local/remoto exactas. El verificador oficial `post_menu_v1` devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`; `db lint --level error` quedó limpio para `app`, `app_private` y `app_rls`.
- Limpieza/límites: se eliminó únicamente `C:\tmp\superrestaurant-menu-20260902000100` después de validar ruta y contenido. No hubo repair, SQL directo, Data API, cambio de credenciales, provisioning/recovery de `app_api`, fixtures, E2E, smoke, offline, commit ni push.
- Estado/siguiente acción: menú P1 sigue `IN_PROGRESS`. Solicitar autorización separada para una E2E remota marcada que demuestre aislamiento, RBAC, idempotencia/replay/conflicto y cleanup; si queda verde, ejecutar después el smoke protegido desktop/móvil con otra autorización acorde al runner. Subagentes: ninguno; razonamiento alto.

## Continuación 2026-09-02 — preparación local de E2E de menú

- Runner: `run-menu-catalog-tenancy-verification.ts` reutiliza el harness de tenancy y el auditor global `post_menu_v1`. Verifica Data API y tablas privadas, lectura vacía por tenant, publicación manager, lectura viewer en otra sucursal propia, rechazo cross-tenant/false-pair, replay exacto, conflicto idempotente divergente, rechazo de escritura viewer y revocación posterior. El resumen exige `menuCatalogVerified=true`, `fixtureRowsRemoved=true` y `fixtureUsersRemoved=2`.
- Cleanup/recovery: el cleanup normal comprueba conteos, IDs, nombres marcados, actor, head y auditoría antes de eliminar el release en orden FK-safe. El recovery acotado al `runId` acepta únicamente cero filas o un release completo ligado a los dos usuarios/tenants marcados; rechaza grafos parciales o contaminados y confirma cero filas después.
- Verificación local: API pasó lint, typecheck, suite completa y build; `git diff --check` pasó antes de la actualización documental. CodeGraph se sincronizó en 181 archivos, 3,297 nodos y 12,981 relaciones. El impacto de `runTenancyVerification` quedó en seis runners conocidos, `recoverTenancyFixtures` en cinco símbolos operativos/prueba y `MenuCatalogService` en diez símbolos de módulo, controlador y pruebas; no aparecieron consumidores inesperados.
- Límite/siguiente acción: no se ejecutó el runner remoto ni se crearon fixtures. Requiere autorización nueva y explícita para una única E2E contra `zwbyiefqeujstyzysydn`; si emite `runId` y falla, ejecutar solo recovery para ese UUID y no repetir automáticamente. El smoke protegido se autoriza y ejecuta después únicamente si la E2E queda verde. No hubo Data API, cambios de credenciales/`app_api`, migraciones nuevas, offline, commit, push ni subagentes; razonamiento alto.

## Continuación 2026-09-02 — primera E2E autorizada de menú rechazada antes de `runId`

- Precheck/clave pública: el verificador read-only confirmó `state=runtime`, `catalogAudit=true` y `activeSessions=false`. Supabase CLI listó una única clave moderna de tipo `publishable`; las secret permanecieron enmascaradas y solo la pública se cargó en memoria.
- Resultado: la única ejecución autorizada de `verify:menu-catalog:remote` terminó `TENANCY_VERIFICATION_CONFIGURATION_REJECTED` antes de `onStart`, sin `runId`. Por ello no creó usuarios Auth ni filas y no correspondía ejecutar recovery. No se repitió.
- Causa/corrección preparada: la construcción operativa de `DATABASE_URL` partió de `ADR010_DATABASE_URL` y heredó `sslrootcert`. `readDatabaseConfig` permite deliberadamente solo `sslmode=verify-full` en la URL de runtime, ya que la CA se carga por `DATABASE_CA_CERT_PATH`. La variante corregida conserva el mismo host/puerto/base/usuario y contraseña, reemplaza la query de la URL de `app_api` por exactamente `sslmode=verify-full`, y pasó localmente el parser productivo completo con secret moderna y opt-ins exactos.
- Postcheck/límite: el postcheck read-only final volvió a `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`. P1 menú permanece `IN_PROGRESS`. Una nueva ejecución corregida requiere autorización humana nueva; si emite `runId` y falla, usar solo recovery acotado a ese UUID y no repetir automáticamente. No hubo Data API, cambio de credenciales/`app_api`, migraciones, smoke, offline, commit, push ni subagentes; razonamiento alto.

## Continuación 2026-09-02 — segunda E2E corregida de menú y diagnóstico seguro

- Ejecución autorizada: la segunda y única E2E cargó las tres credenciales solo en memoria, derivó la URL privada con exactamente `sslmode=verify-full`, obtuvo por CLI únicamente la clave pública `publishable` y alcanzó la etapa funcional. Emitió `runId=4d0c244f-5ecf-4a9b-bdf5-4be1ce1f9a72`, pero terminó `TENANCY_VERIFICATION_ASSERTION_FAILED` en `menu_catalog`. No se repitió automáticamente y el fallo ya no corresponde a configuración ni contraseñas.
- Cleanup/recovery: el runner ejecutó su cleanup interno antes de terminar. La recuperación autorizada, invocada directamente con los dos argumentos exactos después de que el wrapper `pnpm` añadiera un separador literal y fuese rechazado antes de configurar, devolvió `fixtureRowsRemoved=0`, `fixtureUsersRemoved=0`, `status=ok`; por tanto no quedaban filas ni usuarios asociados al UUID. No se tocó ningún fixture ajeno.
- Postcheck remoto: el verificador read-only confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false` y `status=ok`. No hubo cambios de Data API, credenciales, ciclo de vida de `app_api`, migraciones, smoke, offline, commit ni push.
- Diagnóstico local: la salida anterior solo identificaba la etapa. Se añadieron checkpoints allowlisted y no sensibles para cada operación del catálogo —lecturas, publicación y respuesta, persistencia, replay, conflicto, RBAC y revocación— sin exponer cuerpos, IDs privados ni secretos. API pasó lint, typecheck, suite completa y build. CodeGraph quedó actualizado en 181 archivos, 3,299 nodos y 12,988 relaciones; `runTenancyVerification` conserva seis runners conocidos y los checkpoints solo alcanzan su módulo y prueba.
- Estado/siguiente acción: menú P1 continúa `IN_PROGRESS`. La autorización de la segunda E2E quedó consumida. Una nueva ejecución diagnóstica requiere autorización humana separada; sus checkpoints permitirán localizar la primera operación fallida antes de diseñar la corrección. El smoke protegido desktop/móvil solo procede después de una E2E verde. Subagentes: ninguno; razonamiento alto por tenancy, Auth, esquema y cleanup remoto.

## Continuación 2026-09-02 — tercera E2E diagnóstica bloqueada por autenticación administrativa

- CLI/ejecución: después de que Emmanuel renovó el acceso local, Supabase CLI devolvió cuatro entradas y exactamente una `publishable`; sus valores nunca se imprimieron. La única E2E autorizada emitió `runId=a6029fff-4ad4-4f4e-b180-ba3c3464e895` y terminó `TENANCY_VERIFICATION_FIXTURES_FAILED` en la primera etapa, sin alcanzar checkpoints de menú ni repetirse.
- Causa: una lectura mínima que replica la configuración TLS interna devolvió SQLSTATE `28P01`. `ADR010_DATABASE_URL` ya no autentica al usuario administrativo. El primer acceso del runner tras `onStart` intenta adquirir el advisory lock por PostgreSQL; ocurre antes del auditor, Auth y cualquier inserción. Por construcción no pudo crear filas, y una comprobación independiente de Auth Admin confirmó cero usuarios marcados para el UUID.
- Recovery/postcheck: los intentos iniciales de recovery usaron exclusivamente el mismo `runId`; fueron rechazados en `database` por la conexión administrativa y no alcanzaron borrados. Las pruebas intermedias que conservaron `sslrootcert` o `sslmode` directamente en `pg` reprodujeron errores locales de ruta/CA y no son evidencia del servicio; la representación productiva correcta elimina la query después de validarla y fue la que aisló `28P01`. Después de actualizar `ADR010_DATABASE_URL`, esa conexión mínima devolvió `status=ok`; el recovery autorizado del UUID terminó `status=ok`, `fixtureRowsRemoved=0`, `fixtureUsersRemoved=0`. Los postchecks oficiales anterior y posterior confirmaron `state=runtime`, `catalogAudit=true`, `activeSessions=false` y `status=ok`.
- Hardening local: se añadieron checkpoints allowlisted y sin IDs para creación de amber/cobalt, inicio de transacción, restaurantes, sucursales, membresías, roles y commit; complementan los checkpoints de catálogo ya existentes. API pasó lint, typecheck, suite completa y build. No hubo Data API, cambio de `app_api`, migración, smoke, offline, commit, push ni subagentes.
- Siguiente acción: la tercera autorización E2E quedó consumida y el prerrequisito remoto ya está verde. Solicitar autorización nueva y separada para una única E2E de menú; no repetir automáticamente si falla. Solo si queda verde, ejecutar el smoke protegido desktop/móvil antes de mover P1 a REVIEW. No hubo rotación adicional, Data API, cambio de `app_api`, migración, smoke, commit ni push; subagentes: ninguno, razonamiento alto.

## Continuación 2026-09-02 — cuarta E2E de menú y diagnóstico granular

- Ejecución única: el precheck oficial devolvió `state=runtime`, `catalogAudit=true`, `activeSessions=false`; la CLI aportó en memoria exactamente una clave `publishable`. El runner emitió `runId=fa0e96a2-17d8-4d6d-ba7c-357f560e87ac`, completó lock, creación de amber/cobalt y commit del grafo de tenancy. Validó escritura anónima rechazada y persistencia vacía; falló `TENANCY_VERIFICATION_ASSERTION_FAILED` durante `menu_catalog.manager_empty_read`, antes de `other_tenant_empty_read`. No hubo repetición.
- Cleanup: la recuperación autorizada y acotada al UUID terminó `status=ok`, `fixtureRowsRemoved=0`, `fixtureUsersRemoved=0`, confirmando que el cleanup interno ya había eliminado todo. El primer postcheck inmediato observó una sesión `app_api` transitoria y por diseño omitió el auditor; tras cerrarse el pool, el postcheck final confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`.
- Diagnóstico: una consulta estrictamente read-only con scope sintético confirmó `emptyStateValid=true`, `executeGranted=true`; no leyó ni mostró datos reales. El esquema remoto y el grant de `get_menu_catalog` no explican el fallo, que queda dentro del trayecto HTTP autenticado. El runner ahora divide la lectura manager en respuesta, status, cache, JSON, contrato, scope y catálogo, y emite una observación allowlisted sin cuerpos, tokens, IDs ni errores nativos.
- Verificación local: `@super-restaurant/api` pasó lint, typecheck, suite completa y build. CodeGraph sincronizó los cuatro archivos modificados y quedó actualizado en 181 archivos, 3,307 nodos y 13,055 relaciones; `diagnoseMenuCatalogRead` alcanza solo implementación, harness y prueba, y el callback nuevo solo el baseline y su entrypoint.
- Estado/siguiente acción: menú P1 permanece `IN_PROGRESS`. La cuarta autorización quedó consumida; una quinta E2E diagnóstica requiere autorización humana separada y no debe repetirse automáticamente. Solo si queda verde procede el smoke protegido desktop/móvil. No hubo migraciones, Data API, cambios de credenciales/`app_api`, smoke, offline, commit, push ni subagentes; razonamiento alto.

## Continuación 2026-09-02 — quinta E2E de menú y corrección del comparador

- Ejecución única: precheck `state=runtime`, `catalogAudit=true`, `activeSessions=false`; clave `publishable` única obtenida solo en memoria. `runId=90b3b194-7dde-4156-8cea-dd8593e9b426` completó lock y fixtures. La lectura manager alcanzó respuesta HTTP 200, cache válida, JSON decodificado, contrato válido y scope exacto; el diagnóstico final fue únicamente `catalog_mismatch`. No hubo repetición.
- Causa/corrección: el catálogo esperado y observado eran ambos `null`, pero `valuesEqual` entraba a la rama de objetos porque `typeof null === "object"` y retornaba falso. El helper ahora compara por identidad cuando cualquiera de los operandos es `null`. Las regresiones cubren `null` directo, catálogo vacío dentro de un objeto, arreglos anidados y divergencia `null`/objeto.
- Cleanup/estado remoto: el recovery exclusivo del UUID terminó `status=ok`, `fixtureRowsRemoved=0`, `fixtureUsersRemoved=0`; el cleanup interno ya había retirado ambos usuarios y todas las filas. El postcheck final confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`.
- Verificación local: `@super-restaurant/api` pasó lint, typecheck, suite completa y build. CodeGraph sincronizó `tenancy-verification.ts` y su prueba. No hubo migraciones, Data API, cambios de credenciales o ciclo de vida de `app_api`, smoke, offline, commit, push ni subagentes; razonamiento alto.
- Estado/siguiente acción: menú P1 permanece `IN_PROGRESS`. La quinta autorización quedó consumida. Solicitar una sexta y única E2E de validación; no repetirla automáticamente. Si queda verde, proceder al smoke protegido desktop/móvil y después mover P1 a REVIEW.

## Continuación 2026-09-02 — sexta E2E verde y preparación del smoke de menú

- E2E: el precheck oficial confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false`; la CLI aportó en memoria exactamente una clave `publishable`. `runId=cdcbac4a-400c-42e4-837a-a703baba55f0` terminó `status=ok`, 141 checks, `menuCatalogVerified=true`, `fixtureRowsRemoved=true`, `fixtureUsersRemoved=2`. Validó lectura vacía por tenant, false-pair, publicación manager, persistencia, replay, conflicto idempotente, lectura viewer, escrituras rechazadas, revocación y cleanup. No correspondió recovery.
- Estado remoto: los dos primeros postchecks inmediatos detectaron únicamente una sesión `app_api` transitoria del pool y por diseño omitieron el auditor. Sin terminar sesiones ni mutar el rol, el siguiente postcheck confirmó `state=runtime`, `catalogAudit=true`, `activeSessions=false`, `status=ok`.
- Smoke preparado: se añadió `run-menu-web-protected-smoke.ts` y el script `verify:menu-web-protected-smoke:remote`. Reutiliza el coordinador lease/ack probado, ejecuta el perfil `post_menu_v1`, publica el fixture con catálogo poblado antes de revocación y espera la comprobación del navegador antes de continuar a cleanup. El contrato confirma que el hook ocurre después del baseline de menú y antes de revocar.
- Verificación local: `@super-restaurant/api` pasó lint, typecheck, suite completa y build. CodeGraph sincronizó el runner nuevo y su consumidor contractual. No hubo migraciones, Data API, cambios de credenciales o ciclo de vida de `app_api`, smoke, offline, commit, push ni subagentes; razonamiento alto.
- Estado/siguiente acción: P1 menú permanece `IN_PROGRESS`. Requiere autorización separada para una sola ejecución del smoke remoto, sus fixtures temporales y la introducción de credenciales temporales en el login local. Verificar editor en desktop y 390×844, consola y red, confirmar revocación/cleanup y postcheck; si todo queda verde, mover P1 a REVIEW, no DONE.

## Git remoto

- SSH: la huella de GitHub fue aceptada, pero la autenticación falló con Permission denied (publickey).
- HTTPS: lectura, fetch y escritura autorizados para este cierre.
- Push de este corte: `main` se publica por fast-forward desde la base común `bf41eb31cd95a34bd83d8618fd36c3122044c7e6`; no incluye apply de migraciones, cambios Supabase ni force-push.
- origin/main y origin/master contienen 10,427 rutas de node_modules.
- origin/main además contiene un prototipo legado.
- No se hizo merge, checkout, reset, rebase ni push; los documentos locales no fueron sobrescritos.
- Auditoría read-only de licencia: `origin/main@293a1551` y `origin/master@f87d5b25` no declaran licencia para código propio; `package.json` tampoco tiene campo `license`. No se reutilizó código del prototipo y queda bloqueado hasta autorización/licenciamiento escrito del titular.

## Cambios documentales

### AGENTS.md

Se eliminó todo el contexto ATS/candidatos/evaluaciones. La nueva versión regula arquitectura, dominio POS, dinero, pagos, tenancy, offline, seguridad, pruebas, UI, observabilidad, Git y delegación.

### Plan maestro

La versión 2.2:

- define piloto online y v1 offline;
- fija stack greenfield TypeScript;
- añade invariantes financieras;
- especifica sync causal/idempotente;
- separa offline por dispositivo de servidor LAN;
- añade seguridad, backups y observabilidad desde Fase 0;
- incorpora criterios de aceptación, ADRs, riesgos y estrategia Git.
- convierte ADR-010 en puerta bloqueante con tres opciones y un spike verificable;
- define `card_manual` sin confundirlo con cobro integrado;
- corrige información y licencias de proyectos de referencia.
- limita el spike a 4 días hábiles, con hard stop al quinto;
- define gates comunes, scoring ≥75/100 y GO/NO-GO por opción;
- autoriza packages/domain como track paralelo independiente.

## Bloqueos

- **Estrategia Git del prototipo:** ADR-006 fue aceptada por el humano. El greenfield usa la raíz `ceaffcb`; el merge `ours` `f9f4428` conserva la historia anterior como segundo padre sin incorporar su árbol. No se reutiliza código legado sin licencia/autorización escrita.
- **Publicación Git:** HTTPS quedó verificado. `main` y `codex/modernizacion-fase0` publican el mismo árbol moderno; `main` avanzó desde `293a155` a `f9f4428` sin force-push y `origin/master` no cambió.
- Mercado/CFDI/hardware no bloquean toda Fase 0, pero sí sus módulos.
- **Regla fiscal definitiva:** ADR-007 solo resuelve invariantes técnicas. País, moneda inicial, tasas, exenciones, tratamiento fiscal de propina y CFDI requieren decisión humana y asesoría fiscal antes de producción.
- **Decisión final de ADR-010 B:** Emmanuel aceptó expresamente B el 2026-08-29. Los diez gates y el score 75/100 sustentan la decisión. NestJS es la única frontera crítica; Supabase gestiona Auth/PostgreSQL y Storage cuando un módulo lo requiera. ORM, cola, transporte Realtime, despliegue y schema productivo no fueron decididos por el spike. La recuperación física es evidencia operativa separada. El proyecto anterior `cxcnnhafchqslvgvkeye` sigue siendo ajeno/no aislado y no debe usarse para gates destructivos.
- **Evidencia de ADR-010 A:** no hay Docker, PostgreSQL, Redis ni distribución WSL disponible en este equipo.
- **Implementación offline:** ADR-004 y sus especificaciones están en REVIEW, y ADR-005 (almacenamiento local) sigue pendiente. No afirmar que Web, Mobile o KDS funcionan offline.
- **Persistencia futura de auditoría:** el dominio no autentica actores ni demuestra unicidad o atomicidad de almacenamiento. La frontera seleccionada por ADR-010 deberá verificar RBAC, asignar `receivedAt`, comparar evidencia rehidratada con eventos, aplicar unicidad a `eventId`/idempotency key y guardar Order+evento en una sola transacción.
- **Credencial administrativa expuesta en salida técnica:** incidente histórico cerrado mediante rotación confirmada. La credencial vigente autenticó con TLS `verify-full`; no fue impresa, versionada ni copiada a documentación.
- **Despliegue del directorio:** `20260830000200`, la exposición exacta de `app` y la E2E remota ya están aplicadas/verificadas. La autorización fue consumida para esa ejecución; no repetir ninguna mutación remota sin una autorización nueva.

## Siguiente acción recomendada

1. Solicitar revisión humana de `docs/architecture/RBAC_MATRIX_V1.md`; cualquier cambio de concesiones debe conservar versión explícita y regresiones. No desplegar ni inferir nuevos permisos mientras la matriz siga en REVIEW.
2. Con la matriz aceptada, implementar el primer endpoint de negocio restringido y su capacidad PostgreSQL privada. Debe usar un permiso RBAC tipado, devolver 403 genérico ante rol insuficiente/false pair/revocación y demostrar cero efectos antes de persistir. No inventar una ruta de autorización separada ni confiar en la UI.
3. No repetir migraciones, provisioning de `app_api`, exposición Data API ni E2E remota: ya quedaron verdes y cualquier nueva mutación remota necesita autorización nueva. El nuevo slice debe preparar primero migración/rollback y pruebas locales/rollback transaccional.
4. Emmanuel puede asignar explícitamente FE-0.1 a Claude, limitado a `apps/web/**`; sin esa frase Claude permanece detenido. FE-0 sigue IN_PROGRESS únicamente por smoke humano del login.
5. Mantener Redis/jobs/Realtime, recuperación física/RPO/RTO/observabilidad y repetición de Gate 4 como tracks separados que ADR-010 no cerró.

## Subagentes y verificación de la frontera financiera B

- `auditoria_final_finanzas_b` (razonamiento alto): auditó FKs, orden de gates y representación `bigint`; implementó correcciones acotadas en auditoría/preflight/gates.
- `completar_parcial_finanzas_b` (razonamiento alto): implementó restauración del cursor y la quinta migración correctiva, más auditoría, preflight y documentación técnica.
- `corregir_close_b` (razonamiento moderado): hizo `close()` reintentable y conservó errores múltiples de cleanup.
- Verificación exacta acumulada posterior a la integración: lint, typecheck, 77 pruebas de dominio + 57 del spike (134 pruebas Node), contrato runtime + prueba compile-time de shared-types y build exitosos; última cobertura medida antes de añadir las propiedades/adversariales recientes: 96.26% líneas, 93.00% ramas y 98.19% funciones; `supabase db push --dry-run`; cinco migraciones aplicadas; auditoría 27/27; `supabase db lint` sin errores; 15 evidencias remotas; probe RLS; runner completo repetido con TLS `verify-full`; limpieza final 0 usuarios y 0 filas operativas.
- CodeGraph se consultó antes y después; el impacto de la frontera financiera queda contenido en el adapter/Nest/gates del spike y sus funciones SQL privadas. Rollback: las migraciones son aditivas; no se ejecutó rollback porque la evidencia y limpieza quedaron verdes.
- `implementar_agregado_order` (gpt-5.6-terra, razonamiento moderado): implementó y corrigió el agregado Order/OrderItem; `endurecer_agregados_financieros` (gpt-5.6-sol, razonamiento alto): cerró integridad de Payment/CashRegister; `validar_money_tax_runtime` (gpt-5.6-terra, razonamiento moderado): cerró validaciones Money/TaxSnapshot.
- `corregir_invariantes_order` (gpt-5.6-terra, razonamiento alto): corrigió cancelación total, terminalidad y accessors; `auditar_finanzas_reintento` (gpt-5.6-terra, razonamiento alto): reprodujo TOCTOU y añadió snapshots/idempotencia financiera. Una primera auditoría financiera independiente falló por límite de uso y no se usó como evidencia.
- `verificar_licencia_legado` (gpt-5.6-luna, razonamiento bajo): verificó refs/commits y ausencia de licencia del prototipo; `auditar_propiedades_dominio` (gpt-5.6-terra, razonamiento alto): añadió propiedades deterministas de Money, descuentos y caja sin dependencias.
- `auditar_gate4_gate8` (gpt-5.6-terra, razonamiento alto): auditó la frontera B, separó Gate 8 lógico ya demostrado de DR físico operacional y corrigió README, reporte estructurado y regresión. En ese corte todavía faltaban Gate 4 y Gate 7; ambos quedaron demostrados posteriormente.
- `auditar_cobertura_fase0_dominio` (gpt-5.6-terra, razonamiento alto): auditó cobertura neutral, aisló modificadores por restaurante y endureció estados runtime; `auditar_order_adversarial` (gpt-5.6-terra, razonamiento alto): reprodujo getters heredados y cierre prematuro, y corrigió ambos; `auditar_finanzas_adversarial_2` (gpt-5.6-terra, razonamiento alto): añadió eventos explícitos y scope de auditoría a Payment/Refund y cerró la pérdida de motivo al rehidratar caja.
- `endurecer_totales_runtime` (gpt-5.6-terra, razonamiento alto): endureció calculadoras y modificadores contra formas/accessors/proxies hostiles; `validar_scopes_shared` (gpt-5.6-terra, razonamiento moderado): añadió parsers runtime versionados de scope; `auditar_integracion_dominio_actual` (gpt-5.6-terra, razonamiento alto): encontró y corrigió colisiones cross-operation en Payment/CashRegister.
- CodeGraph posterior indexó 56 archivos, 1,281 nodos y 3,481 relaciones; los nuevos parsers solo tienen consumidores de prueba y las fronteras financieras/totales permanecen contenidas en dominio, sin `apps/` productivas. No hubo cambios de esquema, offline ni Supabase remoto en esta unidad.
- Sesión 2026-08-29: CodeGraph se inicializó y consultó antes/después del cambio; quedó en 56 archivos, 1,290 nodos y 5,203 relaciones. `requireFreshRemotePushOptIn` y la invocación CLI fijada alcanzan solo el runner y su preflight; la nueva configuración de migración no llega a aplicaciones productivas.
- Verificación limpia 2026-08-29: instalación frozen exitosa; el primer `typecheck`/`test` detectó la dependencia de orden. Tras corregirla, una ejecución sin `dist`, caché ni `tsbuildinfo` pasó typecheck y 77 pruebas de dominio + 57 del spike, además de shared-types. El alcance opción B pasó lint, typecheck, 31 preflights y build. No se ejecutó Supabase remoto.
- Pipeline global posterior 2026-08-29: `pnpm install --frozen-lockfile`, lint, typecheck, 77 pruebas de dominio, 57 del spike, contratos runtime/compile-time de shared-types y build exitosos. `git diff --check` pasó y no quedaron `.test-dist`, secretos ni evidencia remota generada.
- Preparación Gate 7 2026-08-29: CodeGraph limitó la invocación CLI al fresh-push/runner/preflight; lint, typecheck, 31 preflights B y build del alcance pasaron. `npx.cmd --yes supabase@2.109.1 --version` devolvió `2.109.1`. `projects list` fue solo lectura y confirmó que la sesión actual no ve `zwbyiefqeujstyzysydn`; no hubo enlace, configuración remota ni migraciones.
- Ejecución Gate 7 2026-08-29: la sesión CLI correcta enumeró y enlazó exclusivamente `zwbyiefqeujstyzysydn`. El runner confirmó destino vacío, dry-run de las cinco migraciones esperadas, revalidación anti-TOCTOU, push completo y postcheck de historial exacto. Se expuso solo `adr010_b`; auditoría estructural 27/27 y `db lint` quedaron verdes. Los 31 preflights y 15 capacidades remotas pasaron con código 0; el cleanup final dejó 0 usuarios Auth y 0 filas operativas/financieras, conservando solo 2 restaurantes y 4 sucursales deterministas.
- Cierre local Gate 7 2026-08-29: el reporte distingue Gate 7 como evidencia externa demostrada y mantiene `eligibleForAdr010Go: false` solo por Gate 4. CodeGraph quedó en 56 archivos, 1,290 nodos y 5,203 relaciones, con impacto limitado al reporte. Instalación frozen, lint, typecheck, 77 pruebas de dominio, 57 del spike, contratos shared-types y build global pasaron.
- Preinspección técnica Gate 4 2026-08-29: se recorrieron los DTOs, servicios Nest, verificador Auth, puertos PostgreSQL y funciones SQL finales; búsquedas dirigidas no encontraron mutaciones Data API ni clientes productivos. El token separado del supervisor deriva una identidad verificada y SQL exige rol activo `owner`/`manager` en el scope exacto. La auditoría independiente identificó `restore/reset` como escrituras PostgreSQL administrativas del runner; quedaron documentadas como excepciones privadas no alcanzables por clientes. Los hooks `induceFailureAfter*` y puertos del spike deben excluirse de controladores/bundles productivos. `WRITE_FRONTIER.md` conserva Reviewer/Date/Decision sin completar para no sustituir la aceptación humana.
- Verificación posterior a la preinspección Gate 4: CodeGraph siguió actualizado con 56 archivos, 1,290 nodos y 5,203 relaciones; ambos servicios críticos solo impactan adapter, preflight, módulo Nest y barrel del spike. El alcance B pasó lint, typecheck, 31/31 preflights y build. La instalación frozen y el pipeline global completo pasaron: lint 4/4, typecheck 6/6, tests 6/6 y build 4/4; no se ejecutó ninguna nueva mutación remota.
- Cierre Gate 4 y scoring 2026-08-29: Emmanuel aceptó la frontera de escritura después de revisar su propósito y mantuvo la aprobación tras una explicación funcional. `WRITE_FRONTIER.md` registra Reviewer/Date/Decision, el reporte declara elegibilidad y `B_SCORING.md` asigna 75/100 con todos los mínimos cumplidos exactamente. Emmanuel aceptó después el GO final y seleccionó B. No hubo mutación remota nueva.
- Catálogo neutral de menú 2026-08-29: `packages/domain/src/menu.ts` valida y congela categorías/productos por Restaurant, versión y moneda, ordena por `displayOrder`, verifica SKU/referencias/allowlists y resuelve grupos obligatorios mediante `modifiers.ts` para producir `OrderItemPriceSnapshot` histórico compatible con Order. Diez pruebas nuevas cubren integración exacta, estados, duplicados, scope/versiones, monedas, mutaciones y entradas hostiles. El primer test descubrió y se corrigió un normalizador que rechazaba instancias válidas de Money. CodeGraph posterior: 58 archivos, 1,383 nodos y 5,562 relaciones; el contrato nuevo solo impacta su prueba/barrel y reusa `OrderItemPriceSnapshot` sin cambiarlo. Pipeline global: instalación frozen, lint 4/4, typecheck 6/6, 87 pruebas de dominio, 57 del spike, shared-types runtime/compile-time y build 4/4. No hubo esquema, red, Supabase ni aplicación productiva.
- Auditoría neutral de órdenes 2026-08-29: se añadieron `order-audit.ts` y su suite, se integró `OrderMutation` en creación/altas/transiciones/cancelación y se endureció `order-state.ts`. Una revisión independiente detectó dos huecos antes de REVIEW: lectura de inputs dinámicos durante creación y líneas canceladas rehidratadas sin evidencia; ambos se corrigieron con validación previa y evidencia discriminada/enlazada para toda cancelación. CodeGraph quedó actualizado con 60 archivos, 1,460 nodos y 6,115 relaciones; `OrderItemCancellationAudit` alcanza solo dominio, barrel y pruebas, sin consumidores productivos. Instalación frozen, lint 4/4, typecheck 6/6, 100 pruebas de dominio, 57 del spike, shared-types runtime/compile-time y build 4/4 pasaron; `git diff --check` quedó limpio. No hubo esquema, migración, red, Supabase, Git remoto ni implementación offline.
- `auditar_eventos_auditoria` — gpt-5.6-sol, razonamiento alto, read-only. Delimitó el contrato específico de Order, identificó los dos huecos anteriores y recomendó evidencia obligatoria enlazada para rehidratación; alcanzó su límite de uso después de entregar los hallazgos, sin editar archivos. El agente principal integró y verificó las correcciones.
- `auditar_adr001_opcion_b` — razonamiento alto, read-only. Separó el núcleo B aprobado de decisiones no demostradas y evitó fijar prematuramente ORM, cola, Realtime, despliegue o schema.
- `auditar_auth_api` — razonamiento alto, read-only. Detectó aceptación ciega del principal, protección local opt-in y una inyección Nest faltante; el agente principal corrigió los tres puntos y añadió smoke real. El cierre confirmó 18/18 pruebas de API sin bypass P0/P1; la integración de membresías sigue pendiente.
- Slice Auth/API 2026-08-29: `apps/api` valida solo URL HTTPS raíz y publishable key moderna/acotada, verifica tokens mediante `auth.getUser`, deriva/clona/congela `actorId`, protege globalmente con `APP_GUARD` y expone únicamente health como público. `MembershipAuthorizationService` rechaza scopes, roles, adapters y datos hostiles; todavía no tiene adapter PostgreSQL ni consumidor productivo. No se añadió schema, migración, DB URL, secret key ni acceso remoto Supabase.
- Verificación final ADR/Auth 2026-08-29: CodeGraph quedó actualizado en 71 archivos, 1,574 nodos y 6,396 relaciones. `SupabaseAuthGuard` impacta solo módulo/controladores/pruebas de API; `MembershipAuthorizationService` todavía solo tiene consumidores de prueba. Instalación frozen, lint 5/5, typecheck 7/7, 100 pruebas de dominio, 57 del spike, contratos shared-types, 18 pruebas de API y build 5/5 pasaron. El smoke Nest confirmó health 200 y session anónima 401; `git diff --check` y hashes idénticos de ambas copias del plan pasaron. No hubo mutación remota, schema, migración, secreto, certificado versionado ni operación Git remota.
- Slice tenancy/Auth 2026-08-30: se añadieron ADR-002, migración productiva inicial, configuración Data API, auditoría SQL de catálogo, conexión `pg` TLS fail-closed, rol dedicado `app_api`, lookup privado y selección de Branch. La revisión independiente encontró ownership implícito y posible rol preexistente contaminado; ambos se corrigieron fijando owner `postgres`/`BYPASSRLS`, contraseña nula, cero membresías y marcador de colisión. CodeGraph quedó en 77 archivos, 1,654 nodos y 6,608 relaciones; `MembershipAuthorizationService` ahora alcanza controller, adapter, módulo y pruebas. Treinta pruebas API, lint, typecheck y build de alcance pasaron. NestJS 11.2.3 se reemplazó por 11.2.2 en `apps/api` porque la publicación 11.2.3 instalada carecía de un módulo runtime. La migración sigue sin prueba PostgreSQL real por ausencia de Docker/Podman.
- Incidente de credencial 2026-08-30: un verificador transaccional temporal no llegó a conectarse, pero un error de parseo imprimió la URL administrativa completa en la salida de la tarea. El archivo temporal fue eliminado y no se versionó ningún secreto; la contraseña de `zwbyiefqeujstyzysydn` debe considerarse comprometida y rotarse antes de cualquier nuevo acceso. No hubo cambio remoto persistente.
- Verificación local final 2026-08-30: instalación frozen; lint 5/5, typecheck 7/7, tests 7/7 y build 5/5 pasaron. Las suites incluyen 100 pruebas de dominio, 57 del spike, contratos shared-types y 30 pruebas de API. CodeGraph quedó actualizado con 77 archivos, 1,654 nodos y 6,608 relaciones; el adapter de membresías solo alcanza servicio, controller, módulo y pruebas de API. `git diff --check` quedó limpio, `.test-dist` se eliminó y ni `.env.adr010.local` ni el certificado están versionados. La auditoría SQL de catálogo existe pero no se ejecutó en PostgreSQL real.
- `auditar_schema_memberships` — razonamiento alto, read-only. Definió el modelo multirrol histórico, exact Branch scope, constraints, RLS/grants, rollback y matriz 2×2; evitó copiar el rol único del spike.
- `auditar_slice_tenancy_final` — razonamiento alto, read-only. No encontró P0; detectó owner implícito de funciones, reutilización posible de un rol contaminado y ausencia de prueba PostgreSQL real. Los dos primeros hallazgos se corrigieron; el tercero mantiene las tareas IN_PROGRESS.
- Verificadores productivos 2026-08-30: `apps/api` incorporó un dry-run fresh que ejecuta migración+auditoría dentro de una transacción y hace rollback/cierre aun ante fallos, y un runner remoto 2×2 opt-in para Auth/RLS/Data API/`app_api`/Nest/revocación/constraints. Ambos validan proyecto, TLS `verify-full` y CA antes de red y solo reportan códigos allowlisted. La auditoría de catálogo se separó en estado pre-provisioning `NOLOGIN` y runtime `LOGIN` mínimo; el cleanup remoto valida la propiedad de Restaurant/Branch antes de borrar. No se ejecutó red ni Supabase porque la contraseña expuesta sigue pendiente de rotación.
- Verificación local de los verificadores: lint, typecheck, 42 pruebas API y build pasaron; ambos entrypoints sin opt-in rechazaron antes de red con JSON sanitizado. CodeGraph quedó actualizado en 84 archivos, 1,824 nodos y 7,251 relaciones; `runTenancyVerification` solo alcanza su entrypoint y `runSchemaVerification` solo su entrypoint/pruebas. El pipeline global pasó instalación frozen, lint 5/5, typecheck 7/7, tests 7/7 y build 5/5. Las tareas permanecen IN_PROGRESS hasta evidencia remota real.
- `implementar_verificador_schema_seguro` — razonamiento alto, unidad acotada con edición. Implementó el runner transaccional fresh, siete pruebas finales tras integración y CLI sanitizada; no accedió a red ni actualizó documentación operativa.
- `disenar_runner_tenancy_2x2` — razonamiento alto. Primero auditó read-only la matriz y detectó el conflicto `NOLOGIN`/runtime; después implementó configuración, runner 2×2 y cuatro pruebas de configuración. El agente principal separó las auditorías y endureció cleanup, contratos y documentación; no hubo ejecución remota.
- Verificación remota rollback-only 2026-08-30: la contraseña rotada conectó al ref exacto `zwbyiefqeujstyzysydn` por pooler con TLS `verify-full` y la CA local ignorada. La migración+auditoría oficial pasó con 5 políticas, 5 tablas protegidas y 4 funciones endurecidas; el rollback y postcheck dejaron ausentes `app`, `app_private`, `app_rls` y `app_api`. No se imprimieron credenciales ni hubo mutación persistente.
- PostgreSQL 16/Supabase exigió adaptar el contrato: `postgres` no es superusuario y al crear `app_api` recibe una concesión ADMIN inevitable otorgada por `supabase_admin`. Como no incluye INHERIT ni SET y la cuenta ya es administrativa, ambas auditorías aceptan exactamente esa fila y rechazan cualquier variación o membresía adicional. El verificador también normaliza la respuesta multi-statement de `pg`. Pasaron instalación frozen, pipeline global (lint 5/5, typecheck 7/7, tests 7/7 y build 5/5), 42 pruebas API, 100 de dominio, 57 del spike y contratos shared-types; CodeGraph quedó en 84 archivos, 1,825 nodos y 7,255 relaciones.
- `implementar_verificador_schema_seguro` — segundo análisis read-only, razonamiento alto. Identificó la semántica `GRANTED BY` de PostgreSQL 16; la prueba remota confirmó que el usuario administrado no puede revocar la concesión bootstrap, por lo que el agente principal integró el contrato exacto compatible con Supabase.
- `implementar_recuperacion_tenancy` — razonamiento alto, unidad standalone de recuperación. El subagente agotó su cuota antes de trabajar y no modificó archivos; la herramienta de recuperación ante cierre forzado sigue pendiente y no bloquea la ejecución normal con cleanup `finally`.
- Integración de historial 2026-08-30: una consulta remota read-only confirmó las cinco versiones del spike (`20260825000100`–`20260829000100`). La primera versión productiva fue renombrada a `20260830000100_create_tenancy_memberships.sql`; Supabase CLI la reconoce como la única migración local pendiente y sin colisión. No se ejecutó `db push`, link ni reparación de historial.
- Preparación de aplicación persistente: el dry-run directo detectó que las cinco versiones remotas no viven en la carpeta productiva y se rechazó reparar/revertir el historial. Un staging temporal con copias exactas de las cinco migraciones históricas más `20260830000100` confirmó que `db push` aplicaría solo la nueva migración, sin seed, roles ni Vault; el staging fue eliminado. La ejecución real requiere autorización humana explícita.
- Aplicación persistente 2026-08-30: Emmanuel autorizó exactamente `20260830000100` en `zwbyiefqeujstyzysydn`. Se aplicó desde un staging temporal con las cinco migraciones históricas exactas; no hubo reparación de historial, seed, roles, Vault ni push de configuración. El postcheck desde staging confirmó las seis versiones esperadas, auditoría pre-provisioning correcta (5 tablas, 5 políticas, 4 funciones, `app_api` NOLOGIN) y `db lint` sin errores.
- Recuperación E2E 2026-08-30: `apps/api` añadió un CLI destructivo separado con confirmación que repite el UUID, opt-in propio, salida allowlisted y configuración administrativa mínima. Recovery y runner comparten advisory lock; Auth exige marker/version/runId/fixture_key/email exactos, y PostgreSQL solo acepta estado vacío, Auth-only o el grafo completo 2R/4B/4M/5G con prefijo de revocación válido. Los deletes son por IDs exactos con `RETURNING` y postcheck; estados parciales/contaminados abortan.
- Verificación del recovery: 50 pruebas API locales pasaron, incluidas configuración destructiva, idempotencia, Auth-only, grafo completo, contaminación, orden de revocación y TOCTOU Auth. Lint, typecheck y build del alcance pasaron; CodeGraph quedó en 90 archivos, 1,949 nodos y 7,744 relaciones y confirmó que el entrypoint no es consumidor de controladores. La prueba remota del recovery queda pendiente hasta que exista un `runId` residual real o una ejecución controlada autorizada.
- Pipeline global posterior: instalación frozen, lint 5/5, typecheck 7/7, tests 7/7 y build 5/5 pasaron; las suites incluyen 100 pruebas de dominio, 57 del spike, 50 de API y contratos runtime/compile-time de shared-types. No hubo nueva mutación Supabase durante esta verificación.
- `implementar_recuperacion_e2e` — razonamiento alto, edición parcial. Añadió marcadores compartidos, lock y el esqueleto fail-closed; agotó cuota antes de tests/CLI. El agente principal auditó, completó e integró sus cambios.
- `auditar_recuperacion_e2e` — razonamiento alto, read-only. Identificó riesgos de nombres genéricos, deletes amplios, estados parciales, paginación Auth, carrera con runner y prefijos de revocación; agotó cuota después de entregar hallazgos. Todos quedaron incorporados.
- Aprovisionamiento privado local 2026-08-30: `apps/api` añadió configuración y CLI independientes con opt-in/confirmación exactos, correlación de URL/ref/host, CA y TLS `verify-full`, contraseña fuerte solo por entorno y reportes allowlisted. El flujo toma un lock global compartido con el E2E, fija SCRAM, activa `app_api` por diez minutos usando un parámetro enlazado, verifica identidad y permisos desde una conexión nueva, audita catálogo y promueve a vigencia indefinida. Ante cualquier resultado ambiguo o fallo posterior al intento de cambio, una sesión administrativa nueva compensa a `NOLOGIN PASSWORD NULL` y reaudita el estado seguro.
- Verificación del aprovisionador: lint, typecheck, 62 pruebas API y build del alcance pasaron. Las pruebas cubren éxito, precheck, lock ocupado, commit ambiguo, identidad errónea, auditoría alterada, compensación y fallo de compensación; la contraseña nunca aparece en SQL. `git diff --check` pasó. CodeGraph no estuvo disponible en este corte; la inspección dirigida confirmó que el CLI es el único consumidor del aprovisionador y que el lock solo enlaza aprovisionador y runner, sin alcance desde controladores/clientes.
- `auditar_aprovisionamiento_app_api` — razonamiento alto, read-only. Definió prechecks de catálogo, contraseña parametrizada/SCRAM, lock global, credencial temporal, verificación real y compensación; también acotó la afirmación de privilegios al schema de producto. El agente principal integró los hallazgos.
- Segundo incidente de credencial 2026-08-30: un diagnóstico read-only temporal falló antes de abrir red porque la URL administrativa local estaba envuelta en comillas; el error nativo imprimió la URL completa en la salida de la tarea. El archivo temporal se eliminó, no se versionó ningún secreto y no hubo mutación remota. Esa contraseña se considera comprometida, no se reutilizará y debe rotarse otra vez antes de continuar remotamente.
- Pipeline global posterior al aprovisionador: instalación frozen, lint 5/5, typecheck 7/7, tests 7/7 y build 5/5 pasaron; las suites incluyen 100 pruebas de dominio, 57 del spike, 62 de API y contratos runtime/compile-time de shared-types. `git diff --check` quedó limpio, `.test-dist` y el script temporal están ausentes, y Git confirmó que `.env.adr010.local` y `.certs/prod-ca-2021.crt` siguen ignorados. No hubo operación Git remota ni nueva mutación Supabase.
- Rotación posterior 2026-08-30: Emmanuel confirmó que reemplazó la contraseña administrativa expuesta. La nueva credencial permanece privada; esta unidad no leyó `.env`, no abrió red y no ejecutó ningún cambio remoto.
- Recuperación standalone de `app_api` 2026-08-30: se añadió CLI/configuración propios, sin contraseña de aplicación, con correlación URL/ref/DB/CA/TLS y confirmación destructiva larga. El fingerprint previo conserva atributos, membresía administrativa exacta, ownership y superficie productiva; el flujo fija el OID, confirma `NOLOGIN PASSWORD NULL` antes de drenar, termina sesiones en barridos acotados y revalida desde una conexión nueva. Es idempotente y reconcilia commits ambiguos aplicados/no aplicados con un único reintento.
- La compensación automática del aprovisionador también se endureció: ya no exige cero sesiones antes de deshabilitar; primero confirma el cambio y después termina cualquier sesión superviviente. La suite API pasó lint, typecheck, 74 pruebas y build. `auditar_recuperacion_app_api` — razonamiento alto, read-only — identificó la diferencia entre expiración y contención, la ventana de sesiones vivas y la necesidad de postcheck fresco; no editó, leyó secretos ni conectó Supabase.
- Pipeline global posterior a recovery: instalación frozen, lint 5/5, typecheck 7/7, tests 7/7 y build 5/5 pasaron; las suites incluyen 100 pruebas de dominio, 57 del spike, 74 de API y contratos runtime/compile-time de shared-types. El CLI recovery sin opt-in rechazó antes de red con JSON allowlisted; `git diff --check` quedó limpio, no quedaron `.test-dist`/scripts temporales y `.env.adr010.local`/CA siguen ignorados. No hubo mutación Supabase ni interacción Git remota.
- Verificador read-only de `app_api` 2026-08-30: se añadió configuración/CLI sin argumentos ni contraseña de aplicación, con correlación exacta de URL/ref/DB/CA/TLS, lock compartido, fingerprint completo y clasificación `safe_disabled|temporary|expired|runtime|partial`. Los estados estables ejecutan la auditoría pinneada correspondiente; los intermedios devuelven atención sin intentar corregirlos. La salida solo incluye estado, auditoría y presencia booleana de sesiones.
- La allowlist administrativa ahora exige endpoint directo o pooler de sesión en 5432 y admite un único `sslrootcert` opcional, no vacío y acotado, además del `sslmode=verify-full` obligatorio. Rechaza 6543 porque los advisory locks son de sesión, así como duplicados y cualquier parámetro adicional; elimina el query completo antes de entregar la URL a `pg`. La CA efectiva sigue cargándose y validándose desde `DATABASE_CA_CERT_PATH`.
- Evidencia remota read-only posterior a la segunda rotación: la conexión mínima confirmó contraseña/TLS y el verificador completo confirmó `{"activeSessions":false,"catalogAudit":true,"state":"safe_disabled","status":"ok"}` en `zwbyiefqeujstyzysydn`. No se habilitó `LOGIN`, no se escribió base/Auth/configuración y no se expuso Data API. El primer intento completo agotó el timeout de conexión y el reintento controlado pasó; los diagnósticos temporales no imprimieron URL, usuario, contraseña ni mensajes nativos y fueron eliminados.
- Verificación local de esta unidad antes del pipeline global: lint, typecheck y 83 pruebas API pasaron. CodeGraph no está disponible entre las herramientas de esta sesión; la inspección manual dirigida localizó cuatro consumidores del parser administrativo (verificación, aprovisionamiento, recovery y estado) y confirmó que los entrypoints operativos no son alcanzables desde controladores.
- Pipeline global previo al último hardening del verificador: instalación frozen offline al día, lint 5/5, typecheck 7/7, tests 7/7 y build 5/5 pasaron; las suites incluían 100 pruebas de dominio, 57 del spike, 81 de API y contratos runtime/compile-time de shared-types. La instalación online solo emitió un aviso no bloqueante al consultar metadata de actualización de pnpm; el modo offline confirmó el lockfile y dependencias locales.
- `auditar_verificador_estado_app_api` — razonamiento alto, read-only. Confirmó el diseño fail-closed y detectó que aceptar el pooler transaccional 6543 era incompatible con el advisory lock ligado a sesión; el agente principal restringió todas las conexiones administrativas a 5432 y añadió la regresión. No editó, leyó secretos ni conectó Supabase; CodeGraph no estaba disponible.
- Hardening posterior a auditoría: el fingerprint y ambas auditorías SQL ahora exigen `rolconnlimit=-1`; los estados estables con sesiones activas devuelven `attention` y preservan `activeSessions=true` sin ejecutar una auditoría que requiere quiescencia. Se añadieron regresiones para 6543, NUL/duplicados TLS, orden lock→fingerprint→sesiones→auditoría, sesiones activas en ambos estados estables y fallo de cierre. La reejecución remota read-only volvió a confirmar `safe_disabled`, auditoría endurecida verde y cero sesiones.
- Pipeline global final de la unidad: instalación frozen offline, lint 5/5, typecheck 7/7, tests 7/7 y build 5/5 pasaron; las suites incluyen 100 pruebas de dominio, 57 del spike, 83 de API y contratos runtime/compile-time de shared-types.
- Cierre de limpieza: `git diff --check` pasó, no quedaron `.test-dist` ni diagnósticos temporales, y Git confirmó que `.env.adr010.local` y `.certs/prod-ca-2021.crt` continúan ignorados. No hubo push, merge, rebase, force-push ni otra escritura Git remota.
- Coordinación frontend 2026-08-30: la primera eliminación de `apps/web` fue una interferencia accidental, porque Claude trabajaba simultáneamente en el mismo checkout. Claude reconstruyó los 23 archivos y sus dependencias; el integrador confirmó que el lockfile conserva también la dependencia backend. `frontend.md` ahora fija una lista blanca obligatoria: Claude solo escribe `apps/web/**`, Codex no toca esa carpeta mientras el slice esté activo y manifests/lockfile raíz pertenecen al integrador.
- Hardening de caché 2026-08-30: las dos rutas autenticadas existentes de Nest declaran `Cache-Control: private, no-store`, con regresión en el smoke para identidad y scope. CodeGraph quedó actualizado en 105 archivos, 2,192 nodos y 8,592 relaciones; cada controller solo alcanza `AppModule` y su prueba, sin consumidores huérfanos.
- Verificación posterior: instalación frozen desde lockfile, lint 5/5, typecheck 7/7, tests 7/7 y build 5/5 pasaron; se mantienen 100 pruebas de dominio, 57 del spike, 83 de API y contratos runtime/compile-time de shared-types. No hubo mutación Supabase ni operación Git remota.
- Preparación neutral de persistencia 2026-08-30: se añadió en `apps/api/src/persistence/**` un códec v1 de `Order`, `OrderAuditEvent` y `OrderMutation`. Conserva scopes, snapshots, cancelaciones y auditoría; representa `Money` sin float y ratios bigint como strings decimales canónicos; rehidrata y congela antes de delegar validación al dominio. Falla cerrado ante versión/campos extra, accessors, proxies, ciclos, prototipos hostiles, números inseguros, ratios no canónicos o divergencia agregado/evento.
- Verificación del códec: lint, typecheck, 87 pruebas API y build pasaron; las 4 pruebas nuevas cubren round-trip JSON completo, evidencia de cancelación, datos hostiles y scope divergente. CodeGraph quedó al día en 125 archivos, 2,386 nodos y 9,349 relaciones; los cuatro entrypoints del códec solo tienen consumidores internos y su prueba, y `OrderMutation` mantiene su dependencia en dominio/API sin UI ni migración.
- Auditoría de siguiente migración: `auditar_siguiente_migracion` — razonamiento alto, read-only — recomendó un futuro core aditivo/deny-by-default de Order, pero bloqueó escribir/aplicar SQL hasta cerrar contratos de DiningTable/ProductionStation, RBAC y prueba PostgreSQL de upgrade. No editó ni accedió a Supabase/secretos.
- Implementación delegada del códec: `implementar_codec_orden` — razonamiento alto — dejó el archivo inicial sin pruebas antes de ser interrumpido; el agente principal auditó, corrigió bigint/correlación de cancelaciones, añadió tests, integró la dependencia y verificó el resultado. `implementar_recuperacion_e2e` no produjo cambios en este corte por límite de uso.
- FE-0 reconstruido: lint, typecheck, 11/11 pruebas de configuración y build Next pasaron. La revisión del integrador detectó que el rechazo remoto llama `signOut()` desde un Server Component cuyo adaptador ignora escrituras; Claude debe mover la invalidación a una frontera mutable, evitar llamadas duplicadas/ciclo de redirects y añadir pruebas funcionales. FE-0 permanece IN_PROGRESS y FE-0.1 BLOCKED; no se hizo smoke humano.
- Integración final de FE-0: Claude movió la revalidación Nest y `signOut()` a `proxy.ts`, copió cookies refrescadas/eliminadas al redirect fijo, retiró la invalidación del layout y añadió siete pruebas del parser UUID y fallos remotos/hostiles. El integrador añadió, sin dependencias nuevas, un loader ESM y doble Supabase exclusivos de test para ejecutar directamente el proxy con `NextRequest`/`NextResponse` reales. Lint, typecheck, 23/23 pruebas y build pasaron; cinco casos demuestran limpieza efectiva, redirect fijo sin ciclo, llamada Nest única y `Cache-Control: private, no-store`. Solo falta smoke humano.
- Directorio de membresías 2026-08-30: `shared-types` añadió el contrato/parser v1 exacto y congelado; Nest añadió `GET /api/v1/access/memberships`, adapter PostgreSQL parametrizado, 503 genérico y `no-store`. La respuesta contiene únicamente scope, nombres y roles activos, admite lista vacía y rechaza respuestas parciales/hostiles. Los roles dejaron de duplicarse entre API y contrato compartido.
- Esquema del directorio: `20260830000200` añade una única función privada `SECURITY DEFINER`, owner `postgres`, `search_path=''`, límite 501 y `EXECUTE` exclusivo de `app_api`. Las auditorías/lifecycle fingerprints permiten exactamente cuatro funciones antes de aplicarla o cinco después. El runner E2E quedó ampliado para dos actores, listado HTTP/privado, role/membership revocation, Branch/Restaurant deshabilitados, token vivo y cache `no-store`.
- CodeGraph posterior quedó actualizado con 129 archivos, 2,459 nodos y 9,627 relaciones. `PostgresMembershipDirectory` solo alcanza controller, módulo y prueba; el catálogo de roles compartido solo alcanza su parser, autorización Nest y pruebas. El cálculo conservador identificó 36 suites transitivamente afectadas; las suites acotadas están verdes y el pipeline global se reserva hasta que Claude deje de escribir FE-0.
- Verificación: shared-types pasó lint/typecheck/test/build; API pasó lint/typecheck, 94 pruebas y build. La migración compiló en `zwbyiefqeujstyzysydn`, pasó auditoría/capability/actor vacío y luego se revirtió; el postcheck confirmó que la función no existe remotamente. No hubo cambio persistente en Supabase ni interacción Git remota.
- Verificador reproducible de `20260830000200`: `schema-verification` admite una expectativa exacta de cuatro o cinco funciones y el nuevo entrypoint fija cinco, aplica solo la segunda migración y siempre revierte. Configuración ausente rechaza antes de red con JSON allowlisted. Lint/typecheck, 97 pruebas API y build pasaron; CodeGraph quedó en 130 archivos, 2,467 nodos y 9,646 relaciones y limita el runner a la frontera administrativa y sus pruebas.
- Estrategia de aplicación: se descartó escribir directamente `supabase_migrations` o usar `db push` desde la raíz. Cuando exista rotación y autorización se repetirá el staging temporal ya probado para `20260830000100`, con las cinco migraciones históricas exactas, ambas productivas, dry-run exclusivo, CLI Supabase y postcheck de siete versiones. Esto conserva una única autoridad de historial sin reparar ni mezclar el spike en el árbol productivo.
- Incidente de credencial: el primer lector ad hoc dejó que una excepción `URL` imprimiera el valor completo de la conexión local. La contraseña administrativa debe rotarse nuevamente; no fue versionada ni copiada a documentación. No realizar más conexiones remotas con ese valor.
- Delegación: `auditar_listado_membresias` — razonamiento alto, read-only — definió el contrato mínimo, amenazas, función privada, allowlist exacta y secuencia de despliegue. El agente principal implementó, integró y verificó. `implementar_recuperacion_e2e` no produjo cambios por límite de uso.
- Delegación de despliegue: `auditar_aplicador_migracion` — razonamiento alto, read-only — identificó hashes, invariantes, riesgo de commit ambiguo y la incompatibilidad del `db push` raíz. El integrador contrastó esa propuesta con la evidencia del staging ya usado y eligió conservar el CLI oficial mediante staging temporal. `implementar_config_aplicador` fue interrumpido antes de editar cuando se descartó el aplicador SQL personalizado.
- CodeGraph posterior a FE-0 quedó en 133 archivos, 2,491 nodos y 9,712 relaciones; `proxy` y `redirectPreservingCookies` solo alcanzan implementación y prueba. `investigar_test_proxy_next` — razonamiento moderado, read-only — reprodujo la incompatibilidad ESM de Node/Next y recomendó el loader acotado que integró el agente principal.
- Coordinación vigente: Claude debe detenerse. FE-0.1 será su siguiente slice previsto únicamente después de aplicar la migración, aprovisionar `app_api`, pasar E2E remota, enlazar `shared-types` a web, publicar este checkpoint y emitir una asignación nueva explícita.
- Emmanuel autorizó aplicar persistentemente `20260830000200`. Dos preflights CLI correctamente invocados conservaron URL/TLS como un solo argumento y se detuvieron con SQLSTATE 28P01 por la contraseña administrativa antes de leer historial o escribir. La contraseña local de `app_api` sí pasó fuerza y separación; no se imprimieron secretos ni hubo mutación remota.
- Pipeline global del checkpoint: `pnpm install --frozen-lockfile`, lint, typecheck, tests y build pasaron en los seis paquetes; Turbo reportó 6/6 lint, 9/9 tareas de tipos, 9/9 tareas de test y 6/6 builds. Web conserva 23/23 pruebas. La máquina local usó Node 22.20.0 y emitió el aviso de engine porque CI/producción fija Node 24.19.0; no produjo fallos.
- Checkpoint Git 2026-08-30: tras confirmar que `HEAD`, `origin/main` y su merge-base eran exactamente `f0091f30324263ad253d5ea395733f0d1f56200a`, se publicó por fast-forward `36946ca002c69931fba35e03bd559f5ecdf2fb60` (`feat: establish phase 0 application foundations`). Incluye 128 archivos del avance acumulado. El staging pasó `git diff --cached --check` y excluyó `.env` reales, certificados, claves, dependencias y artefactos generados. No hubo merge, rebase, force-push ni cambio de `origin/master`.
- Continuación publicada 2026-08-31: el checkout inició limpio y `HEAD`, `origin/main` y merge-base coincidieron exactamente en `ca2e4cb0b2cea33b9387fd0a3947f333b6e6d7b4`; `git fetch origin main` no requirió mover `main`. CodeGraph previo estaba completo y sin pendientes: 133 archivos, 2,491 nodos y 9,712 relaciones.
- Aplicación de `20260830000200`: el staging temporal reunió siete copias byte a byte con hashes coincidentes. El historial previo confirmó seis versiones y solo la segunda productiva pendiente; el dry-run anunció exclusivamente `20260830000200_list_active_branch_memberships.sql`. La aplicación oficial terminó con código 0 y el postcheck confirmó exactamente `20260825000100`–`20260830000200`. No hubo reparación, seed, roles, Vault, configuración, `db push` raíz ni secreto impreso; el staging se eliminó.
- Aprovisionamiento `app_api`: dos intentos iniciales alcanzaron la auditoría runtime y fallaron porque Supavisor recreaba un backend retenido mientras `LOGIN` seguía visible; ambos compensaron y el verificador fresco confirmó `safe_disabled`, catálogo verde y cero sesiones. El flujo se corrigió con un corte quiescente: confirmar `NOLOGIN PASSWORD NULL`, drenar sesiones y reinstalar SCRAM+`infinity`+auditoría dentro de una sola transacción. El aprovisionador final devolvió `status=ok`; dos postchecks administrativos frescos confirmaron `runtime`, auditoría verde y cero sesiones.
- Hardening posterior: las auditorías pinneadas ya no aceptan la superficie antigua de cuatro funciones. Exigen cinco funciones exactas y niegan `EXECUTE` de `anon`, `authenticated` y `service_role` en las capacidades privadas. El drenaje usa `pg_terminate_backend(pid, 5000)` y 14 pruebas del aprovisionador cubren reaparición del pooler, límite agotado, terminación falsa, sesión en el segundo corte y commits ambiguos 2/3. La instalación frozen y el pipeline global pasaron: lint 6/6, typecheck 9/9, tests 9/9 y build 6/6; API conserva 103 pruebas verdes. Node 22.20.0 emitió el aviso esperado frente a Node 24.19.0 fijado.
- `auditar_supavisor_app_api` — razonamiento alto, read-only. Validó el corte quiescente, rechazó allowlists de PID/application name/sesión y enumeró riesgos de crash y regresiones; no editó, usó red ni leyó secretos.
- `revisar_parche_provisionamiento` — razonamiento alto, read-only. Confirmó que drenar bajo `LOGIN` era una carrera, recomendó timeout acotado y detectó que las auditorías todavía aceptaban cuatro funciones; sus hallazgos quedaron integrados. No editó, usó red ni leyó secretos.
- Esquema: cambio aditivo sin rollback destructivo; `20260830000200` permanece aplicado. Offline: sin cambios. Git remoto: solo fetch de `origin/main`; no hubo push, merge, rebase ni force-push. Frontend: Claude continúa detenido y FE-0.1 no fue autorizado.
- Checkpoint backend: `0274e660c0464d34bab2fffe7c576ad459b6d552` se publicó por fast-forward en `origin/main`; el postcheck confirmó checkout limpio y `HEAD=origin/main`. No hubo merge, rebase ni force-push.
- Enlace de contrato: el integrador añadió `@super-restaurant/shared-types` como dependencia `workspace:*` de `apps/web` y actualizó únicamente su importer en `pnpm-lock.yaml`; no inició FE-0.1 ni añadió lógica de scope. Web pasó lint, typecheck, 23/23 pruebas y build. CodeGraph quedó completo, sin pendientes y sin cambio estructural: 133 archivos, 2,491 nodos y 9,744 relaciones.
- Instalación: el primer intento offline quedó detenido durante una reconstrucción de `node_modules` y se canceló sin tocar fuentes. La reinstalación frozen reutilizó 260 paquetes del store, sin descargas, y pasó la política de supply chain; solo persistió el warning de Node 22.20.0 frente a 24.19.0 fijado.
- `auditar_siguiente_e2e` — razonamiento alto, read-only. Confirmó los seis prerrequisitos de FE-0.1, las nueve variables por nombre, la matriz mutativa/cleanup del runner y que `config push` general no es aceptable; no editó, usó red ni leyó valores secretos.
- Configuración Data API dirigida: se añadieron config/runner/CLI y 11 pruebas para GET/PATCH de PostgREST con endpoint fijo, body de un solo campo, estados fuente allowlisted, timeout, idempotencia, drift, respuestas hostiles y reconciliación de PATCH ambiguo. La invocación sin opt-in rechazó antes de red; API pasó lint, typecheck, 114 pruebas y build.
- `implementar_data_api_config` — razonamiento alto. Implementó únicamente los cinco archivos nuevos de la operación y sus pruebas, sin tocar manifests, web, documentación ni red. El integrador auditó, endureció tipos de campos no objetivo e incorporó script, entorno y runbook.
- `revisar_checkpoint_data_api` — razonamiento alto, read-only. Dio GO al checkpoint sin hallazgos P0/P1, confirmó las guardas fail-closed y que no hay credenciales reales; dejó como riesgo menor ampliar la comparación si Supabase incorpora nuevas opciones PostgREST. No editó ni usó red.

## Verificación

Esta sección es un historial acumulado de verificaciones. Las cifras anteriores (por ejemplo, tres migraciones o 24/24 checks) corresponden a cortes intermedios y no sustituyen la evidencia vigente de cinco migraciones y 27/27 checks.

- Verificación vigente 2026-08-31: CodeGraph completo y sin pendientes, 133 archivos, 2,491 nodos y 9,744 relaciones. El impacto de `provisionAppApi` y `verifyAppApiState` queda limitado a cada implementación, runner y prueba.
- Verificación vigente 2026-08-31: `pnpm install --frozen-lockfile`, lint 6/6, typecheck 9/9, tests 9/9 y build 6/6 pasaron con pnpm 11.19.0/Turbo 2.10.12. API suma 103 pruebas; las 14 de aprovisionamiento incluyen las regresiones de pooler y commit ambiguo. Solo apareció el warning de Node 22.20.0 frente al 24.19.0 fijado.
- Verificación remota vigente 2026-08-31: historial exacto de siete migraciones, auditoría de catálogo de cinco funciones y grants cliente denegados; dos postchecks frescos clasificaron `app_api` como `runtime`, con auditoría verde y cero sesiones.
- Verificación local del checkpoint Data API 2026-08-31: instalación frozen, lint 6/6, typecheck 9/9, tests 9/9 y build 6/6 pasaron; API suma 114 pruebas y web conserva 23/23. La invocación sin opt-in rechazó antes de red. CodeGraph quedó completo y sin pendientes en 138 archivos, 2,570 nodos y 9,993 relaciones; `exposeAppDataApi` y `readDataApiExposureConfig` solo alcanzan runner y pruebas. `git diff --check` quedó limpio y el escaneo final encontró únicamente placeholders y tokens sintéticos en los archivos cambiados. Node 22.20.0 mantuvo el aviso esperado frente a 24.19.0 fijado.
- Verificación remota final 2026-08-31: el runner dirigido de Management API cambió y auditó exactamente `public,graphql_public,app`. El primer intento fue rechazado antes de red por un argumento extra; la repetición exacta fue exitosa. La E2E `runId=07f7a91b-f455-4421-a5b8-97bcd39d438d` pasó 97 checks; `fixtureRowsRemoved=true` y `fixtureUsersRemoved=2`. Las credenciales se obtuvieron de almacenes locales y se mantuvieron solo en memoria; no se imprimieron ni versionaron.
- Verificación RBAC 2026-08-31: la reconstrucción local usó Node 24.19.0 y pnpm 11.19.0; el lockfile pasó la política de supply chain y no cambió. Shared-types pasó lint/typecheck/test/build y API pasó lint/typecheck, 117 pruebas y build. La compuerta global pasó lint 6/6, typecheck 9/9, tests 9/9 y build 6/6; web conserva 23/23 pruebas y compiló sin cambios de fuente. La primera prueba adversarial reprodujo un `Proxy` que lanzaba antes del guard, se corrigió moviendo toda la inspección al cierre fail-closed y la repetición quedó verde. CodeGraph quedó al día en 140 archivos, 2,587 nodos y 7,219 relaciones; `rolesGrantPermission` permanece interno a su módulo y `RbacPermissionCode` alcanza solo shared-types, política RBAC, autorización y selección de Branch. `git diff --check` pasó. No se usaron subagentes. No hubo cambio de esquema, offline, Supabase ni Git remoto; no se hizo commit ni push.

- CodeGraph antes y después del cambio: 0 archivos/nodos/relaciones; no existe código fuente local que analizar y `.codegraph/` quedó excluido.
- Se validó el remoto con ls-remote y fetch.
- `git check-ignore -v --no-index` verificó reglas para `node_modules`, `.env.local`, `.codegraph`, `.next`, `dist`, `coverage`, Android y credenciales de firma; las reglas negadas conservan `.env.example` y `.env.production.example`.
- `pnpm install --frozen-lockfile`: exitoso con pnpm 11.19.0.
- `pnpm lint`, `pnpm typecheck`, `pnpm test` y `pnpm build`: exitosos mediante Turborepo 2.10.12; 0 paquetes/tareas porque todavía no se crearon workspaces funcionales.
- CodeGraph posterior al monorepo: 2 YAML indexados (`pnpm-workspace.yaml`, `pnpm-lock.yaml`), 0 nodos y 0 relaciones; no hay consumidores o referencias compartidas que romper.
- No había UI que verificar para estos cambios fundacionales.
- Plan raíz y copia en `docs/`: SHA-256 idéntico `551267BCAF3B9DB69BAC828009BA23D85E7B8413D925BDFF722F4BB620730FD4`.
- Verificación fresca directa por workspace: lint real, typecheck, build y tests exitosos; dominio 13/13 pruebas.
- CodeGraph posterior: 12 archivos, 118 nodos y 210 relaciones; `Money` impacta 12 símbolos internos y `transitionOrderItem` 2 símbolos internos, sin consumidores externos conocidos.
- `.test-dist` no permanece tras pruebas y su regla de ignore fue verificada.
- El sandbox bloqueó procesos hijos `pnpm --recursive` con `EPERM`; se repitieron fuera del sandbox y pasaron. Los intentos previos de pasar `--force` mediante el script raíz reenviaron el argumento a TypeScript y no se usan como evidencia.
- CI: YAML validado con parser temporal, revisión independiente sin hallazgos y CodeGraph reconoce `.github/workflows/ci.yml`; la ejecución real en runner GitHub queda pendiente de publicar una rama autorizada.
- Harness ADR-010: lint, typecheck, build y 15/15 pruebas frescas exitosas; detecta fugas de lectura/escritura, estado parcial/huérfano, duplicados, pérdida/fuga KDS, revocación tardía, migración/restore incompletos, secretos y evidencia insuficiente.
- CodeGraph posterior al harness: 19 archivos, 204 nodos y 521 relaciones; `Adr010Adapter` afecta solo harness/gates/adaptador de referencia, sin dependencias productivas.
- Verificación integrada del 2026-08-26: `pnpm install --frozen-lockfile --yes`, lint, typecheck, test y build exitosos; 13 pruebas de dominio, 15 del harness, 5 preflights A, 9 B y 6 C (48 pruebas Node en total).
- Verificación monetaria posterior del 2026-08-26: 9 pruebas nuevas de totales; suite integrada queda en 22 pruebas de dominio + 35 del spike (57 pruebas Node), con instalación frozen, lint, typecheck y build exitosos.
- Auditoría CodeGraph de C reportó 30 archivos, 327 nodos y 742 relaciones; `SupabaseDirectReadClient` solo afecta 6 símbolos internos y C no consume `Adr010Adapter`. La reconsulta final del agente principal falló tres veces con `Transport closed`; se hizo inspección manual dirigida y la limitación queda registrada.
- CodeGraph del auditor A/B confirmó rutas internas: el runner es el único consumidor del adapter A y del bootstrap B; restore solo alcanza validación allowlist/comparación de referencias, y bootstrap/cleanup solo alcanza descubrimiento server-only por metadata. La reconsulta posterior del agente principal volvió a fallar para status e impactos con `Transport closed`.
- CodeGraph final volvió a estar operativo: 41 archivos, 494 nodos y 1,089 relaciones. `SupabaseAdr010CriticalOrderService` afecta 7 símbolos internos de la frontera Nest; `withAdr010BAuthenticatedFixtures` afecta solo su archivo/runner y no hay consumidores productivos.
- CodeGraph posterior a totales: 43 archivos, 568 nodos y 1,346 relaciones. `OrderItemPriceSnapshot` impacta 13 símbolos, todos internos a `order-totals`; no existen consumidores externos ni referencias productivas que migrar.
- CodeGraph antes y después del contrato offline: 43 archivos, 568 nodos y 1,346 relaciones. Solo encontró cursores/idempotencia internos del spike ADR-010; no existe motor de sync productivo ni consumidores que migrar.
- Documentación offline: fences y tablas Markdown balanceados, referencias ADR verificadas y `git diff --check` sin errores. La validación integrada posterior mantuvo instalación frozen, lint, typecheck, 57 pruebas Node y build exitosos.
- `.test-dist` fue limpiado por las suites y permanece ignorado. No se detectaron archivos `.env` reales ni secretos versionables.
- Supabase CLI 2.109.1 autenticó y enlazó el ref `cxcnnhafchqslvgvkeye`. Las tres migraciones experimentales quedaron aplicadas; la auditoría remota read-only reportó 24/24 checks aprobados para los schemas ADR. Docker no está instalado.
- El security advisor falló por errores preexistentes del schema `public` ajeno al spike (tres tablas sin RLS y una función con `search_path` mutable), evidencia de que el proyecto no es aislado. Por seguridad no se ejecutaron Auth desechable, gates destructivos, reset, concurrencia ni restore.
- El push de configuración que expuso `adr010_b` alteró temporalmente defaults de Auth; la reversión exacta se aplicó y una comparación posterior confirmó API/DB/Auth/Storage al día. No hubo funcionalidad offline ni interacción Git remota.
- La primera reinstalación final quedó sin red dentro del sandbox después de recrear `node_modules`; se interrumpió y se repitió con el acceso aprobado usando exactamente el lockfile. No se descargaron versiones diferentes ni cambió el lockfile.
- Verificación final del 2026-08-27: instalación frozen reproducible, lint, typecheck, 61/61 pruebas Node y build exitosos; no quedaron `.test-dist`, archivos `.env` reales ni metadata Supabase versionada.
- `supabase migration list --linked` confirmó las tres versiones locales/remotas y `supabase db push --linked --dry-run` reportó la base al día. CodeGraph final indexó 43 archivos, 626 nodos y 1,500 relaciones; el impacto de `SupabaseNestAdr010Adapter` permanece contenido en su módulo, sin consumidores productivos.
- Verificación de modificadores del 2026-08-27: pipeline global lint/typecheck/test/build exitoso con 30 pruebas de dominio y 39 del spike (69 pruebas Node). `git diff --check` pasó; no quedaron `.test-dist` ni archivos `.env` reales.
- CodeGraph posterior indexó 45 archivos, 715 nodos y 1,665 relaciones. `ModifierPriceSnapshot` impacta 10 símbolos internos de `order-totals`; `createModifierSelectionSnapshot` solo su propio módulo y no tiene consumidores productivos.
- Verificación global posterior a pagos/caja: lint, typecheck, 46 pruebas de dominio + 39 del spike (85 pruebas Node) y build exitosos; `git diff --check` pasó y no quedaron `.test-dist`.
- CodeGraph final indexó 49 archivos, 915 nodos y 2,173 relaciones. `refundPayment` y `appendCashMovement` solo afectan sus propios módulos; no existen consumidores productivos todavía.
- Verificación remota final B del 2026-08-27: `test:option-b:rls` y `test:option-b:gates` exitosos; 11 capacidades reportadas, `eligibleForAdr010Go: false` deliberado, cleanup final con 0 usuarios/filas operativas temporales.
- Auditoría posterior: 24/24 checks, tres migraciones locales/remotas idénticas, `db push --dry-run` al día y `db lint` sin errores en los schemas ADR.
- Pipeline global posterior: lint, typecheck, 46 pruebas de dominio + 43 del spike (89 pruebas Node) y build exitosos.
- Verificación global del 2026-08-28 posterior a propiedades financieras: lint, typecheck, 69 pruebas de dominio + 56 del spike (125 pruebas Node) y build exitosos; 96 casos Money, 61×4 permutaciones de descuento y 48 secuencias de caja pasaron con semillas fijas; suites sin `.test-dist` residual.

## Subagentes

- `verificar_pipeline` — razonamiento bajo. Ejecutó la instalación/pipeline inicial, reprodujo el fallo limpio y confirmó read-only que la corrección mínima era añadir `^build` a typecheck/test; no editó archivos.
- `auditar_gate7` — razonamiento alto. Auditó seguridad/esquema del runner fresco, sus entradas y brechas; identificó el hardening local y confirmó que al usuario solo debe pedirse la URL pública del segundo proyecto.
- `auditar_gate7` (seguimiento) — razonamiento alto. Revalidó el ref `zwbyiefqeujstyzysydn`, el procedimiento fail-closed, la ventana TOCTOU y los prerrequisitos de autenticación/CA/configuración; no conectó ni editó.
- `auditar_gate7_ejecucion` — razonamiento alto. Auditó de forma independiente enlace, cinco migraciones, preflight, dry-run/apply, postcheck y riesgos de push parcial; no editó ni conectó servicios. Su checklist condicionó el apply a la vaciedad, orden exacto y revalidación que finalmente pasaron.
- `auditar_gate4_final` — razonamiento alto. Auditoría independiente y read-only de DTOs, Auth, puertos, SQL privado, permisos y posibles bypasses; sus conclusiones se integran solo como evidencia de apoyo y no como aceptación humana.
- `proponer_scoring_b` — gpt-5.6-sol, razonamiento alto. Auditó read-only los seis criterios y calculó la recomendación condicional conservadora de 75/100 antes de la aceptación de Gate 4; tras la aprobación humana, el agente principal la registró como score oficial. No editó ni accedió a servicios remotos.
- `auditar_menu_domain` — gpt-5.6-sol, razonamiento moderado. Auditó read-only los contratos de modificadores, snapshots y Order, confirmó que no existía un catálogo duplicado y delimitó el slice neutral; el agente principal implementó, integró y verificó el resultado.
- `verificar_codegraph_cert` — razonamiento bajo. Comprobó disponibilidad inicial de CodeGraph, mapeó manualmente el runner y confirmó que no había certificado visible; no editó ni conectó servicios remotos.

- implementar_gitignore — modelo gpt-5.6-luna, razonamiento bajo.
- Alcance: implementación mecánica del `.gitignore` raíz y verificación inicial; modificó solo `.gitignore`.
- revisar_gitignore — modelo gpt-5.6-luna, razonamiento bajo.
- Alcance: revisión independiente y corrección acotada de artefactos móviles, credenciales de firma y logs; modificó solo `.gitignore`.
- implementar_monorepo — modelo gpt-5.6-terra, razonamiento moderado.
- Alcance: configuración raíz pnpm/Turborepo y verificación inicial; creó `package.json`, `pnpm-workspace.yaml` y `turbo.json`.
- revisar_monorepo_turbo — modelo gpt-5.6-terra, razonamiento moderado.
- Alcance: cerró la integración real con Turborepo, fijó `turbo@2.10.12`, generó `pnpm-lock.yaml` y verificó los cuatro pipelines.
- documentar_greenfield — modelo gpt-5.6-luna, razonamiento bajo.
- Alcance: README y copia byte a byte del plan en `docs/`.
- crear_config_tipos — modelo gpt-5.6-terra, razonamiento moderado.
- Alcance: presets TypeScript, shared-types neutrales, pruebas de tipos y lockfile.
- implementar_dominio_pos — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: Money, errores, máquinas de estados y pruebas unitarias iniciales.
- auditar_dominio_pos — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: auditoría financiera/estados, evidencia inmutable de cancelación y ampliación a 13 pruebas.
- configurar_eslint — modelo gpt-5.6-terra, razonamiento moderado.
- Alcance: ESLint real, regla de imports prohibidos y ajuste TypeScript 7.0.2 a 6.0.3 por compatibilidad verificada.
- implementar_ci — modelo gpt-5.6-terra, razonamiento moderado.
- Alcance: workflow GitHub Actions, verificación de versiones oficiales, parser YAML y pipeline local.
- revisar_ci — modelo gpt-5.6-luna, razonamiento bajo.
- Alcance: revisión independiente de permisos, eventos, caché, reproducibilidad y ausencia de secretos.
- crear_harness_adr010 — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: harness Día 1, fixtures, contrato, adaptador de referencia, ADR/README y pruebas negativas iniciales.
- auditar_harness_adr010 — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: reforzó gates contra falsos verdes, separó inspección humana de frontera y actualizó claves Supabase a publishable/secret.
- scaffold_opcion_b — modelo heredado, razonamiento moderado.
- Alcance: inició la opción B; alcanzó límite de uso tras dejar una implementación parcial, que fue inspeccionada e integrada por el agente principal.
- auditar_opcion_b — modelo heredado, razonamiento alto.
- Alcance: auditó seguridad/tenancy de B, separó configuración pública/servidor, endureció SQL/RPC/Auth FKs y dejó explícitas las brechas no probadas.
- scaffold_opcion_c — modelo heredado, razonamiento moderado.
- Alcance: implementó el cliente público de solo lectura, hardening SQL, runner opt-in y preflights de C.
- auditar_opcion_c — modelo heredado, razonamiento alto.
- Alcance: encapsuló el SDK, endureció claves/cursor/scope/revocaciones y confirmó que C no puede ser frontera de escritura crítica.
- implementar_bootstrap_b — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: Admin API server-only, usuarios desechables, memberships deterministas, cleanup FK-safe y preflights B; no ejecutó remoto.
- scaffold_opcion_a — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: adapter PostgreSQL A, migración thin-slice, frontera Nest, runner opt-in, dependencias `pg` y preflights; no pudo ejecutar PostgreSQL por falta de infraestructura.
- auditar_adr010_a_b — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: auditoría cruzada de persistencia/tenancy/Auth; bloqueó identificadores controlados por backup, conflictos silenciosos y usuarios Auth huérfanos, añadió regresiones y mantuvo NO-GO explícito.
- implementar_principal_jwt_b — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: inició el principal JWT B y dejó cambios parciales antes de agotar cuota; el orquestador los inspeccionó y reasignó.
- preparar_probe_rls_b — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: inició fixtures autenticados/probe RLS; sus cambios parciales se integraron en la recuperación posterior.
- completar_auth_rls_b — modelo gpt-5.6-luna, razonamiento alto.
- Alcance: recuperó cambios parciales, cerró principal verificado, probe RLS, scripts, pruebas y documentación sin ejecutar remoto.
- privatizar_rpc_b — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: movió funciones privilegiadas a schema privado y cambió escritura/bootstrap a PostgreSQL server-only conforme a guías oficiales.
- auditar_auth_rls_b — modelo gpt-5.6-luna, razonamiento alto.
- Alcance: corrigió cleanup parcial/concurrente con run IDs, endureció ACL privadas y verificó 9 preflights B.
- redactar_adr007_monetario — modelo gpt-5.6-luna, razonamiento alto.
- Alcance: ADR-007 neutral, orden de cálculo, redondeo, snapshots, ejemplos y bloqueos fiscales; no modificó código.
- implementar_totales_dominio — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: snapshots y cálculo puro de líneas/orden, impuestos, descuentos, propina, prorrateo v1 y pruebas iniciales.
- auditar_totales_monetarios — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: eliminó efectos laterales, completó versiones/estación/unidad, estabilizó desempate, alineó ADR y añadió regresiones financieras.
- redactar_contrato_sync — modelo gpt-5.6-luna, razonamiento alto.
- Alcance: especificó topología, envelope, push/pull, outbox, deduplicación, versiones, tombstones, seguridad y pruebas sin implementar almacenamiento ni motor de sync.
- matriz_offline_conflictos — modelo gpt-5.6-luna, razonamiento alto.
- Alcance: definió capacidades por cliente/conectividad, conflictos por entidad, mensajes UI y failure injection con política financiera conservadora.
- auditar_contrato_offline — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: auditó causalidad, idempotencia, cursores, revocación, dinero/KDS y corrigió ambos documentos contra doble efecto, pérdida de pull y falsas garantías offline.
- crear_adr004 — modelo gpt-5.6-luna, razonamiento bajo.
- Alcance: creó el ADR breve de topología y enlazó las especificaciones auditadas; no duplicó contratos ni prometió implementación.
- auditar_plan_pos — modelo gpt-5.6-terra, razonamiento moderado.
- Alcance: auditoría independiente de contradicciones, decisiones, secciones faltantes y criterios de aceptación.
- No editó archivos. Sus hallazgos se integraron en el plan v2.
- revisar_plan_claude — modelo gpt-5.6-terra, razonamiento moderado.
- Alcance: revisión independiente de las aclaraciones añadidas por Claude. No editó archivos; sus hallazgos se integraron en el plan v2.1.
- completar_adapter_b — modelo heredado, razonamiento alto.
- Alcance: implementó el adapter B con Auth real, frontera Nest/PostgreSQL privada, lecturas RLS, migración/ACL checks, backup/restore, cleanup y runner opt-in; mantuvo `eligibleForAdr010Go: false`.
- preparar_migraciones_b_c — modelo heredado, razonamiento alto.
- Alcance: versionó el hardening C como migración, completó configuración Supabase, ignores, preflights y documentación de rollback.
- auditar_adapter_migraciones_b — modelo heredado, razonamiento alto.
- Alcance: auditó seguridad/tenancy/restauración; ligó idempotencia al actor/scope/payload, endureció backup/restore, cleanup, guards de ref/TLS y ACL de bootstrap.
- crear_auditoria_sql_remota_b — modelo heredado, razonamiento alto.
- Alcance: creó la auditoría SQL estrictamente read-only y la migración de hardening RLS/ACL para `bootstrap_users`; corrigió aliases SQL y añadió regresión local.
- actualizar_documentacion_final — modelo heredado, razonamiento moderado.
- Alcance: consolidó TODO/PROJECT_NOTES/HANDOFF con la evidencia remota, el incidente restaurado y el requisito de repetir gates en un proyecto aislado; no ejecutó acciones remotas.
- implementar_modificadores_dominio — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: implementó catálogo y selección pura de modificadores, snapshots históricos compatibles, errores de dominio, exports y pruebas unitarias.
- auditar_diseno_modificadores — modelo gpt-5.6-luna, razonamiento alto.
- Alcance: definió invariantes/casos límite y auditó el cambio real; corrigió entradas no-string para producir errores de dominio deterministas y añadió regresión.
- implementar_payment_dominio — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: implementó Payment/Refund inmutables, estados, idempotencia, scope, evidencia y pruebas.
- auditar_payment_dominio — modelo gpt-5.6-luna, razonamiento alto.
- Alcance: corrigió refunds parciales/total y conflictos idempotentes de scope/moneda; añadió regresiones.
- disenar_cash_register — modelo gpt-5.6-luna, razonamiento alto; interrumpido tras dejar una implementación funcional para limitar el tiempo de la subtarea.
- Alcance: diseñó e implementó la primera versión pura de CashRegister/CashMovement y pruebas.
- auditar_cash_register — modelo gpt-5.6-luna, razonamiento alto.
- Alcance: endureció cursor global por dispositivo, compensaciones acumuladas, evidencia/scope, integridad y saldo contado; amplió pruebas.
- inspect_gate_runner — modelo gpt-5.6-luna, razonamiento bajo.
- Alcance: revisión read-only del runner; confirmó que código 0 representa éxito técnico aunque `eligibleForAdr010Go` sea falso y enumeró brechas deliberadas.
- fix_auth_cleanup_json — modelo gpt-5.6-terra, razonamiento moderado.
- Alcance: corrigió serialización JSONB en bootstrap y dos rutas de cleanup, mapeo snake_case, fixtures del probe RLS y diagnóstico PostgreSQL redactado; añadió cuatro regresiones y validó 43 pruebas del spike.
- auditar_scaffold_next_web — modelo heredado, razonamiento moderado.
- Alcance: auditó de forma read-only los límites de un futuro workspace Next y confirmó que puede avanzar aislado usando los contratos existentes; CodeGraph reportó 90 archivos, 1,949 nodos y 7,744 relaciones.
- implementar_frontend_auth — modelo heredado, razonamiento moderado; interrumpido por corrección de alcance humano.
- Alcance: redactó `frontend.md`. El scaffold que alcanzó a preparar se eliminó por completo porque la solicitud autorizaba solo el plan, no la implementación; no tocó Supabase ni secretos.

## Nota de seguridad

No se publicaron secretos ni archivos. Las claves se mantuvieron en memoria y la contraseña permaneció en `.env.adr010.local` ignorado. Los gates crearon únicamente identidades/datos marcados y desechables en el proyecto aislado; el cleanup final dejó 0 usuarios y 0 filas operativas temporales. No hubo interacción Git remota ni cambios offline.

En la sesión 2026-08-29 se clonó y actualizó `main` por HTTPS hasta `f0091f30324263ad253d5ea395733f0d1f56200a`; no hubo push, merge, rebase ni force-push. No existe `.env` real versionado. El usuario añadió después `.certs/prod-ca-2021.crt`, que permanece local e ignorado y se usó en las verificaciones remotas documentadas.
