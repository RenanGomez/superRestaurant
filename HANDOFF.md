# HANDOFF

Última actualización: 2026-08-31

## Estado actual

El plan v2.3 está en implementación. Emmanuel aprobó ADR-010 opción B; ADR-001 registra sus límites. `apps/api` integra Auth Supabase fail-closed, guard global, conexión PostgreSQL TLS restringida al rol mínimo `app_api`, selección exacta y el directorio versionado `GET /api/v1/access/memberships`. `20260830000100` y `20260830000200` están aplicadas persistentemente en `zwbyiefqeujstyzysydn`; el historial remoto contiene exactamente siete versiones. La auditoría exige cinco funciones privadas/RLS exactas y ausencia de grants cliente. `app_api` quedó aprovisionado como `LOGIN` con SCRAM, vigencia infinita, mínimos privilegios, auditoría runtime verde y cero sesiones. Auth/E2E siguen IN_PROGRESS a la espera de autorización separada para exponer únicamente `app` en Data API y ejecutar la matriz remota. FE-0 cerró implementación y pruebas automatizadas con 23/23 casos; solo falta smoke humano. Claude no tiene ownership de escritura todavía y FE-0.1 sigue bloqueado por E2E, enlace `shared-types`, checkpoint publicado y asignación explícita.

## Trabajo realizado

Las viñetas de esta sección conservan la evolución cronológica; cuando una cifra histórica difiere, prevalecen el estado actual y las entradas posteriores.

- Git inicializado en la carpeta local.
- origin configurado por HTTPS hacia RenanGomez/superRestaurant.
- fetch exitoso de origin/main y origin/master.
- AGENTS.md reemplazado por reglas específicas de superRestaurant.
- PLAN_MODERNIZACION_POS_RESTAURANTE.md actualizado a versión 2.2.
- TODO.md y PROJECT_NOTES.md actualizados.
- `.gitignore` raíz creado para dependencias, secretos, builds, cobertura, caches, CodeGraph, artefactos móviles y credenciales de firma; las plantillas `.env*.example` siguen siendo versionables.
- Monorepo raíz inicializado con pnpm 11.19.0, Turborepo 2.10.12, workspaces `apps/*`/`packages/*`, scripts orquestados y lockfile reproducible.
- README greenfield/API-first creado y el plan maestro copiado byte a byte en `docs/`.
- `packages/config`, `packages/shared-types` y `packages/domain` creados con ESLint/TypeScript compartidos, contratos tenant, Money, estados Order/OrderItem y pruebas.
- Reglas puras de modificadores añadidas en `packages/domain`: validan catálogo/grupo/producto, actividad, min/max por suma de cantidades, caps por opción, duplicados, moneda y precios; generan snapshots canónicos profundamente inmutables con identidad de grupo compatibles con totales históricos.
- ADR-007 propuesto y `packages/domain/order-totals.ts` implementado: snapshots financieros versionados, impuestos incluidos/excluidos, descuentos de línea/orden, propina, cálculo exacto e inmutabilidad profunda.
- ADR-004 propuesto y acompañado por contrato offline/sync y matriz de capacidades/conflictos auditados. Define protocolo, outbox, deduplicación, causalidad limitada, cursores pull, tombstones, autorización y políticas conservadoras para dinero/KDS sin implementar todavía Fase 3.
- `.github/workflows/ci.yml` creado con instalación frozen, lint, typecheck, tests y build, permisos mínimos y concurrencia por ref.
- `frontend.md` comenzó como plan coordinado FE-0–FE-7. Posteriormente Emmanuel autorizó FE-0 y Claude reconstruyó `apps/web`; el protocolo activo limita a Claude a FE-0 dentro de `apps/web/**` y reserva backend, esquema, contratos, lockfile y documentos operativos al integrador.
- Harness ADR-010 creado en `spikes/adr-010`, con ADR propuesta, fixtures 2×2, contrato de adapters, control en memoria y gates comunes reforzados.
- Opción A preparada en `spikes/adr-010/options/a-own-stack`: adapter PostgreSQL que implementa el contrato, migración aislada, frontera Nest, runner opt-in y restore lógico endurecido. El runner sale con código 2/NO-GO incluso si pasan checks PostgreSQL porque no existe Auth real.
- Opción B completada localmente en `spikes/adr-010/options/b-supabase-nest`: implementa `Adr010Adapter`; Nest verifica sesiones reales con Supabase Auth, deriva el actor y delega una única escritura crítica por PostgreSQL privado. Idempotencia, cleanup, guards de proyecto/TLS y backup/restore lógico fueron endurecidos. El runner permanece opt-in y no puede declarar GO.
- La migración B separa `adr010_b` expuesto para lecturas RLS de `adr010_b_private`, no expuesto, donde viven todas las funciones privilegiadas sin grants Data API.
- Bootstrap Auth B server-only añadido: crea dos usuarios desechables por ejecución, memberships 2×2 y cleanup FK-safe/concurrente/reintentable; `app_metadata` con run ID permite redescubrir usuarios si falla antes del marcador SQL. No imprime credenciales ni prueba el gate Auth.
- Probe RLS B ejecutado: dos sesiones publishable independientes demostraron lecturas propias, filtrado cross-scope y revocación efectiva en el siguiente read; el runner prepara fixtures estructurales y siempre cierra/limpia.
- Opción C preparada en `spikes/adr-010/options/c-supabase-direct`: cliente público encapsulado, solo lecturas scoped de órdenes/KDS, hardening SQL contra escrituras y preflights no probatorios. No contiene mutaciones críticas ni consume `Adr010Adapter`.
- El proyecto Supabase proporcionado `cxcnnhafchqslvgvkeye` fue enlazado. Se aplicaron `20260825000100_adr010_b_thin_slice.sql`, `20260826000100_adr010_c_read_only_hardening.sql` y `20260827000100_adr010_b_bootstrap_rls_hardening.sql`; el rollback está documentado, pero no se ejecutó.
- La auditoría remota read-only de catálogo/historial, acotada a `adr010_b` y `adr010_b_private`, pasó 24/24 checks. No inspecciona ni valida tablas ajenas del schema `public`.
- Al exponer `adr010_b`, un primer push de configuración materializó defaults no deseados de Auth. Se restauraron inmediatamente los valores remotos previos y la verificación posterior confirmó API, DB, Auth y Storage al día; `adr010_b` es la única diferencia intencional y el schema privado nunca fue expuesto.
- No se crearon usuarios Auth ni se ejecutaron bootstrap, probe, gates comunes, concurrencia, reset o restore contra el proyecto proporcionado.
- El proyecto nuevo `ndblkcmdgpxsxylacutx` fue confirmado vacío (0 relaciones de usuario/Auth/Storage/migraciones), recibió las tres migraciones ADR y configuró únicamente la exposición adicional de `adr010_b`; Auth/DB/Storage permanecieron sin cambios.
- Su auditoría estructural pasó 24/24 y el security advisor oficial quedó sin hallazgos. La contraseña restablecida permitió conectar por el pooler; las llaves publishable/secret se obtuvieron solo en memoria y `.env.adr010.local` siguió ignorado.
- La ejecución remota reveló y permitió corregir contratos JSONB, nombres snake_case, preparación de fixtures y cleanup duplicado. El probe RLS y las compuertas completas finalizaron con código 0. El reporte verificó 11 capacidades y mantuvo deliberadamente `eligibleForAdr010Go: false` por brechas conocidas.
- La comprobación final quedó en 0 usuarios marcados, 0 bootstrap rows, memberships, órdenes, líneas, snapshots, idempotencia, auditoría y eventos KDS. Permanecen 2 restaurantes y 4 sucursales deterministas del spike. No quedaron identidades ni datos operativos temporales.
- Limitación TLS: el pooler funcionó con cifrado requerido y compatibilidad libpq, pero sin CA del proyecto; no cuenta como evidencia de `verify-full` para producción.
- Payment/Refund y CashRegister/CashMovement puros quedaron implementados y auditados: estados/idempotencia/scope, refunds parciales y totales, cierre con diferencia preservada, compensaciones acumuladas y cursor global por dispositivo.
- La opción B recibió `20260828000100_adr010_b_financial_write_boundary.sql` y `20260829000100_adr010_b_financial_conflict_fix.sql`. La frontera Nest/Auth/PostgreSQL privada crea pagos y refunds de efectivo con idempotencia, supervisor verificado, movimientos inmutables, auditoría y cursor global por dispositivo.
- La primera auditoría remota descubrió una expectativa de FK demasiado débil en el auditor y `db lint` detectó objetivos `ON CONFLICT` incompatibles. Se corrigieron el auditor y una nueva migración versionada; no se editó la migración ya aplicada. La auditoría final pasó 27/27 y `db lint` quedó sin hallazgos.
- `test:option-b:gates` verificó 14 capacidades remotas y `test:option-b:rls` volvió a pasar. La limpieza final confirmó 0 usuarios Auth y 0 filas operativas/financieras/cursor; quedaron solo 2 restaurantes y 4 sucursales estructurales.
- El gate Auth B conserva refresh tokens únicamente en el mapa privado del adapter, deshabilita auto-refresh/persistencia, rota explícitamente con el SDK, valida identidad y `token_type`, reemplaza access+refresh de forma atómica, prueba una escritura crítica con el nuevo access token y exige rechazo del refresh vigente tras `signOut(..., "global")`. El reporte solo emite seis booleanos. La ejecución remota pasó y elevó la evidencia B a 15 capacidades; la limpieza posterior volvió a confirmar 0 usuarios y 0 datos.
- El intento TLS `verify-full` sin CA falló con `SELF_SIGNED_CERT_IN_CHAIN`. El usuario descargó `prod-ca-2021.crt`; se validó como `Supabase Root 2021 CA`, la conexión obtuvo certificado/hostname autorizados y el runner completo volvió a pasar usando `sslmode=verify-full&sslrootcert=...`. Por instrucción expresa no se volverá a controlar su navegador.
- Se añadió `B_SCORING.md`: gates 1,2,3,5,6,8,9,10 demostrados; gate 4 pendiente de humano y gate 7 parcial hasta ejecutar las cinco migraciones completas en otro proyecto fresco/CI. No asigna score antes de elegibilidad.
- Se añadió un runner fail-closed para proyecto remoto fresco. Verifica enlace CLI al mismo project ref, ausencia total de migraciones/usuarios Auth/Storage/relaciones de usuario, hace dry-run, repite checks anti-TOCTOU y solo aplica con un segundo opt-in. No crea ni elimina proyectos/cuentas.
- `packages/domain/src/order.ts` añadió el agregado puro `Order`/`OrderItem`: canal/mesa, scope explícito por línea, snapshots inmutables, altas, transiciones, cancelación total auditada y totales compartidos. La auditoría corrigió bypasses de líneas KDS, mutaciones después del cierre, total de orden cancelada y accessors alternantes.
- `packages/domain/src/order-audit.ts` añadió eventos inmutables schema v1 para creación, alta de línea y transiciones de Order/OrderItem. Todas las mutaciones del agregado requieren contexto y devuelven la Order junto con su evento; scope, entidad y transición no se aceptan del caller. Toda línea cancelada conserva evidencia enlazada, pending exige motivo y post-KDS también supervisor. La creación y rehidratación fallan cerradas ante accessors, proxies, prototipos, timestamps no canónicos, evidencia ausente u origen imposible.
- Payment/CashRegister ahora crean snapshots estables y revalidan agregados/historias rehidratadas, incluidos refunds, secuencias, referencias, compensaciones acumuladas y saldos de cierre. Transición Payment y cierre CashRegister exigen `eventId`/`idempotencyKey`, soportan replay determinista y detectan conflicto. Money/TaxSnapshot también rechazan formas runtime inválidas mediante errores de dominio.
- El pipeline inicial de esta sesión reprodujo un fallo limpio: el spike resolvía `@super-restaurant/shared-types` desde `dist` antes de que Turbo lo construyera. `typecheck` y `test` ahora dependen también de `^build`; una repetición sin `dist`, caché ni `tsbuildinfo` previos pasó y conserva el contrato público del paquete.
- El fresh-push de Gate 7 separa la configuración exclusiva de migraciones de la configuración Auth: requiere URL pública, conexión PostgreSQL privada y confirmación exacta, pero no una secret key. La frescura ahora incluye relaciones, rutinas y tipos de aplicación inesperados, y el postcheck rechaza cualquier historial distinto de las cinco migraciones ADR exactas.
- `.gitignore` excluye `.crt`, `.cer` y `.pem`. `.certs/prod-ca-2021.crt` existe localmente, fue validado para TLS `verify-full`, permanece ignorado y no se versionó ni imprimió.
- El fresh-push resuelve Supabase CLI mediante `npx`/`npx.cmd` con la versión exacta `2.109.1`; no depende de instalación global. La ruta Windows fue comprobada con `--version` y no amplía los comandos permitidos por el runner.
- FE-0 2026-08-30: antes de escribir código se publicó un mockup (Claude Design) con tres direcciones visuales para login/shell; Emmanuel aprobó "Operational Crisp" (Sora + IBM Plex Sans, acentos oklch azul-teal) y descartó las otras dos. Con esa dirección y autorización explícita de iniciar FE-0, se creó `apps/web` (Next.js 16.3.3 App Router, React 19.2.8, Tailwind CSS 4.3.3, `@supabase/ssr` 0.12.5 — versiones verificadas contra el registro npm, no de memoria): `/login` con Server Action de contraseña y error genérico, `/app` protegido que exige tanto `supabase.auth.getUser()` como una revalidación server-to-server contra `GET /api/v1/session` en `apps/api` (cualquiera de los dos que falle cierra sesión y redirige), `src/proxy.ts` (Next.js 16 renombró `middleware.ts` a `proxy.ts`) que refresca la sesión y fuerza `Cache-Control: private, no-store` en `/app/*`, y `src/env.ts` fail-closed para `SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY`/`API_BASE_URL`/`WEB_ORIGIN` (ninguna `NEXT_PUBLIC_`). No hay cliente Supabase de navegador, recuperación de contraseña, signup ni redirect con destino dinámico en ningún archivo. `apps/web` no depende de `packages/domain` ni `packages/shared-types` en esta unidad (FE-0 no maneja scope de Restaurant/Branch todavía).
- Verificación FE-0: `corepack pnpm --filter @super-restaurant/web` pasó `lint`, `typecheck`, `test` (7/7 en `env.test.ts`) y `build` (`next build` con Turbopack) en verde. El `pnpm lint/typecheck/test/build` agregado por Turbo en la raíz no pudo ejecutarse porque este entorno solo expone `pnpm` a través de `corepack` (no en el PATH), y Turbo necesita invocar el binario directamente; el mismo síntoma se reprodujo contra `packages/domain` ya existente, confirmando que es una limitación del entorno y no algo introducido por esta unidad. La instalación corrió con Node 22.20.0 local (`engines` fija `24.19.0`); pnpm solo emitió una advertencia, no bloqueó la instalación. No se ejecutó smoke humano en navegador (no hay `apps/api`/Supabase reales conectados en esta sesión); permanece IN_PROGRESS por esa razón, no REVIEW. CodeGraph no está disponible como herramienta en esta sesión; la inspección manual dirigida confirmó que ningún archivo fuera de `apps/web/**` cambió y que ningún paquete existente depende de `apps/web`.

## Git remoto

- SSH: la huella de GitHub fue aceptada, pero la autenticación falló con Permission denied (publickey).
- HTTPS: lectura y fetch verificados.
- Push: no ejecutado y autenticación de escritura no verificada.
- origin/main y origin/master contienen 10,427 rutas de node_modules.
- origin/main además contiene un prototipo legado.
- No se hizo merge, checkout, reset, rebase ni push; los documentos locales no fueron sobrescritos.
- Auditoría read-only de licencia: `origin/main@293a1551` y `origin/master@f87d5b25` no declaran licencia para código propio; `package.json` tampoco tiene campo `license`. No se reutilizó código del prototipo y queda bloqueado hasta autorización/licenciamiento escrito del titular.

## Cambios documentales

### AGENTS.md

Se eliminó todo el contexto ATS/candidatos/evaluaciones. La nueva versión regula arquitectura, dominio POS, dinero, pagos, tenancy, offline, seguridad, pruebas, UI, observabilidad, Git y delegación.

### Plan maestro

La versión 2.2:

- define piloto online y v1 offline;
- fija stack greenfield TypeScript;
- añade invariantes financieras;
- especifica sync causal/idempotente;
- separa offline por dispositivo de servidor LAN;
- añade seguridad, backups y observabilidad desde Fase 0;
- incorpora criterios de aceptación, ADRs, riesgos y estrategia Git.
- convierte ADR-010 en puerta bloqueante con tres opciones y un spike verificable;
- define `card_manual` sin confundirlo con cobro integrado;
- corrige información y licencias de proyectos de referencia.
- limita el spike a 4 días hábiles, con hard stop al quinto;
- define gates comunes, scoring ≥75/100 y GO/NO-GO por opción;
- autoriza packages/domain como track paralelo independiente.

## Bloqueos

- **Estrategia Git del prototipo:** ADR-006 fue aceptada por el humano. El greenfield usa la raíz `ceaffcb`; el merge `ours` `f9f4428` conserva la historia anterior como segundo padre sin incorporar su árbol. No se reutiliza código legado sin licencia/autorización escrita.
- **Publicación Git:** HTTPS quedó verificado. `main` y `codex/modernizacion-fase0` publican el mismo árbol moderno; `main` avanzó desde `293a155` a `f9f4428` sin force-push y `origin/master` no cambió.
- Mercado/CFDI/hardware no bloquean toda Fase 0, pero sí sus módulos.
- **Regla fiscal definitiva:** ADR-007 solo resuelve invariantes técnicas. País, moneda inicial, tasas, exenciones, tratamiento fiscal de propina y CFDI requieren decisión humana y asesoría fiscal antes de producción.
- **Decisión final de ADR-010 B:** Emmanuel aceptó expresamente B el 2026-08-29. Los diez gates y el score 75/100 sustentan la decisión. NestJS es la única frontera crítica; Supabase gestiona Auth/PostgreSQL y Storage cuando un módulo lo requiera. ORM, cola, transporte Realtime, despliegue y schema productivo no fueron decididos por el spike. La recuperación física es evidencia operativa separada. El proyecto anterior `cxcnnhafchqslvgvkeye` sigue siendo ajeno/no aislado y no debe usarse para gates destructivos.
- **Evidencia de ADR-010 A:** no hay Docker, PostgreSQL, Redis ni distribución WSL disponible en este equipo.
- **Implementación offline:** ADR-004 y sus especificaciones están en REVIEW, y ADR-005 (almacenamiento local) sigue pendiente. No afirmar que Web, Mobile o KDS funcionan offline.
- **Persistencia futura de auditoría:** el dominio no autentica actores ni demuestra unicidad o atomicidad de almacenamiento. La frontera seleccionada por ADR-010 deberá verificar RBAC, asignar `receivedAt`, comparar evidencia rehidratada con eventos, aplicar unicidad a `eventId`/idempotency key y guardar Order+evento en una sola transacción.
- **Credencial administrativa expuesta en salida técnica:** incidente histórico cerrado mediante rotación confirmada. La credencial vigente autenticó con TLS `verify-full`; no fue impresa, versionada ni copiada a documentación.
- **Despliegue del directorio:** `20260830000200` ya está aplicada y auditada. El bloqueo restante es operativo: exponer solo `app` en Data API requiere autorización humana separada antes de la E2E destructiva/controlada.

## Siguiente acción recomendada

1. Solicitar autorización separada para exponer únicamente `app` en Data API; no ejecutar `config push` general ni alterar Auth.
2. Cargar publishable/secret solo en memoria y correr la matriz E2E 2×2 ampliada con listado, revocación, deshabilitación y cleanup/recovery.
3. Enlazar `@super-restaurant/shared-types` a `apps/web`, ejecutar el pipeline global y publicar un checkpoint Git por fast-forward si Emmanuel lo autoriza.
4. Cuando backend remoto, contrato y checkpoint estén verdes, avisar a Emmanuel y emitir una asignación explícita de FE-0.1 a Claude limitada a `apps/web/**`.
5. Repetir Gate 4 con controladores críticos/bundles reales y mantener recuperación física/RPO/RTO/observabilidad en el track preproducción.

## Subagentes y verificación de la frontera financiera B

- `auditoria_final_finanzas_b` (razonamiento alto): auditó FKs, orden de gates y representación `bigint`; implementó correcciones acotadas en auditoría/preflight/gates.
- `completar_parcial_finanzas_b` (razonamiento alto): implementó restauración del cursor y la quinta migración correctiva, más auditoría, preflight y documentación técnica.
- `corregir_close_b` (razonamiento moderado): hizo `close()` reintentable y conservó errores múltiples de cleanup.
- Verificación exacta acumulada posterior a la integración: lint, typecheck, 77 pruebas de dominio + 57 del spike (134 pruebas Node), contrato runtime + prueba compile-time de shared-types y build exitosos; última cobertura medida antes de añadir las propiedades/adversariales recientes: 96.26% líneas, 93.00% ramas y 98.19% funciones; `supabase db push --dry-run`; cinco migraciones aplicadas; auditoría 27/27; `supabase db lint` sin errores; 15 evidencias remotas; probe RLS; runner completo repetido con TLS `verify-full`; limpieza final 0 usuarios y 0 filas operativas.
- CodeGraph se consultó antes y después; el impacto de la frontera financiera queda contenido en el adapter/Nest/gates del spike y sus funciones SQL privadas. Rollback: las migraciones son aditivas; no se ejecutó rollback porque la evidencia y limpieza quedaron verdes.
- `implementar_agregado_order` (gpt-5.6-terra, razonamiento moderado): implementó y corrigió el agregado Order/OrderItem; `endurecer_agregados_financieros` (gpt-5.6-sol, razonamiento alto): cerró integridad de Payment/CashRegister; `validar_money_tax_runtime` (gpt-5.6-terra, razonamiento moderado): cerró validaciones Money/TaxSnapshot.
- `corregir_invariantes_order` (gpt-5.6-terra, razonamiento alto): corrigió cancelación total, terminalidad y accessors; `auditar_finanzas_reintento` (gpt-5.6-terra, razonamiento alto): reprodujo TOCTOU y añadió snapshots/idempotencia financiera. Una primera auditoría financiera independiente falló por límite de uso y no se usó como evidencia.
- `verificar_licencia_legado` (gpt-5.6-luna, razonamiento bajo): verificó refs/commits y ausencia de licencia del prototipo; `auditar_propiedades_dominio` (gpt-5.6-terra, razonamiento alto): añadió propiedades deterministas de Money, descuentos y caja sin dependencias.
- `auditar_gate4_gate8` (gpt-5.6-terra, razonamiento alto): auditó la frontera B, separó Gate 8 lógico ya demostrado de DR físico operacional y corrigió README, reporte estructurado y regresión. En ese corte todavía faltaban Gate 4 y Gate 7; ambos quedaron demostrados posteriormente.
- `auditar_cobertura_fase0_dominio` (gpt-5.6-terra, razonamiento alto): auditó cobertura neutral, aisló modificadores por restaurante y endureció estados runtime; `auditar_order_adversarial` (gpt-5.6-terra, razonamiento alto): reprodujo getters heredados y cierre prematuro, y corrigió ambos; `auditar_finanzas_adversarial_2` (gpt-5.6-terra, razonamiento alto): añadió eventos explícitos y scope de auditoría a Payment/Refund y cerró la pérdida de motivo al rehidratar caja.
- `endurecer_totales_runtime` (gpt-5.6-terra, razonamiento alto): endureció calculadoras y modificadores contra formas/accessors/proxies hostiles; `validar_scopes_shared` (gpt-5.6-terra, razonamiento moderado): añadió parsers runtime versionados de scope; `auditar_integracion_dominio_actual` (gpt-5.6-terra, razonamiento alto): encontró y corrigió colisiones cross-operation en Payment/CashRegister.
- CodeGraph posterior indexó 56 archivos, 1,281 nodos y 3,481 relaciones; los nuevos parsers solo tienen consumidores de prueba y las fronteras financieras/totales permanecen contenidas en dominio, sin `apps/` productivas. No hubo cambios de esquema, offline ni Supabase remoto en esta unidad.
- Sesión 2026-08-29: CodeGraph se inicializó y consultó antes/después del cambio; quedó en 56 archivos, 1,290 nodos y 5,203 relaciones. `requireFreshRemotePushOptIn` y la invocación CLI fijada alcanzan solo el runner y su preflight; la nueva configuración de migración no llega a aplicaciones productivas.
- Verificación limpia 2026-08-29: instalación frozen exitosa; el primer `typecheck`/`test` detectó la dependencia de orden. Tras corregirla, una ejecución sin `dist`, caché ni `tsbuildinfo` pasó typecheck y 77 pruebas de dominio + 57 del spike, además de shared-types. El alcance opción B pasó lint, typecheck, 31 preflights y build. No se ejecutó Supabase remoto.
- Pipeline global posterior 2026-08-29: `pnpm install --frozen-lockfile`, lint, typecheck, 77 pruebas de dominio, 57 del spike, contratos runtime/compile-time de shared-types y build exitosos. `git diff --check` pasó y no quedaron `.test-dist`, secretos ni evidencia remota generada.
- Preparación Gate 7 2026-08-29: CodeGraph limitó la invocación CLI al fresh-push/runner/preflight; lint, typecheck, 31 preflights B y build del alcance pasaron. `npx.cmd --yes supabase@2.109.1 --version` devolvió `2.109.1`. `projects list` fue solo lectura y confirmó que la sesión actual no ve `zwbyiefqeujstyzysydn`; no hubo enlace, configuración remota ni migraciones.
- Ejecución Gate 7 2026-08-29: la sesión CLI correcta enumeró y enlazó exclusivamente `zwbyiefqeujstyzysydn`. El runner confirmó destino vacío, dry-run de las cinco migraciones esperadas, revalidación anti-TOCTOU, push completo y postcheck de historial exacto. Se expuso solo `adr010_b`; auditoría estructural 27/27 y `db lint` quedaron verdes. Los 31 preflights y 15 capacidades remotas pasaron con código 0; el cleanup final dejó 0 usuarios Auth y 0 filas operativas/financieras, conservando solo 2 restaurantes y 4 sucursales deterministas.
- Cierre local Gate 7 2026-08-29: el reporte distingue Gate 7 como evidencia externa demostrada y mantiene `eligibleForAdr010Go: false` solo por Gate 4. CodeGraph quedó en 56 archivos, 1,290 nodos y 5,203 relaciones, con impacto limitado al reporte. Instalación frozen, lint, typecheck, 77 pruebas de dominio, 57 del spike, contratos shared-types y build global pasaron.
- Preinspección técnica Gate 4 2026-08-29: se recorrieron los DTOs, servicios Nest, verificador Auth, puertos PostgreSQL y funciones SQL finales; búsquedas dirigidas no encontraron mutaciones Data API ni clientes productivos. El token separado del supervisor deriva una identidad verificada y SQL exige rol activo `owner`/`manager` en el scope exacto. La auditoría independiente identificó `restore/reset` como escrituras PostgreSQL administrativas del runner; quedaron documentadas como excepciones privadas no alcanzables por clientes. Los hooks `induceFailureAfter*` y puertos del spike deben excluirse de controladores/bundles productivos. `WRITE_FRONTIER.md` conserva Reviewer/Date/Decision sin completar para no sustituir la aceptación humana.
- Verificación posterior a la preinspección Gate 4: CodeGraph siguió actualizado con 56 archivos, 1,290 nodos y 5,203 relaciones; ambos servicios críticos solo impactan adapter, preflight, módulo Nest y barrel del spike. El alcance B pasó lint, typecheck, 31/31 preflights y build. La instalación frozen y el pipeline global completo pasaron: lint 4/4, typecheck 6/6, tests 6/6 y build 4/4; no se ejecutó ninguna nueva mutación remota.
- Cierre Gate 4 y scoring 2026-08-29: Emmanuel aceptó la frontera de escritura después de revisar su propósito y mantuvo la aprobación tras una explicación funcional. `WRITE_FRONTIER.md` registra Reviewer/Date/Decision, el reporte declara elegibilidad y `B_SCORING.md` asigna 75/100 con todos los mínimos cumplidos exactamente. Emmanuel aceptó después el GO final y seleccionó B. No hubo mutación remota nueva.
- Catálogo neutral de menú 2026-08-29: `packages/domain/src/menu.ts` valida y congela categorías/productos por Restaurant, versión y moneda, ordena por `displayOrder`, verifica SKU/referencias/allowlists y resuelve grupos obligatorios mediante `modifiers.ts` para producir `OrderItemPriceSnapshot` histórico compatible con Order. Diez pruebas nuevas cubren integración exacta, estados, duplicados, scope/versiones, monedas, mutaciones y entradas hostiles. El primer test descubrió y se corrigió un normalizador que rechazaba instancias válidas de Money. CodeGraph posterior: 58 archivos, 1,383 nodos y 5,562 relaciones; el contrato nuevo solo impacta su prueba/barrel y reusa `OrderItemPriceSnapshot` sin cambiarlo. Pipeline global: instalación frozen, lint 4/4, typecheck 6/6, 87 pruebas de dominio, 57 del spike, shared-types runtime/compile-time y build 4/4. No hubo esquema, red, Supabase ni aplicación productiva.
- Auditoría neutral de órdenes 2026-08-29: se añadieron `order-audit.ts` y su suite, se integró `OrderMutation` en creación/altas/transiciones/cancelación y se endureció `order-state.ts`. Una revisión independiente detectó dos huecos antes de REVIEW: lectura de inputs dinámicos durante creación y líneas canceladas rehidratadas sin evidencia; ambos se corrigieron con validación previa y evidencia discriminada/enlazada para toda cancelación. CodeGraph quedó actualizado con 60 archivos, 1,460 nodos y 6,115 relaciones; `OrderItemCancellationAudit` alcanza solo dominio, barrel y pruebas, sin consumidores productivos. Instalación frozen, lint 4/4, typecheck 6/6, 100 pruebas de dominio, 57 del spike, shared-types runtime/compile-time y build 4/4 pasaron; `git diff --check` quedó limpio. No hubo esquema, migración, red, Supabase, Git remoto ni implementación offline.
- `auditar_eventos_auditoria` — gpt-5.6-sol, razonamiento alto, read-only. Delimitó el contrato específico de Order, identificó los dos huecos anteriores y recomendó evidencia obligatoria enlazada para rehidratación; alcanzó su límite de uso después de entregar los hallazgos, sin editar archivos. El agente principal integró y verificó las correcciones.
- `auditar_adr001_opcion_b` — razonamiento alto, read-only. Separó el núcleo B aprobado de decisiones no demostradas y evitó fijar prematuramente ORM, cola, Realtime, despliegue o schema.
- `auditar_auth_api` — razonamiento alto, read-only. Detectó aceptación ciega del principal, protección local opt-in y una inyección Nest faltante; el agente principal corrigió los tres puntos y añadió smoke real. El cierre confirmó 18/18 pruebas de API sin bypass P0/P1; la integración de membresías sigue pendiente.
- Slice Auth/API 2026-08-29: `apps/api` valida solo URL HTTPS raíz y publishable key moderna/acotada, verifica tokens mediante `auth.getUser`, deriva/clona/congela `actorId`, protege globalmente con `APP_GUARD` y expone únicamente health como público. `MembershipAuthorizationService` rechaza scopes, roles, adapters y datos hostiles; todavía no tiene adapter PostgreSQL ni consumidor productivo. No se añadió schema, migración, DB URL, secret key ni acceso remoto Supabase.
- Verificación final ADR/Auth 2026-08-29: CodeGraph quedó actualizado en 71 archivos, 1,574 nodos y 6,396 relaciones. `SupabaseAuthGuard` impacta solo módulo/controladores/pruebas de API; `MembershipAuthorizationService` todavía solo tiene consumidores de prueba. Instalación frozen, lint 5/5, typecheck 7/7, 100 pruebas de dominio, 57 del spike, contratos shared-types, 18 pruebas de API y build 5/5 pasaron. El smoke Nest confirmó health 200 y session anónima 401; `git diff --check` y hashes idénticos de ambas copias del plan pasaron. No hubo mutación remota, schema, migración, secreto, certificado versionado ni operación Git remota.
- Slice tenancy/Auth 2026-08-30: se añadieron ADR-002, migración productiva inicial, configuración Data API, auditoría SQL de catálogo, conexión `pg` TLS fail-closed, rol dedicado `app_api`, lookup privado y selección de Branch. La revisión independiente encontró ownership implícito y posible rol preexistente contaminado; ambos se corrigieron fijando owner `postgres`/`BYPASSRLS`, contraseña nula, cero membresías y marcador de colisión. CodeGraph quedó en 77 archivos, 1,654 nodos y 6,608 relaciones; `MembershipAuthorizationService` ahora alcanza controller, adapter, módulo y pruebas. Treinta pruebas API, lint, typecheck y build de alcance pasaron. NestJS 11.2.3 se reemplazó por 11.2.2 en `apps/api` porque la publicación 11.2.3 instalada carecía de un módulo runtime. La migración sigue sin prueba PostgreSQL real por ausencia de Docker/Podman.
- Incidente de credencial 2026-08-30: un verificador transaccional temporal no llegó a conectarse, pero un error de parseo imprimió la URL administrativa completa en la salida de la tarea. El archivo temporal fue eliminado y no se versionó ningún secreto; la contraseña de `zwbyiefqeujstyzysydn` debe considerarse comprometida y rotarse antes de cualquier nuevo acceso. No hubo cambio remoto persistente.
- Verificación local final 2026-08-30: instalación frozen; lint 5/5, typecheck 7/7, tests 7/7 y build 5/5 pasaron. Las suites incluyen 100 pruebas de dominio, 57 del spike, contratos shared-types y 30 pruebas de API. CodeGraph quedó actualizado con 77 archivos, 1,654 nodos y 6,608 relaciones; el adapter de membresías solo alcanza servicio, controller, módulo y pruebas de API. `git diff --check` quedó limpio, `.test-dist` se eliminó y ni `.env.adr010.local` ni el certificado están versionados. La auditoría SQL de catálogo existe pero no se ejecutó en PostgreSQL real.
- `auditar_schema_memberships` — razonamiento alto, read-only. Definió el modelo multirrol histórico, exact Branch scope, constraints, RLS/grants, rollback y matriz 2×2; evitó copiar el rol único del spike.
- `auditar_slice_tenancy_final` — razonamiento alto, read-only. No encontró P0; detectó owner implícito de funciones, reutilización posible de un rol contaminado y ausencia de prueba PostgreSQL real. Los dos primeros hallazgos se corrigieron; el tercero mantiene las tareas IN_PROGRESS.
- Verificadores productivos 2026-08-30: `apps/api` incorporó un dry-run fresh que ejecuta migración+auditoría dentro de una transacción y hace rollback/cierre aun ante fallos, y un runner remoto 2×2 opt-in para Auth/RLS/Data API/`app_api`/Nest/revocación/constraints. Ambos validan proyecto, TLS `verify-full` y CA antes de red y solo reportan códigos allowlisted. La auditoría de catálogo se separó en estado pre-provisioning `NOLOGIN` y runtime `LOGIN` mínimo; el cleanup remoto valida la propiedad de Restaurant/Branch antes de borrar. No se ejecutó red ni Supabase porque la contraseña expuesta sigue pendiente de rotación.
- Verificación local de los verificadores: lint, typecheck, 42 pruebas API y build pasaron; ambos entrypoints sin opt-in rechazaron antes de red con JSON sanitizado. CodeGraph quedó actualizado en 84 archivos, 1,824 nodos y 7,251 relaciones; `runTenancyVerification` solo alcanza su entrypoint y `runSchemaVerification` solo su entrypoint/pruebas. El pipeline global pasó instalación frozen, lint 5/5, typecheck 7/7, tests 7/7 y build 5/5. Las tareas permanecen IN_PROGRESS hasta evidencia remota real.
- `implementar_verificador_schema_seguro` — razonamiento alto, unidad acotada con edición. Implementó el runner transaccional fresh, siete pruebas finales tras integración y CLI sanitizada; no accedió a red ni actualizó documentación operativa.
- `disenar_runner_tenancy_2x2` — razonamiento alto. Primero auditó read-only la matriz y detectó el conflicto `NOLOGIN`/runtime; después implementó configuración, runner 2×2 y cuatro pruebas de configuración. El agente principal separó las auditorías y endureció cleanup, contratos y documentación; no hubo ejecución remota.
- Verificación remota rollback-only 2026-08-30: la contraseña rotada conectó al ref exacto `zwbyiefqeujstyzysydn` por pooler con TLS `verify-full` y la CA local ignorada. La migración+auditoría oficial pasó con 5 políticas, 5 tablas protegidas y 4 funciones endurecidas; el rollback y postcheck dejaron ausentes `app`, `app_private`, `app_rls` y `app_api`. No se imprimieron credenciales ni hubo mutación persistente.
- PostgreSQL 16/Supabase exigió adaptar el contrato: `postgres` no es superusuario y al crear `app_api` recibe una concesión ADMIN inevitable otorgada por `supabase_admin`. Como no incluye INHERIT ni SET y la cuenta ya es administrativa, ambas auditorías aceptan exactamente esa fila y rechazan cualquier variación o membresía adicional. El verificador también normaliza la respuesta multi-statement de `pg`. Pasaron instalación frozen, pipeline global (lint 5/5, typecheck 7/7, tests 7/7 y build 5/5), 42 pruebas API, 100 de dominio, 57 del spike y contratos shared-types; CodeGraph quedó en 84 archivos, 1,825 nodos y 7,255 relaciones.
- `implementar_verificador_schema_seguro` — segundo análisis read-only, razonamiento alto. Identificó la semántica `GRANTED BY` de PostgreSQL 16; la prueba remota confirmó que el usuario administrado no puede revocar la concesión bootstrap, por lo que el agente principal integró el contrato exacto compatible con Supabase.
- `implementar_recuperacion_tenancy` — razonamiento alto, unidad standalone de recuperación. El subagente agotó su cuota antes de trabajar y no modificó archivos; la herramienta de recuperación ante cierre forzado sigue pendiente y no bloquea la ejecución normal con cleanup `finally`.
- Integración de historial 2026-08-30: una consulta remota read-only confirmó las cinco versiones del spike (`20260825000100`–`20260829000100`). La primera versión productiva fue renombrada a `20260830000100_create_tenancy_memberships.sql`; Supabase CLI la reconoce como la única migración local pendiente y sin colisión. No se ejecutó `db push`, link ni reparación de historial.
- Preparación de aplicación persistente: el dry-run directo detectó que las cinco versiones remotas no viven en la carpeta productiva y se rechazó reparar/revertir el historial. Un staging temporal con copias exactas de las cinco migraciones históricas más `20260830000100` confirmó que `db push` aplicaría solo la nueva migración, sin seed, roles ni Vault; el staging fue eliminado. La ejecución real requiere autorización humana explícita.
- Aplicación persistente 2026-08-30: Emmanuel autorizó exactamente `20260830000100` en `zwbyiefqeujstyzysydn`. Se aplicó desde un staging temporal con las cinco migraciones históricas exactas; no hubo reparación de historial, seed, roles, Vault ni push de configuración. El postcheck desde staging confirmó las seis versiones esperadas, auditoría pre-provisioning correcta (5 tablas, 5 políticas, 4 funciones, `app_api` NOLOGIN) y `db lint` sin errores.
- Recuperación E2E 2026-08-30: `apps/api` añadió un CLI destructivo separado con confirmación que repite el UUID, opt-in propio, salida allowlisted y configuración administrativa mínima. Recovery y runner comparten advisory lock; Auth exige marker/version/runId/fixture_key/email exactos, y PostgreSQL solo acepta estado vacío, Auth-only o el grafo completo 2R/4B/4M/5G con prefijo de revocación válido. Los deletes son por IDs exactos con `RETURNING` y postcheck; estados parciales/contaminados abortan.
- Verificación del recovery: 50 pruebas API locales pasaron, incluidas configuración destructiva, idempotencia, Auth-only, grafo completo, contaminación, orden de revocación y TOCTOU Auth. Lint, typecheck y build del alcance pasaron; CodeGraph quedó en 90 archivos, 1,949 nodos y 7,744 relaciones y confirmó que el entrypoint no es consumidor de controladores. La prueba remota del recovery queda pendiente hasta que exista un `runId` residual real o una ejecución controlada autorizada.
- Pipeline global posterior: instalación frozen, lint 5/5, typecheck 7/7, tests 7/7 y build 5/5 pasaron; las suites incluyen 100 pruebas de dominio, 57 del spike, 50 de API y contratos runtime/compile-time de shared-types. No hubo nueva mutación Supabase durante esta verificación.
- `implementar_recuperacion_e2e` — razonamiento alto, edición parcial. Añadió marcadores compartidos, lock y el esqueleto fail-closed; agotó cuota antes de tests/CLI. El agente principal auditó, completó e integró sus cambios.
- `auditar_recuperacion_e2e` — razonamiento alto, read-only. Identificó riesgos de nombres genéricos, deletes amplios, estados parciales, paginación Auth, carrera con runner y prefijos de revocación; agotó cuota después de entregar hallazgos. Todos quedaron incorporados.
- Aprovisionamiento privado local 2026-08-30: `apps/api` añadió configuración y CLI independientes con opt-in/confirmación exactos, correlación de URL/ref/host, CA y TLS `verify-full`, contraseña fuerte solo por entorno y reportes allowlisted. El flujo toma un lock global compartido con el E2E, fija SCRAM, activa `app_api` por diez minutos usando un parámetro enlazado, verifica identidad y permisos desde una conexión nueva, audita catálogo y promueve a vigencia indefinida. Ante cualquier resultado ambiguo o fallo posterior al intento de cambio, una sesión administrativa nueva compensa a `NOLOGIN PASSWORD NULL` y reaudita el estado seguro.
- Verificación del aprovisionador: lint, typecheck, 62 pruebas API y build del alcance pasaron. Las pruebas cubren éxito, precheck, lock ocupado, commit ambiguo, identidad errónea, auditoría alterada, compensación y fallo de compensación; la contraseña nunca aparece en SQL. `git diff --check` pasó. CodeGraph no estuvo disponible en este corte; la inspección dirigida confirmó que el CLI es el único consumidor del aprovisionador y que el lock solo enlaza aprovisionador y runner, sin alcance desde controladores/clientes.
- `auditar_aprovisionamiento_app_api` — razonamiento alto, read-only. Definió prechecks de catálogo, contraseña parametrizada/SCRAM, lock global, credencial temporal, verificación real y compensación; también acotó la afirmación de privilegios al schema de producto. El agente principal integró los hallazgos.
- Segundo incidente de credencial 2026-08-30: un diagnóstico read-only temporal falló antes de abrir red porque la URL administrativa local estaba envuelta en comillas; el error nativo imprimió la URL completa en la salida de la tarea. El archivo temporal se eliminó, no se versionó ningún secreto y no hubo mutación remota. Esa contraseña se considera comprometida, no se reutilizará y debe rotarse otra vez antes de continuar remotamente.
- Pipeline global posterior al aprovisionador: instalación frozen, lint 5/5, typecheck 7/7, tests 7/7 y build 5/5 pasaron; las suites incluyen 100 pruebas de dominio, 57 del spike, 62 de API y contratos runtime/compile-time de shared-types. `git diff --check` quedó limpio, `.test-dist` y el script temporal están ausentes, y Git confirmó que `.env.adr010.local` y `.certs/prod-ca-2021.crt` siguen ignorados. No hubo operación Git remota ni nueva mutación Supabase.
- Rotación posterior 2026-08-30: Emmanuel confirmó que reemplazó la contraseña administrativa expuesta. La nueva credencial permanece privada; esta unidad no leyó `.env`, no abrió red y no ejecutó ningún cambio remoto.
- Recuperación standalone de `app_api` 2026-08-30: se añadió CLI/configuración propios, sin contraseña de aplicación, con correlación URL/ref/DB/CA/TLS y confirmación destructiva larga. El fingerprint previo conserva atributos, membresía administrativa exacta, ownership y superficie productiva; el flujo fija el OID, confirma `NOLOGIN PASSWORD NULL` antes de drenar, termina sesiones en barridos acotados y revalida desde una conexión nueva. Es idempotente y reconcilia commits ambiguos aplicados/no aplicados con un único reintento.
- La compensación automática del aprovisionador también se endureció: ya no exige cero sesiones antes de deshabilitar; primero confirma el cambio y después termina cualquier sesión superviviente. La suite API pasó lint, typecheck, 74 pruebas y build. `auditar_recuperacion_app_api` — razonamiento alto, read-only — identificó la diferencia entre expiración y contención, la ventana de sesiones vivas y la necesidad de postcheck fresco; no editó, leyó secretos ni conectó Supabase.
- Pipeline global posterior a recovery: instalación frozen, lint 5/5, typecheck 7/7, tests 7/7 y build 5/5 pasaron; las suites incluyen 100 pruebas de dominio, 57 del spike, 74 de API y contratos runtime/compile-time de shared-types. El CLI recovery sin opt-in rechazó antes de red con JSON allowlisted; `git diff --check` quedó limpio, no quedaron `.test-dist`/scripts temporales y `.env.adr010.local`/CA siguen ignorados. No hubo mutación Supabase ni interacción Git remota.
- Verificador read-only de `app_api` 2026-08-30: se añadió configuración/CLI sin argumentos ni contraseña de aplicación, con correlación exacta de URL/ref/DB/CA/TLS, lock compartido, fingerprint completo y clasificación `safe_disabled|temporary|expired|runtime|partial`. Los estados estables ejecutan la auditoría pinneada correspondiente; los intermedios devuelven atención sin intentar corregirlos. La salida solo incluye estado, auditoría y presencia booleana de sesiones.
- La allowlist administrativa ahora exige endpoint directo o pooler de sesión en 5432 y admite un único `sslrootcert` opcional, no vacío y acotado, además del `sslmode=verify-full` obligatorio. Rechaza 6543 porque los advisory locks son de sesión, así como duplicados y cualquier parámetro adicional; elimina el query completo antes de entregar la URL a `pg`. La CA efectiva sigue cargándose y validándose desde `DATABASE_CA_CERT_PATH`.
- Evidencia remota read-only posterior a la segunda rotación: la conexión mínima confirmó contraseña/TLS y el verificador completo confirmó `{"activeSessions":false,"catalogAudit":true,"state":"safe_disabled","status":"ok"}` en `zwbyiefqeujstyzysydn`. No se habilitó `LOGIN`, no se escribió base/Auth/configuración y no se expuso Data API. El primer intento completo agotó el timeout de conexión y el reintento controlado pasó; los diagnósticos temporales no imprimieron URL, usuario, contraseña ni mensajes nativos y fueron eliminados.
- Verificación local de esta unidad antes del pipeline global: lint, typecheck y 83 pruebas API pasaron. CodeGraph no está disponible entre las herramientas de esta sesión; la inspección manual dirigida localizó cuatro consumidores del parser administrativo (verificación, aprovisionamiento, recovery y estado) y confirmó que los entrypoints operativos no son alcanzables desde controladores.
- Pipeline global previo al último hardening del verificador: instalación frozen offline al día, lint 5/5, typecheck 7/7, tests 7/7 y build 5/5 pasaron; las suites incluían 100 pruebas de dominio, 57 del spike, 81 de API y contratos runtime/compile-time de shared-types. La instalación online solo emitió un aviso no bloqueante al consultar metadata de actualización de pnpm; el modo offline confirmó el lockfile y dependencias locales.
- `auditar_verificador_estado_app_api` — razonamiento alto, read-only. Confirmó el diseño fail-closed y detectó que aceptar el pooler transaccional 6543 era incompatible con el advisory lock ligado a sesión; el agente principal restringió todas las conexiones administrativas a 5432 y añadió la regresión. No editó, leyó secretos ni conectó Supabase; CodeGraph no estaba disponible.
- Hardening posterior a auditoría: el fingerprint y ambas auditorías SQL ahora exigen `rolconnlimit=-1`; los estados estables con sesiones activas devuelven `attention` y preservan `activeSessions=true` sin ejecutar una auditoría que requiere quiescencia. Se añadieron regresiones para 6543, NUL/duplicados TLS, orden lock→fingerprint→sesiones→auditoría, sesiones activas en ambos estados estables y fallo de cierre. La reejecución remota read-only volvió a confirmar `safe_disabled`, auditoría endurecida verde y cero sesiones.
- Pipeline global final de la unidad: instalación frozen offline, lint 5/5, typecheck 7/7, tests 7/7 y build 5/5 pasaron; las suites incluyen 100 pruebas de dominio, 57 del spike, 83 de API y contratos runtime/compile-time de shared-types.
- Cierre de limpieza: `git diff --check` pasó, no quedaron `.test-dist` ni diagnósticos temporales, y Git confirmó que `.env.adr010.local` y `.certs/prod-ca-2021.crt` continúan ignorados. No hubo push, merge, rebase, force-push ni otra escritura Git remota.
- Coordinación frontend 2026-08-30: la primera eliminación de `apps/web` fue una interferencia accidental, porque Claude trabajaba simultáneamente en el mismo checkout. Claude reconstruyó los 23 archivos y sus dependencias; el integrador confirmó que el lockfile conserva también la dependencia backend. `frontend.md` ahora fija una lista blanca obligatoria: Claude solo escribe `apps/web/**`, Codex no toca esa carpeta mientras el slice esté activo y manifests/lockfile raíz pertenecen al integrador.
- Hardening de caché 2026-08-30: las dos rutas autenticadas existentes de Nest declaran `Cache-Control: private, no-store`, con regresión en el smoke para identidad y scope. CodeGraph quedó actualizado en 105 archivos, 2,192 nodos y 8,592 relaciones; cada controller solo alcanza `AppModule` y su prueba, sin consumidores huérfanos.
- Verificación posterior: instalación frozen desde lockfile, lint 5/5, typecheck 7/7, tests 7/7 y build 5/5 pasaron; se mantienen 100 pruebas de dominio, 57 del spike, 83 de API y contratos runtime/compile-time de shared-types. No hubo mutación Supabase ni operación Git remota.
- Preparación neutral de persistencia 2026-08-30: se añadió en `apps/api/src/persistence/**` un códec v1 de `Order`, `OrderAuditEvent` y `OrderMutation`. Conserva scopes, snapshots, cancelaciones y auditoría; representa `Money` sin float y ratios bigint como strings decimales canónicos; rehidrata y congela antes de delegar validación al dominio. Falla cerrado ante versión/campos extra, accessors, proxies, ciclos, prototipos hostiles, números inseguros, ratios no canónicos o divergencia agregado/evento.
- Verificación del códec: lint, typecheck, 87 pruebas API y build pasaron; las 4 pruebas nuevas cubren round-trip JSON completo, evidencia de cancelación, datos hostiles y scope divergente. CodeGraph quedó al día en 125 archivos, 2,386 nodos y 9,349 relaciones; los cuatro entrypoints del códec solo tienen consumidores internos y su prueba, y `OrderMutation` mantiene su dependencia en dominio/API sin UI ni migración.
- Auditoría de siguiente migración: `auditar_siguiente_migracion` — razonamiento alto, read-only — recomendó un futuro core aditivo/deny-by-default de Order, pero bloqueó escribir/aplicar SQL hasta cerrar contratos de DiningTable/ProductionStation, RBAC y prueba PostgreSQL de upgrade. No editó ni accedió a Supabase/secretos.
- Implementación delegada del códec: `implementar_codec_orden` — razonamiento alto — dejó el archivo inicial sin pruebas antes de ser interrumpido; el agente principal auditó, corrigió bigint/correlación de cancelaciones, añadió tests, integró la dependencia y verificó el resultado. `implementar_recuperacion_e2e` no produjo cambios en este corte por límite de uso.
- FE-0 reconstruido: lint, typecheck, 11/11 pruebas de configuración y build Next pasaron. La revisión del integrador detectó que el rechazo remoto llama `signOut()` desde un Server Component cuyo adaptador ignora escrituras; Claude debe mover la invalidación a una frontera mutable, evitar llamadas duplicadas/ciclo de redirects y añadir pruebas funcionales. FE-0 permanece IN_PROGRESS y FE-0.1 BLOCKED; no se hizo smoke humano.
- Integración final de FE-0: Claude movió la revalidación Nest y `signOut()` a `proxy.ts`, copió cookies refrescadas/eliminadas al redirect fijo, retiró la invalidación del layout y añadió siete pruebas del parser UUID y fallos remotos/hostiles. El integrador añadió, sin dependencias nuevas, un loader ESM y doble Supabase exclusivos de test para ejecutar directamente el proxy con `NextRequest`/`NextResponse` reales. Lint, typecheck, 23/23 pruebas y build pasaron; cinco casos demuestran limpieza efectiva, redirect fijo sin ciclo, llamada Nest única y `Cache-Control: private, no-store`. Solo falta smoke humano.
- Directorio de membresías 2026-08-30: `shared-types` añadió el contrato/parser v1 exacto y congelado; Nest añadió `GET /api/v1/access/memberships`, adapter PostgreSQL parametrizado, 503 genérico y `no-store`. La respuesta contiene únicamente scope, nombres y roles activos, admite lista vacía y rechaza respuestas parciales/hostiles. Los roles dejaron de duplicarse entre API y contrato compartido.
- Esquema del directorio: `20260830000200` añade una única función privada `SECURITY DEFINER`, owner `postgres`, `search_path=''`, límite 501 y `EXECUTE` exclusivo de `app_api`. Las auditorías/lifecycle fingerprints permiten exactamente cuatro funciones antes de aplicarla o cinco después. El runner E2E quedó ampliado para dos actores, listado HTTP/privado, role/membership revocation, Branch/Restaurant deshabilitados, token vivo y cache `no-store`.
- CodeGraph posterior quedó actualizado con 129 archivos, 2,459 nodos y 9,627 relaciones. `PostgresMembershipDirectory` solo alcanza controller, módulo y prueba; el catálogo de roles compartido solo alcanza su parser, autorización Nest y pruebas. El cálculo conservador identificó 36 suites transitivamente afectadas; las suites acotadas están verdes y el pipeline global se reserva hasta que Claude deje de escribir FE-0.
- Verificación: shared-types pasó lint/typecheck/test/build; API pasó lint/typecheck, 94 pruebas y build. La migración compiló en `zwbyiefqeujstyzysydn`, pasó auditoría/capability/actor vacío y luego se revirtió; el postcheck confirmó que la función no existe remotamente. No hubo cambio persistente en Supabase ni interacción Git remota.
- Verificador reproducible de `20260830000200`: `schema-verification` admite una expectativa exacta de cuatro o cinco funciones y el nuevo entrypoint fija cinco, aplica solo la segunda migración y siempre revierte. Configuración ausente rechaza antes de red con JSON allowlisted. Lint/typecheck, 97 pruebas API y build pasaron; CodeGraph quedó en 130 archivos, 2,467 nodos y 9,646 relaciones y limita el runner a la frontera administrativa y sus pruebas.
- Estrategia de aplicación: se descartó escribir directamente `supabase_migrations` o usar `db push` desde la raíz. Cuando exista rotación y autorización se repetirá el staging temporal ya probado para `20260830000100`, con las cinco migraciones históricas exactas, ambas productivas, dry-run exclusivo, CLI Supabase y postcheck de siete versiones. Esto conserva una única autoridad de historial sin reparar ni mezclar el spike en el árbol productivo.
- Incidente de credencial: el primer lector ad hoc dejó que una excepción `URL` imprimiera el valor completo de la conexión local. La contraseña administrativa debe rotarse nuevamente; no fue versionada ni copiada a documentación. No realizar más conexiones remotas con ese valor.
- Delegación: `auditar_listado_membresias` — razonamiento alto, read-only — definió el contrato mínimo, amenazas, función privada, allowlist exacta y secuencia de despliegue. El agente principal implementó, integró y verificó. `implementar_recuperacion_e2e` no produjo cambios por límite de uso.
- Delegación de despliegue: `auditar_aplicador_migracion` — razonamiento alto, read-only — identificó hashes, invariantes, riesgo de commit ambiguo y la incompatibilidad del `db push` raíz. El integrador contrastó esa propuesta con la evidencia del staging ya usado y eligió conservar el CLI oficial mediante staging temporal. `implementar_config_aplicador` fue interrumpido antes de editar cuando se descartó el aplicador SQL personalizado.
- CodeGraph posterior a FE-0 quedó en 133 archivos, 2,491 nodos y 9,712 relaciones; `proxy` y `redirectPreservingCookies` solo alcanzan implementación y prueba. `investigar_test_proxy_next` — razonamiento moderado, read-only — reprodujo la incompatibilidad ESM de Node/Next y recomendó el loader acotado que integró el agente principal.
- Coordinación vigente: Claude debe detenerse. FE-0.1 será su siguiente slice previsto únicamente después de aplicar la migración, aprovisionar `app_api`, pasar E2E remota, enlazar `shared-types` a web, publicar este checkpoint y emitir una asignación nueva explícita.
- Emmanuel autorizó aplicar persistentemente `20260830000200`. Dos preflights CLI correctamente invocados conservaron URL/TLS como un solo argumento y se detuvieron con SQLSTATE 28P01 por la contraseña administrativa antes de leer historial o escribir. La contraseña local de `app_api` sí pasó fuerza y separación; no se imprimieron secretos ni hubo mutación remota.
- Pipeline global del checkpoint: `pnpm install --frozen-lockfile`, lint, typecheck, tests y build pasaron en los seis paquetes; Turbo reportó 6/6 lint, 9/9 tareas de tipos, 9/9 tareas de test y 6/6 builds. Web conserva 23/23 pruebas. La máquina local usó Node 22.20.0 y emitió el aviso de engine porque CI/producción fija Node 24.19.0; no produjo fallos.
- Checkpoint Git 2026-08-30: tras confirmar que `HEAD`, `origin/main` y su merge-base eran exactamente `f0091f30324263ad253d5ea395733f0d1f56200a`, se publicó por fast-forward `36946ca002c69931fba35e03bd559f5ecdf2fb60` (`feat: establish phase 0 application foundations`). Incluye 128 archivos del avance acumulado. El staging pasó `git diff --cached --check` y excluyó `.env` reales, certificados, claves, dependencias y artefactos generados. No hubo merge, rebase, force-push ni cambio de `origin/master`.
- Continuación publicada 2026-08-31: el checkout inició limpio y `HEAD`, `origin/main` y merge-base coincidieron exactamente en `ca2e4cb0b2cea33b9387fd0a3947f333b6e6d7b4`; `git fetch origin main` no requirió mover `main`. CodeGraph previo estaba completo y sin pendientes: 133 archivos, 2,491 nodos y 9,712 relaciones.
- Aplicación de `20260830000200`: el staging temporal reunió siete copias byte a byte con hashes coincidentes. El historial previo confirmó seis versiones y solo la segunda productiva pendiente; el dry-run anunció exclusivamente `20260830000200_list_active_branch_memberships.sql`. La aplicación oficial terminó con código 0 y el postcheck confirmó exactamente `20260825000100`–`20260830000200`. No hubo reparación, seed, roles, Vault, configuración, `db push` raíz ni secreto impreso; el staging se eliminó.
- Aprovisionamiento `app_api`: dos intentos iniciales alcanzaron la auditoría runtime y fallaron porque Supavisor recreaba un backend retenido mientras `LOGIN` seguía visible; ambos compensaron y el verificador fresco confirmó `safe_disabled`, catálogo verde y cero sesiones. El flujo se corrigió con un corte quiescente: confirmar `NOLOGIN PASSWORD NULL`, drenar sesiones y reinstalar SCRAM+`infinity`+auditoría dentro de una sola transacción. El aprovisionador final devolvió `status=ok`; dos postchecks administrativos frescos confirmaron `runtime`, auditoría verde y cero sesiones.
- Hardening posterior: las auditorías pinneadas ya no aceptan la superficie antigua de cuatro funciones. Exigen cinco funciones exactas y niegan `EXECUTE` de `anon`, `authenticated` y `service_role` en las capacidades privadas. El drenaje usa `pg_terminate_backend(pid, 5000)` y 14 pruebas del aprovisionador cubren reaparición del pooler, límite agotado, terminación falsa, sesión en el segundo corte y commits ambiguos 2/3. La instalación frozen y el pipeline global pasaron: lint 6/6, typecheck 9/9, tests 9/9 y build 6/6; API conserva 103 pruebas verdes. Node 22.20.0 emitió el aviso esperado frente a Node 24.19.0 fijado.
- `auditar_supavisor_app_api` — razonamiento alto, read-only. Validó el corte quiescente, rechazó allowlists de PID/application name/sesión y enumeró riesgos de crash y regresiones; no editó, usó red ni leyó secretos.
- `revisar_parche_provisionamiento` — razonamiento alto, read-only. Confirmó que drenar bajo `LOGIN` era una carrera, recomendó timeout acotado y detectó que las auditorías todavía aceptaban cuatro funciones; sus hallazgos quedaron integrados. No editó, usó red ni leyó secretos.
- Esquema: cambio aditivo sin rollback destructivo; `20260830000200` permanece aplicado. Offline: sin cambios. Git remoto: solo fetch de `origin/main`; no hubo push, merge, rebase ni force-push. Frontend: Claude continúa detenido y FE-0.1 no fue autorizado.

## Verificación

Esta sección es un historial acumulado de verificaciones. Las cifras anteriores (por ejemplo, tres migraciones o 24/24 checks) corresponden a cortes intermedios y no sustituyen la evidencia vigente de cinco migraciones y 27/27 checks.

- Verificación vigente 2026-08-31: CodeGraph completo y sin pendientes, 133 archivos, 2,491 nodos y 9,744 relaciones. El impacto de `provisionAppApi` y `verifyAppApiState` queda limitado a cada implementación, runner y prueba.
- Verificación vigente 2026-08-31: `pnpm install --frozen-lockfile`, lint 6/6, typecheck 9/9, tests 9/9 y build 6/6 pasaron con pnpm 11.19.0/Turbo 2.10.12. API suma 103 pruebas; las 14 de aprovisionamiento incluyen las regresiones de pooler y commit ambiguo. Solo apareció el warning de Node 22.20.0 frente al 24.19.0 fijado.
- Verificación remota vigente 2026-08-31: historial exacto de siete migraciones, auditoría de catálogo de cinco funciones y grants cliente denegados; dos postchecks frescos clasificaron `app_api` como `runtime`, con auditoría verde y cero sesiones.

- CodeGraph antes y después del cambio: 0 archivos/nodos/relaciones; no existe código fuente local que analizar y `.codegraph/` quedó excluido.
- Se validó el remoto con ls-remote y fetch.
- `git check-ignore -v --no-index` verificó reglas para `node_modules`, `.env.local`, `.codegraph`, `.next`, `dist`, `coverage`, Android y credenciales de firma; las reglas negadas conservan `.env.example` y `.env.production.example`.
- `pnpm install --frozen-lockfile`: exitoso con pnpm 11.19.0.
- `pnpm lint`, `pnpm typecheck`, `pnpm test` y `pnpm build`: exitosos mediante Turborepo 2.10.12; 0 paquetes/tareas porque todavía no se crearon workspaces funcionales.
- CodeGraph posterior al monorepo: 2 YAML indexados (`pnpm-workspace.yaml`, `pnpm-lock.yaml`), 0 nodos y 0 relaciones; no hay consumidores o referencias compartidas que romper.
- No había UI que verificar para estos cambios fundacionales.
- Plan raíz y copia en `docs/`: SHA-256 idéntico `551267BCAF3B9DB69BAC828009BA23D85E7B8413D925BDFF722F4BB620730FD4`.
- Verificación fresca directa por workspace: lint real, typecheck, build y tests exitosos; dominio 13/13 pruebas.
- CodeGraph posterior: 12 archivos, 118 nodos y 210 relaciones; `Money` impacta 12 símbolos internos y `transitionOrderItem` 2 símbolos internos, sin consumidores externos conocidos.
- `.test-dist` no permanece tras pruebas y su regla de ignore fue verificada.
- El sandbox bloqueó procesos hijos `pnpm --recursive` con `EPERM`; se repitieron fuera del sandbox y pasaron. Los intentos previos de pasar `--force` mediante el script raíz reenviaron el argumento a TypeScript y no se usan como evidencia.
- CI: YAML validado con parser temporal, revisión independiente sin hallazgos y CodeGraph reconoce `.github/workflows/ci.yml`; la ejecución real en runner GitHub queda pendiente de publicar una rama autorizada.
- Harness ADR-010: lint, typecheck, build y 15/15 pruebas frescas exitosas; detecta fugas de lectura/escritura, estado parcial/huérfano, duplicados, pérdida/fuga KDS, revocación tardía, migración/restore incompletos, secretos y evidencia insuficiente.
- CodeGraph posterior al harness: 19 archivos, 204 nodos y 521 relaciones; `Adr010Adapter` afecta solo harness/gates/adaptador de referencia, sin dependencias productivas.
- Verificación integrada del 2026-08-26: `pnpm install --frozen-lockfile --yes`, lint, typecheck, test y build exitosos; 13 pruebas de dominio, 15 del harness, 5 preflights A, 9 B y 6 C (48 pruebas Node en total).
- Verificación monetaria posterior del 2026-08-26: 9 pruebas nuevas de totales; suite integrada queda en 22 pruebas de dominio + 35 del spike (57 pruebas Node), con instalación frozen, lint, typecheck y build exitosos.
- Auditoría CodeGraph de C reportó 30 archivos, 327 nodos y 742 relaciones; `SupabaseDirectReadClient` solo afecta 6 símbolos internos y C no consume `Adr010Adapter`. La reconsulta final del agente principal falló tres veces con `Transport closed`; se hizo inspección manual dirigida y la limitación queda registrada.
- CodeGraph del auditor A/B confirmó rutas internas: el runner es el único consumidor del adapter A y del bootstrap B; restore solo alcanza validación allowlist/comparación de referencias, y bootstrap/cleanup solo alcanza descubrimiento server-only por metadata. La reconsulta posterior del agente principal volvió a fallar para status e impactos con `Transport closed`.
- CodeGraph final volvió a estar operativo: 41 archivos, 494 nodos y 1,089 relaciones. `SupabaseAdr010CriticalOrderService` afecta 7 símbolos internos de la frontera Nest; `withAdr010BAuthenticatedFixtures` afecta solo su archivo/runner y no hay consumidores productivos.
- CodeGraph posterior a totales: 43 archivos, 568 nodos y 1,346 relaciones. `OrderItemPriceSnapshot` impacta 13 símbolos, todos internos a `order-totals`; no existen consumidores externos ni referencias productivas que migrar.
- CodeGraph antes y después del contrato offline: 43 archivos, 568 nodos y 1,346 relaciones. Solo encontró cursores/idempotencia internos del spike ADR-010; no existe motor de sync productivo ni consumidores que migrar.
- Documentación offline: fences y tablas Markdown balanceados, referencias ADR verificadas y `git diff --check` sin errores. La validación integrada posterior mantuvo instalación frozen, lint, typecheck, 57 pruebas Node y build exitosos.
- `.test-dist` fue limpiado por las suites y permanece ignorado. No se detectaron archivos `.env` reales ni secretos versionables.
- Supabase CLI 2.109.1 autenticó y enlazó el ref `cxcnnhafchqslvgvkeye`. Las tres migraciones experimentales quedaron aplicadas; la auditoría remota read-only reportó 24/24 checks aprobados para los schemas ADR. Docker no está instalado.
- El security advisor falló por errores preexistentes del schema `public` ajeno al spike (tres tablas sin RLS y una función con `search_path` mutable), evidencia de que el proyecto no es aislado. Por seguridad no se ejecutaron Auth desechable, gates destructivos, reset, concurrencia ni restore.
- El push de configuración que expuso `adr010_b` alteró temporalmente defaults de Auth; la reversión exacta se aplicó y una comparación posterior confirmó API/DB/Auth/Storage al día. No hubo funcionalidad offline ni interacción Git remota.
- La primera reinstalación final quedó sin red dentro del sandbox después de recrear `node_modules`; se interrumpió y se repitió con el acceso aprobado usando exactamente el lockfile. No se descargaron versiones diferentes ni cambió el lockfile.
- Verificación final del 2026-08-27: instalación frozen reproducible, lint, typecheck, 61/61 pruebas Node y build exitosos; no quedaron `.test-dist`, archivos `.env` reales ni metadata Supabase versionada.
- `supabase migration list --linked` confirmó las tres versiones locales/remotas y `supabase db push --linked --dry-run` reportó la base al día. CodeGraph final indexó 43 archivos, 626 nodos y 1,500 relaciones; el impacto de `SupabaseNestAdr010Adapter` permanece contenido en su módulo, sin consumidores productivos.
- Verificación de modificadores del 2026-08-27: pipeline global lint/typecheck/test/build exitoso con 30 pruebas de dominio y 39 del spike (69 pruebas Node). `git diff --check` pasó; no quedaron `.test-dist` ni archivos `.env` reales.
- CodeGraph posterior indexó 45 archivos, 715 nodos y 1,665 relaciones. `ModifierPriceSnapshot` impacta 10 símbolos internos de `order-totals`; `createModifierSelectionSnapshot` solo su propio módulo y no tiene consumidores productivos.
- Verificación global posterior a pagos/caja: lint, typecheck, 46 pruebas de dominio + 39 del spike (85 pruebas Node) y build exitosos; `git diff --check` pasó y no quedaron `.test-dist`.
- CodeGraph final indexó 49 archivos, 915 nodos y 2,173 relaciones. `refundPayment` y `appendCashMovement` solo afectan sus propios módulos; no existen consumidores productivos todavía.
- Verificación remota final B del 2026-08-27: `test:option-b:rls` y `test:option-b:gates` exitosos; 11 capacidades reportadas, `eligibleForAdr010Go: false` deliberado, cleanup final con 0 usuarios/filas operativas temporales.
- Auditoría posterior: 24/24 checks, tres migraciones locales/remotas idénticas, `db push --dry-run` al día y `db lint` sin errores en los schemas ADR.
- Pipeline global posterior: lint, typecheck, 46 pruebas de dominio + 43 del spike (89 pruebas Node) y build exitosos.
- Verificación global del 2026-08-28 posterior a propiedades financieras: lint, typecheck, 69 pruebas de dominio + 56 del spike (125 pruebas Node) y build exitosos; 96 casos Money, 61×4 permutaciones de descuento y 48 secuencias de caja pasaron con semillas fijas; suites sin `.test-dist` residual.

## Subagentes

- `verificar_pipeline` — razonamiento bajo. Ejecutó la instalación/pipeline inicial, reprodujo el fallo limpio y confirmó read-only que la corrección mínima era añadir `^build` a typecheck/test; no editó archivos.
- `auditar_gate7` — razonamiento alto. Auditó seguridad/esquema del runner fresco, sus entradas y brechas; identificó el hardening local y confirmó que al usuario solo debe pedirse la URL pública del segundo proyecto.
- `auditar_gate7` (seguimiento) — razonamiento alto. Revalidó el ref `zwbyiefqeujstyzysydn`, el procedimiento fail-closed, la ventana TOCTOU y los prerrequisitos de autenticación/CA/configuración; no conectó ni editó.
- `auditar_gate7_ejecucion` — razonamiento alto. Auditó de forma independiente enlace, cinco migraciones, preflight, dry-run/apply, postcheck y riesgos de push parcial; no editó ni conectó servicios. Su checklist condicionó el apply a la vaciedad, orden exacto y revalidación que finalmente pasaron.
- `auditar_gate4_final` — razonamiento alto. Auditoría independiente y read-only de DTOs, Auth, puertos, SQL privado, permisos y posibles bypasses; sus conclusiones se integran solo como evidencia de apoyo y no como aceptación humana.
- `proponer_scoring_b` — gpt-5.6-sol, razonamiento alto. Auditó read-only los seis criterios y calculó la recomendación condicional conservadora de 75/100 antes de la aceptación de Gate 4; tras la aprobación humana, el agente principal la registró como score oficial. No editó ni accedió a servicios remotos.
- `auditar_menu_domain` — gpt-5.6-sol, razonamiento moderado. Auditó read-only los contratos de modificadores, snapshots y Order, confirmó que no existía un catálogo duplicado y delimitó el slice neutral; el agente principal implementó, integró y verificó el resultado.
- `verificar_codegraph_cert` — razonamiento bajo. Comprobó disponibilidad inicial de CodeGraph, mapeó manualmente el runner y confirmó que no había certificado visible; no editó ni conectó servicios remotos.

- implementar_gitignore — modelo gpt-5.6-luna, razonamiento bajo.
- Alcance: implementación mecánica del `.gitignore` raíz y verificación inicial; modificó solo `.gitignore`.
- revisar_gitignore — modelo gpt-5.6-luna, razonamiento bajo.
- Alcance: revisión independiente y corrección acotada de artefactos móviles, credenciales de firma y logs; modificó solo `.gitignore`.
- implementar_monorepo — modelo gpt-5.6-terra, razonamiento moderado.
- Alcance: configuración raíz pnpm/Turborepo y verificación inicial; creó `package.json`, `pnpm-workspace.yaml` y `turbo.json`.
- revisar_monorepo_turbo — modelo gpt-5.6-terra, razonamiento moderado.
- Alcance: cerró la integración real con Turborepo, fijó `turbo@2.10.12`, generó `pnpm-lock.yaml` y verificó los cuatro pipelines.
- documentar_greenfield — modelo gpt-5.6-luna, razonamiento bajo.
- Alcance: README y copia byte a byte del plan en `docs/`.
- crear_config_tipos — modelo gpt-5.6-terra, razonamiento moderado.
- Alcance: presets TypeScript, shared-types neutrales, pruebas de tipos y lockfile.
- implementar_dominio_pos — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: Money, errores, máquinas de estados y pruebas unitarias iniciales.
- auditar_dominio_pos — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: auditoría financiera/estados, evidencia inmutable de cancelación y ampliación a 13 pruebas.
- configurar_eslint — modelo gpt-5.6-terra, razonamiento moderado.
- Alcance: ESLint real, regla de imports prohibidos y ajuste TypeScript 7.0.2 a 6.0.3 por compatibilidad verificada.
- implementar_ci — modelo gpt-5.6-terra, razonamiento moderado.
- Alcance: workflow GitHub Actions, verificación de versiones oficiales, parser YAML y pipeline local.
- revisar_ci — modelo gpt-5.6-luna, razonamiento bajo.
- Alcance: revisión independiente de permisos, eventos, caché, reproducibilidad y ausencia de secretos.
- crear_harness_adr010 — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: harness Día 1, fixtures, contrato, adaptador de referencia, ADR/README y pruebas negativas iniciales.
- auditar_harness_adr010 — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: reforzó gates contra falsos verdes, separó inspección humana de frontera y actualizó claves Supabase a publishable/secret.
- scaffold_opcion_b — modelo heredado, razonamiento moderado.
- Alcance: inició la opción B; alcanzó límite de uso tras dejar una implementación parcial, que fue inspeccionada e integrada por el agente principal.
- auditar_opcion_b — modelo heredado, razonamiento alto.
- Alcance: auditó seguridad/tenancy de B, separó configuración pública/servidor, endureció SQL/RPC/Auth FKs y dejó explícitas las brechas no probadas.
- scaffold_opcion_c — modelo heredado, razonamiento moderado.
- Alcance: implementó el cliente público de solo lectura, hardening SQL, runner opt-in y preflights de C.
- auditar_opcion_c — modelo heredado, razonamiento alto.
- Alcance: encapsuló el SDK, endureció claves/cursor/scope/revocaciones y confirmó que C no puede ser frontera de escritura crítica.
- implementar_bootstrap_b — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: Admin API server-only, usuarios desechables, memberships deterministas, cleanup FK-safe y preflights B; no ejecutó remoto.
- scaffold_opcion_a — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: adapter PostgreSQL A, migración thin-slice, frontera Nest, runner opt-in, dependencias `pg` y preflights; no pudo ejecutar PostgreSQL por falta de infraestructura.
- auditar_adr010_a_b — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: auditoría cruzada de persistencia/tenancy/Auth; bloqueó identificadores controlados por backup, conflictos silenciosos y usuarios Auth huérfanos, añadió regresiones y mantuvo NO-GO explícito.
- implementar_principal_jwt_b — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: inició el principal JWT B y dejó cambios parciales antes de agotar cuota; el orquestador los inspeccionó y reasignó.
- preparar_probe_rls_b — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: inició fixtures autenticados/probe RLS; sus cambios parciales se integraron en la recuperación posterior.
- completar_auth_rls_b — modelo gpt-5.6-luna, razonamiento alto.
- Alcance: recuperó cambios parciales, cerró principal verificado, probe RLS, scripts, pruebas y documentación sin ejecutar remoto.
- privatizar_rpc_b — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: movió funciones privilegiadas a schema privado y cambió escritura/bootstrap a PostgreSQL server-only conforme a guías oficiales.
- auditar_auth_rls_b — modelo gpt-5.6-luna, razonamiento alto.
- Alcance: corrigió cleanup parcial/concurrente con run IDs, endureció ACL privadas y verificó 9 preflights B.
- redactar_adr007_monetario — modelo gpt-5.6-luna, razonamiento alto.
- Alcance: ADR-007 neutral, orden de cálculo, redondeo, snapshots, ejemplos y bloqueos fiscales; no modificó código.
- implementar_totales_dominio — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: snapshots y cálculo puro de líneas/orden, impuestos, descuentos, propina, prorrateo v1 y pruebas iniciales.
- auditar_totales_monetarios — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: eliminó efectos laterales, completó versiones/estación/unidad, estabilizó desempate, alineó ADR y añadió regresiones financieras.
- redactar_contrato_sync — modelo gpt-5.6-luna, razonamiento alto.
- Alcance: especificó topología, envelope, push/pull, outbox, deduplicación, versiones, tombstones, seguridad y pruebas sin implementar almacenamiento ni motor de sync.
- matriz_offline_conflictos — modelo gpt-5.6-luna, razonamiento alto.
- Alcance: definió capacidades por cliente/conectividad, conflictos por entidad, mensajes UI y failure injection con política financiera conservadora.
- auditar_contrato_offline — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: auditó causalidad, idempotencia, cursores, revocación, dinero/KDS y corrigió ambos documentos contra doble efecto, pérdida de pull y falsas garantías offline.
- crear_adr004 — modelo gpt-5.6-luna, razonamiento bajo.
- Alcance: creó el ADR breve de topología y enlazó las especificaciones auditadas; no duplicó contratos ni prometió implementación.
- auditar_plan_pos — modelo gpt-5.6-terra, razonamiento moderado.
- Alcance: auditoría independiente de contradicciones, decisiones, secciones faltantes y criterios de aceptación.
- No editó archivos. Sus hallazgos se integraron en el plan v2.
- revisar_plan_claude — modelo gpt-5.6-terra, razonamiento moderado.
- Alcance: revisión independiente de las aclaraciones añadidas por Claude. No editó archivos; sus hallazgos se integraron en el plan v2.1.
- completar_adapter_b — modelo heredado, razonamiento alto.
- Alcance: implementó el adapter B con Auth real, frontera Nest/PostgreSQL privada, lecturas RLS, migración/ACL checks, backup/restore, cleanup y runner opt-in; mantuvo `eligibleForAdr010Go: false`.
- preparar_migraciones_b_c — modelo heredado, razonamiento alto.
- Alcance: versionó el hardening C como migración, completó configuración Supabase, ignores, preflights y documentación de rollback.
- auditar_adapter_migraciones_b — modelo heredado, razonamiento alto.
- Alcance: auditó seguridad/tenancy/restauración; ligó idempotencia al actor/scope/payload, endureció backup/restore, cleanup, guards de ref/TLS y ACL de bootstrap.
- crear_auditoria_sql_remota_b — modelo heredado, razonamiento alto.
- Alcance: creó la auditoría SQL estrictamente read-only y la migración de hardening RLS/ACL para `bootstrap_users`; corrigió aliases SQL y añadió regresión local.
- actualizar_documentacion_final — modelo heredado, razonamiento moderado.
- Alcance: consolidó TODO/PROJECT_NOTES/HANDOFF con la evidencia remota, el incidente restaurado y el requisito de repetir gates en un proyecto aislado; no ejecutó acciones remotas.
- implementar_modificadores_dominio — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: implementó catálogo y selección pura de modificadores, snapshots históricos compatibles, errores de dominio, exports y pruebas unitarias.
- auditar_diseno_modificadores — modelo gpt-5.6-luna, razonamiento alto.
- Alcance: definió invariantes/casos límite y auditó el cambio real; corrigió entradas no-string para producir errores de dominio deterministas y añadió regresión.
- implementar_payment_dominio — modelo gpt-5.6-terra, razonamiento alto.
- Alcance: implementó Payment/Refund inmutables, estados, idempotencia, scope, evidencia y pruebas.
- auditar_payment_dominio — modelo gpt-5.6-luna, razonamiento alto.
- Alcance: corrigió refunds parciales/total y conflictos idempotentes de scope/moneda; añadió regresiones.
- disenar_cash_register — modelo gpt-5.6-luna, razonamiento alto; interrumpido tras dejar una implementación funcional para limitar el tiempo de la subtarea.
- Alcance: diseñó e implementó la primera versión pura de CashRegister/CashMovement y pruebas.
- auditar_cash_register — modelo gpt-5.6-luna, razonamiento alto.
- Alcance: endureció cursor global por dispositivo, compensaciones acumuladas, evidencia/scope, integridad y saldo contado; amplió pruebas.
- inspect_gate_runner — modelo gpt-5.6-luna, razonamiento bajo.
- Alcance: revisión read-only del runner; confirmó que código 0 representa éxito técnico aunque `eligibleForAdr010Go` sea falso y enumeró brechas deliberadas.
- fix_auth_cleanup_json — modelo gpt-5.6-terra, razonamiento moderado.
- Alcance: corrigió serialización JSONB en bootstrap y dos rutas de cleanup, mapeo snake_case, fixtures del probe RLS y diagnóstico PostgreSQL redactado; añadió cuatro regresiones y validó 43 pruebas del spike.
- auditar_scaffold_next_web — modelo heredado, razonamiento moderado.
- Alcance: auditó de forma read-only los límites de un futuro workspace Next y confirmó que puede avanzar aislado usando los contratos existentes; CodeGraph reportó 90 archivos, 1,949 nodos y 7,744 relaciones.
- implementar_frontend_auth — modelo heredado, razonamiento moderado; interrumpido por corrección de alcance humano.
- Alcance: redactó `frontend.md`. El scaffold que alcanzó a preparar se eliminó por completo porque la solicitud autorizaba solo el plan, no la implementación; no tocó Supabase ni secretos.

## Nota de seguridad

No se publicaron secretos ni archivos. Las claves se mantuvieron en memoria y la contraseña permaneció en `.env.adr010.local` ignorado. Los gates crearon únicamente identidades/datos marcados y desechables en el proyecto aislado; el cleanup final dejó 0 usuarios y 0 filas operativas temporales. No hubo interacción Git remota ni cambios offline.

En la sesión 2026-08-29 se clonó y actualizó `main` por HTTPS hasta `f0091f30324263ad253d5ea395733f0d1f56200a`; no hubo push, merge, rebase ni force-push. No existe `.env` real versionado. El usuario añadió después `.certs/prod-ca-2021.crt`, que permanece local e ignorado y se usó en las verificaciones remotas documentadas.
